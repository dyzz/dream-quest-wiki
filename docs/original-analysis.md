# Dream Quest 原版解析与 Remaster 研究

## 结论摘要

`original/` 是一份 iOS App Store 形态的 Dream Quest 包，不是可直接运行的 Unity 工程。它包含 Unity 4.7.2f1 的 data files、IL2CPP 元数据和 arm64 Mach-O 主程序，但没有原始 UnityScript/C# 源码。

目前已经从原包提取出八类可用证据：

- Unity asset 索引：`extracted/unity_assets_index.json`
- Unity Resources 路径索引：`extracted/resource_paths.json`
- Texture2D PNG 导出与资源路径映射：`extracted/textures/`、`extracted/structured/texture_export_manifest.json`
- AudioClip MP3 导出：`extracted/audio/`、`extracted/structured/audio_export_manifest.json`
- Remaster-facing 素材目录：`extracted/structured/remaster_asset_catalog.json`
- Starter prototype 素材包：`extracted/structured/remaster_starter_asset_pack.json`
- Starter static-hosted browser prototype：`prototype/starter/index.html`
- IL2CPP metadata 字符串和标识符：`extracted/il2cpp_metadata_index.json`、`extracted/il2cpp_string_literals.json`
- IL2CPP 类型/字段/方法模型：`extracted/il2cpp_types_index.json`、`extracted/il2cpp_game_model.json`、`extracted/structured/key_systems.json`

关键内容清单已经交叉生成在 `extracted/content_inventory.json`：

- 卡牌资源路径 375 个，其中 364 个能匹配到同名 MonoScript 类。
- 地牢大图 actor/monster 资源路径 99 个，其中 93 个能匹配到同名 MonoScript 类。
- 职业资源 18 个形态/贴图入口，16 个匹配到职业脚本；其中包括 Druid human/wolf 形态和 Random。
- Combat ability 图标 32 个，全部匹配到脚本类。
- Dungeon talent 图标 51 个，其中 14 个直接匹配到 DungeonAction 类；剩余多数是 passive/talent 数据而不是一类一脚本。
- Texture2D 已导出 1115 个 PNG；2 个 0x0 Font Texture 无图像数据。ResourceManager 直接映射加少量名称 fallback 后，资源路径匹配 1093/1239，核心图像类别（card/combat ability/dungeon actor/profession/talent/achievement）全部有唯一 PNG 候选；剩余 146 个未匹配路径属于 material、prefab 或 font/other resource。
- AudioClip 已导出 13/13 个 MP3，输出到 `extracted/audio/by_container/sharedassets0/`，manifest 为 `audio_export_manifest.json`；这些是环境/菜单音乐和少量音效，不是 Bard/Sing 的 20 首玩法 song 文本目录。
- `remaster_asset_catalog.json` 已把内容条目接到 PNG：401 个 card 资源/规则 union、99 个 dungeon actor、18 个 profession、37 个 combat ability 和 51 个 dungeon talent icon；5/5 个素材覆盖检查通过。该目录显式标注 31 个 card-art-only 资源、26 个 structured-only card、26 个非 monster-catalog dungeon actor/feature、2 个 profession form/picker-only 资源、5 个无独立 inventory icon 的 combat ability 和 8 个 icon-only dungeon talent 资源。
- `remaster_starter_asset_pack.json` 已把 starter fixture 接到 PNG：10 张 starter card、3 个 starter monster 的 big/tile 图、Warrior big/little 图和 7 个 floor 1 视觉资源，共 21 个条目、25 个 PNG 引用；5/5 个素材包检查通过，所有引用文件均存在。
- `prototype/starter/` 已提供静态托管的浏览器原型，直接加载 starter/full catalog JSON 和导出 PNG，覆盖 Overview/Cards/Professions/Monsters/Dungeon/Remaster/Combat/Play 八个视图；Cards 视图可在 starter 子集和 370 张 recovered cards 全量目录间切换，Monsters 视图可在 starter 子集和 73 个 recovered monsters 全量目录间切换，Dungeon 视图展示 floor-1 seed snapshot 和 recovered generation rule summary，Remaster 视图展示当前 completion audit、阻塞项、artifact/view 覆盖、runtime boundary、area checklist 和下一步工程队列，Professions 视图浏览 16 个职业、37 个 combat ability、33 个 dungeon action 和 43 个 DungeonTalent，Play 视图可跑简化地牢移动、starter card combat、敌方回合、胜利清格和 reset，但仍不是原版准确 runtime。

## 原包取证

`Info.plist` 证据：

- App 名称：Dream Quest
- Bundle ID：`com.Peter.DreamQuest`
- 版本：`CFBundleShortVersionString = 1.12`
- 可执行文件：`DreamQuest`
- 目标平台：iPhoneOS，landscape

Unity/IL2CPP 证据：

- `original/Data/mainData` 内含 Unity 版本字符串：`4.7.2f1`
- `original/DreamQuest` 是 `Mach-O 64-bit executable arm64`
- `original/Data/Managed/Metadata/global-metadata.dat` 头部 magic 为 `0xfab11baf`，metadata version 为 `16`
- `original/DreamQuest` 的 `LC_ENCRYPTION_INFO_64` 标记 `cryptid = 1`，且从 `0x7000` 开始的第一页仍呈加密/不可反汇编形态；`0x8000` 之后可见正常 arm64 指令。因此控制流反编译可做，但需要跳过或处理加密页。
- `mainData` 中记录了场景：`Assets/Scenes/MainMenuScreen.unity`、`Assets/Scenes/Dungeon.unity`
- 代码主体来自 `Assembly-UnityScript.dll`，另有少量 `Assembly-CSharp.dll`、`Assembly-CSharp-firstpass.dll`、`UnityEngine.UI.dll`

## 已生成工具

- `tools/extract_unity_assets.py`：读取 Unity data/assets，输出对象类型、Texture2D、MonoScript、TextAsset 等索引。
- `tools/export_unity_textures.py`：用 UnityPy 导出可读 `Texture2D` 为 PNG，并生成 resource path 到导出 PNG 的 JSON/TSV manifest；当前导出 1115/1117 个 Texture2D，核心图像资源路径全部由 ResourceManager 或名称 fallback 匹配到唯一 PNG，2 个 0x0 Font Texture 无图像数据。
- `tools/build_remaster_asset_catalog.py`：把 `content_inventory`、卡牌/怪物/职业技能结构化目录和 texture export manifest 合并为 remaster-facing 素材目录；当前 606 个 union entry、5/5 覆盖检查通过，并区分 resource-only 与 structured-only 条目。
- `tools/build_remaster_starter_asset_pack.py`：把 `remaster_starter_slice.json`、`remaster_asset_catalog.json` 和 `texture_export_manifest.json` 合并成第一版 prototype 的 PNG-backed 素材包；当前 21 个条目、25 个 PNG 引用、5/5 检查通过。
- `prototype/starter/`：第一版静态托管 browser prototype，展示 starter cards / 全量 370 张 recovered cards、starter monsters / 全量 73 个 recovered monsters、全职业/技能目录、floor 1 dungeon seed、地牢生成规则摘要、remaster audit/roadmap 状态、deterministic combat fixtures 和对应 PNG，并提供简化 Play loop；需从仓库根目录用本地 HTTP server 打开，不代表原版准确 runtime。
- `tools/extract_resource_paths.py`：从二进制字符串里提取 Resources 风格路径，例如 `textures/cards/...`。
- `tools/extract_il2cpp_metadata_strings.py`：解析 IL2CPP metadata v16 的 header、字符串、string literal table。
- `tools/probe_il2cpp_tables.py`：用字符串堆启发式判断 IL2CPP 表结构，确认本包的 type/method/field/parameter 表布局。
- `tools/extract_il2cpp_metadata_model.py`：按 IL2CPP metadata v16 表结构展开类型、字段、方法、参数、属性和事件名。
- `tools/build_content_inventory.py`：交叉匹配资源路径、MonoScript 类和 IL2CPP 标识符。
- `tools/export_literal_corpora.py`：按内容范围导出 TSV 语料。
- `tools/build_structured_content.py`：从 string literal 中构建职业、歌曲、combat ability 文本 JSON。
- `tools/build_key_systems_summary.py`：从 IL2CPP 类型模型生成关键系统摘要。
- `tools/find_il2cpp_method_pointers.py`：在 Mach-O 中扫描 IL2CPP method pointer 数组。
- `tools/disassemble_il2cpp_method.py`：通过 metadata `method_index` 精确反汇编单个 IL2CPP 方法，并给 string literal 调用加注释；`--stop-at-branch` 可用于截断 IL2CPP 尾调用 thunk。
- `tools/extract_disassembly_literals.py`：从带注释反汇编中导出 literal 引用 JSON/TSV。
- `tools/scan_il2cpp_calls.py`：扫描 IL2CPP arm64 方法体中的直接 `bl` 调用，用于验证构造函数是否有普通 call xref。
- `tools/scan_il2cpp_field_accesses.py`：扫描 IL2CPP arm64 方法体中指定对象偏移的直接内存访问，用于定位字段读写证据。
- `tools/extract_card_initializers.py`：批量反汇编卡牌 `PythonInitialize`，导出类型、显示名、规则文本、价格、tier、AI 值、decay 目标和可确认的 `elementalAffinity`。
- `tools/extract_card_effect_index.py`：把 `cards_partial` 和 IL2CPP 类型模型交叉，导出每张卡的 `PlayEffect`、`PythonPlayEffect`、`ReactionEffect*`、`Trigger*` 等效果方法入口索引。
- `tools/extract_card_effect_slots.py`：从 IL2CPP 类型模型导出卡牌效果相关类型的虚方法 slot 反查表。
- `tools/extract_card_effect_tailcalls.py`：批量识别简单 `PythonPlayEffect` 虚方法尾调用，恢复 slot、操作名和立即数参数。
- `tools/extract_card_effect_calls.py`：从卡牌 play/reaction/other custom 方法中导出保守虚调用、直接调用、字段访问和 literal trace。
- `tools/build_cards_remaster_seed.py`：把 `cards_partial`、`card_data`、卡牌效果入口、简单 tailcall 和复杂 trace 合并成 remaster-facing 的 `cards.json` / `cards.tsv` 目录种子，并额外生成保守的 `runtime_rule` 归一化步骤。
- `tools/analyze_card_access_rules.py`：把 `CardData.HasAccess` / `HasNever` 的 requirement gate 应用到全部 `CardData` 行，输出 base-unlocked、requirement-gated 和 never 状态，以及 requirement 到卡牌/achievement 的映射。
- `tools/run_card_access_rules_smoke.py`：验证 `never`、`DAMAGE2 -> Execute`、`WINCHAOS -> WildStrike1` 等 access-rule fixture；当前 17/17 通过。
- `tools/analyze_card_metadata_recovery.py`：交叉 `card_data.json`、`cards.json`、职业 class bias、颜色/子类型访问器扫描、slot 98/123 虚槽扫描和关键反汇编，输出 `card_metadata_recovery.json` / `.tsv`；当前 20/20 通过，记录 bias entry 语义、元素亲和度覆盖、Card colors/subtypes 访问器形态、静态/全局 offset collision 排除、Card 子类 `PythonInitialize` 虚槽初始化命中为 0，以及 per-card colors/subtypes 仍不能安全填值的证据边界。
- `tools/run_card_runtime_smoke.py`：读取 `cards.json`，用 `tools/remaster_runtime.py` 验证代表性简单 tailcall、structured effect、手工 control-flow 恢复、state_write、construct 和 recorded card action request；当前 45/45 通过，但仍不是原版准确 timing/RNG/UI/equipment lifecycle/AI。
- `tools/run_card_runtime_matrix_smoke.py`：读取 `cards.json`，按每张卡的每个 recovered source method 执行共享 runtime；当前 15/15 通过，覆盖 599 个 source-method case、424 个 effect/recorded 可执行 case、633 个 effect event 和 60 个 recorded event，但仍不是原版准确 timing/RNG/UI/equipment lifecycle/AI。
- `tools/run_card_manual_control_flow_replay.py`：专门执行 5 张手工反汇编恢复卡牌的正向和跳过分支 fixture，输出 `card_manual_control_flow_replay.json` / `.tsv`；当前 35/35 通过，覆盖 19 个 branch fixture，但仍不是原版准确 timing/reaction/UI replay。
- `tools/run_card_recorded_step_replay.py`：专门执行卡牌 recorded state/object 步骤，输出 `card_recorded_step_replay.json` / `.tsv`；当前 16/16 通过，覆盖 52 张卡、60 个 recorded step 和 56 个 source-method case，但仍不是 typed Unity object lifecycle/UI ownership replay。
- `tools/run_card_lifecycle_replay.py`：专门执行 `Card.DestroyDie` / `Bounce` / `Flick` / `Destroy` / `WipeClean` / base lifecycle hook 的确定性状态机夹具，输出 `card_lifecycle_replay.json` / `.tsv`；当前 14/14 通过，覆盖 6 个 recovered lifecycle case，但仍不是 Unity physical object/coroutine replay。
- `tools/run_card_equipment_lifecycle_matrix.py`：专门执行 42 张 Equipment 卡的 enter/leave、turn、trigger、CheckCounters 和 recorded state/lifecycle source-method fixture，输出 `card_equipment_lifecycle_matrix.json` / `.tsv`；当前 18/18 通过，覆盖 74 个 recovered source-method case、92 个 effect event 和 27 个 lifecycle/state-write recorded event，但仍不是原版 equipment timing/object identity replay。
- `tools/run_card_zone_replay.py`：专门执行 `Card.Exile` / `Die` / `Discard` / `MoveToGraveyard` / `GetAction` 的确定性状态机夹具，输出 `card_zone_replay.json` / `.tsv`；当前 14/14 通过，覆盖 6 个 recovered zone/action case，但仍不是 Unity physical object/coroutine/action-construction replay。
- `tools/run_card_payment_replay.py`：专门执行 `Card.ResourcesOff` / `CanAfford` / `PayFor` / `WillBeCountered` 的确定性状态机夹具，输出 `card_payment_replay.json` / `.tsv`；当前 17/17 通过，覆盖 9 个 recovered payment/counter case，但仍不是 Player.PayFor internals、Unity UI refresh 或 original combat timing replay。
- `tools/run_card_play_replay.py`：专门执行 `Card.PlayMe` / `Player.PlaySideEffects` 的确定性状态机夹具，输出 `card_play_replay.json` / `.tsv`；当前 17/17 通过，覆盖 9 个 recovered play/side-effect case，但仍不是 reaction marker object semantics、subclass PlayEffect timing、Unity visual order 或 coroutine replay。
- `tools/analyze_creature_ai_weight_recovery.py`：交叉 IL2CPP model、`Creature.AIWeight` 反汇编和 Card 初始化证据，恢复 Creature AI 权重公式，输出 `creature_ai_weight_recovery.json` / `.tsv`；当前 37/37 通过，但只是静态公式 fixture，live combat-state replay 仍为 false。
- `tools/analyze_card_get_ai_value_adjustment_recovery.py`：交叉 `Card.GetAIValue`、`Card.get_player`、`ScenarioBase.ChangeValue`、`Game.StartGame` / `VirtualCopyInto` 反汇编和 IL2CPP model，把 `Card.GetAIValue` slot 105 定位为 `Game.scenario -> ScenarioBase.ChangeValue`，并确认基类 `ChangeValue` 是 identity/no-op，输出 `card_get_ai_value_adjustment_recovery.json` / `.tsv`；当前 33/33 通过，但 live combat-state replay 仍为 false。
- `tools/run_ai_value_runtime_smoke.py`：用 `tools/remaster_runtime.py` 执行 `Card.AIWeight`、`Card.GetAIValue` scenario identity、`Creature.AIWeight` 和 real-hero bonus fixture，输出 `ai_value_runtime_smoke.json` / `.tsv`；当前 18/18 通过，但仍不是 live original combat-state replay。
- `tools/run_card_ai_value_matrix_smoke.py`：用 `tools/remaster_runtime.py` 对 370 张 recovered cards 全量执行 `Card.AIWeight` 和基类 `Card.GetAIValue` scenario identity，输出 `card_ai_value_matrix_smoke.json` / `.tsv`；当前 14/14 通过，覆盖 370 个 AIWeight case、370 个 GetAIValue case，scenario identity mismatch 为 0，但仍不是 live original combat-state replay。
- `tools/run_card_ai_priority_matrix_smoke.py`：用 `tools/remaster_runtime.py` 对 370 张 recovered cards 全量执行 `ActionPlay.AIPriority` 的未反制/反制分支和基类 `ActionPlay.AIUseValue`，输出 `card_ai_priority_matrix_smoke.json` / `.tsv`；当前 20/20 通过，覆盖 370 个未反制 priority case、370 个反制 priority case、740 个 AIUseValue case，273 张卡在未反制/反制分支间 priority 会变化，但仍不是 original target legality 或 turn-level AI replay。
- `tools/run_card_actionplay_bridge_matrix_smoke.py`：用 `tools/remaster_runtime.py` 对 370 张 recovered cards 全量构造 normal `ActionPlay` candidate bridge，并把合成全卡手牌跑过 recovered `GetAllActionsInternal` / `GetAllActions` 枚举过滤与 `GetAllActionsPlusTargets` 包装形态，输出 `card_actionplay_bridge_matrix_smoke.json` / `.tsv`；当前 42/42 通过，覆盖 370 个 candidate case、370 个 full-hand filtered candidate 和 370 个 synthetic action+target pair，null action、payload/cost/slot mismatch、candidate order mismatch、pair order mismatch 均为 0，但仍不是 original `GetActions` list identity、target legality、turn policy 或 RNG/list replay。
- `tools/analyze_card_ai_value_recovery.py`：交叉 `cards.json`、IL2CPP model、Card/Creature AI 反汇编、Card.GetAIValue adjustment recovery、AI value runtime smoke 和 AI dispatch plan，恢复 `Card.AIWeight`、`Card.GetAIValue` 形态、基类 `AIPlayValue/AIFlatValue/AIMultValue` 常量，并纳入 scenario adjustment、runtime fixture 与 `Creature.AIWeight` 公式恢复，输出 `card_ai_value_recovery.json` / `.tsv`；当前 45/45 通过，但完整 AI value replay 仍为 false。
- `tools/analyze_card_rng_recovery.py`：消费 23 张卡牌 RNG/shuffle 候选、`Game.InGameRandomRange/Float`、`Deck.Shuffle/RandomCardName`、`Player.RandomExile`、`Card.ForceRandomDiscard`、`CardFinder.RandomAvailableNonEquipmentCard`、`Stone.PlayEffect` 和关键反汇编，输出 `card_rng_recovery.json` / `.tsv`；当前 27/27 通过，恢复静态 helper/list-order 形态并二进制确认 Stone 的 Curse 插入、Deck.Shuffle 和 ForceDiscard 形态，且补充 InfernalContract 的 Curse+Deck.Shuffle+DiscardAll 形态，但 original RNG stream 与 shuffle/index stream replay 仍为 false。
- `tools/analyze_card_context_dependency_index.py`：读取 `cards.json`、card runtime accuracy gaps、card runtime matrix 和 Card ActionPlay bridge，把 214 张卡的 722 个 read/context dependency step 按 remaster 子系统 owner 分类，输出 `card_context_dependency_index.json` / `.tsv`；当前 14/14 通过，11 个 owner 队列、0 个 uncategorized step，但 original value-flow replay 仍为 false。
- `tools/analyze_card_effect_value_flow_index.py`：读取 `cards.json`、card effect calls、card runtime matrix smoke 和 card runtime accuracy gaps，把 179 个 `effect_without_immediate_args` step 拆成 zero-parameter receiver effect、combat-state payload、string/list payload、choice/action payload、object-graph payload、deck/card payload、visual-stack payload 和 game-turn context 队列，输出 `card_effect_value_flow_index.json` / `.tsv`；当前 20/20 通过，覆盖 115 张卡、71 个 operation、119 个零参数 receiver effect、60 个仍需参数值恢复的 step，unclassified=0，但 original parameter-value 与 receiver-identity replay 仍为 false。
- `tools/analyze_card_original_accuracy_flag_index.py`：递归读取 39 个 card/runtime/AI/action pipeline artifact，把剩余 false original-accuracy/readiness flag 规范化为 `card_original_accuracy_flag_index.json` / `.tsv`；当前 13/13 通过，记录 362 个唯一 false flag entry、8,052 次 occurrence、9 个工作队列（card rule completion、value-flow/parameter/receiver identity、RNG/index stream、AI candidate/target/turn policy、branch timing、typed lifecycle/zone/payment/play、action/reaction/visual turn flow、metadata colors/subtypes、global readiness）且 uncategorized=0。
- `tools/analyze_card_runtime_accuracy_gaps.py`：读取 `cards.json`、card runtime smoke/matrix、card manual-control-flow replay、card recorded-step replay、card lifecycle replay、card equipment lifecycle matrix、card zone replay、card payment replay、card play replay、card context dependency index、card effect value-flow index、card original-accuracy flag index、card RNG recovery、runtime engine coverage、card metadata recovery、card AI value recovery、card AI value matrix smoke、card AI priority matrix smoke、card ActionPlay bridge matrix smoke 和 AI value runtime smoke，把 interpreter 覆盖后的剩余原版准确性缺口拆成 manual control-flow、recorded state/construct/lifecycle、read/context、computed-value、RNG/shuffle、UI/choice/visual、equipment lifecycle、AI 和 colors/subtypes 队列；当前 169/169 通过，并显式检查 read/context owner queue、computed/receiver value-flow queue、card false-flag index、Blur/Incorporeal/StormShape/VaporForm 的 `EffectPlayerAttribute..ctor` 二进制恢复、Anticipate/Bewitch/Charm/Enthrall 的 opponent-hand Action/TriggerOnce 二进制恢复、Scimitars 的 ActionScimitars/DrawExNihil/Temporary 二进制恢复、InfernalContract/SoulCrush 的 direct PlayEffect 二进制恢复、全 equipment lifecycle/trigger matrix fixture、全卡 AIWeight/GetAIValue matrix fixture、全卡 ActionPlay priority/AIUseValue matrix fixture，以及全卡 ActionPlay candidate bridge/full-hand enumeration/action-target-pair fixture。
- `tools/analyze_ai_policy_recovery_gaps.py`：交叉 IL2CPP game model、卡牌/怪物 runtime gap、AI 直接调用扫描、虚拟槽位调用扫描、Card AI value recovery、AI value runtime smoke、card AI priority matrix smoke、card ActionPlay bridge matrix smoke 和关键 AI 反汇编，把 AIPlayer/action/card/object AI 方法、间接派发证据、卡牌 AI scalar/base-value/ActionPlay priority/ActionPlay bridge/hand-enumeration/action-target-pair 与怪物 AI policy 缺口整理成 `ai_policy_recovery_gaps.json` / `.tsv`；当前 54/54 通过。
- `tools/analyze_ai_virtual_dispatch_plan.py`：消费 `ai_policy_virtual_slot_xrefs.json` 与 `ai_policy_recovery_gaps.json`，把 raw virtual-slot 命中拆成 AI policy caller/slot 恢复队列，输出 `ai_virtual_dispatch_plan.json` / `.tsv`；当前 8/8 通过。
- `tools/analyze_ai_action_priority_recovery.py`：消费 AI/action 反汇编、dispatch plan 和 `cards.json`，恢复第一条 action-priority 公式，输出 `ai_action_priority_recovery.json` / `.tsv`；当前 12/12 通过。
- `tools/analyze_ai_highest_priority_selection.py`：消费 `AIPlayer.GetHighestPriorityAction` 反汇编和 action-priority 恢复结果，恢复最高优先级 action 的两遍扫描与随机并列选择形态，输出 `ai_highest_priority_selection.json` / `.tsv`；当前 15/15 通过。
- `tools/analyze_ai_action_candidate_recovery.py`：消费 `AIPlayer.GetAllActions*` / `AddHandCards` 反汇编和最高优先级选择恢复结果，恢复候选 action 构造、手牌 action 抽取和 action+target pair 包装形态，输出 `ai_action_candidate_recovery.json` / `.tsv`；当前静态检查通过，但 original candidate replay 与 target legality replay 仍未完成。
- `tools/run_ai_action_candidate_replay.py`：专门执行 `AIPlayer.GetAllActionsInternal` / `GetAllActions` / `GetAllActionsPlusTargets` 的确定性状态机夹具，输出 `ai_action_candidate_replay.json` / `.tsv`；当前 13/13 通过，覆盖 5 个 recovered candidate/action+target case，但仍不是 Unity collection identity、全量 `ActionBase.IsValid` override、target legality、target-set construction、AI turn ordering 或 RNG replay。
- `tools/analyze_ai_target_legality_recovery.py`：消费 `ActionBase.AIIsValidPlusTargets`、`ActionBase/ActionCrush/ActionSingleTargetDiscard.AIGetTargets` 和 `AIPlayer.Get*Target*` 反汇编，恢复 target result 布局、AI target filter、discard target heuristic、target sampling 和 target-set construction 形态，输出 `ai_target_legality_recovery.json` / `.tsv`；当前 44/44 通过，但 target legality replay 与 target-set replay 仍为 false。
- `tools/run_ai_target_legality_replay.py`：专门执行 `ActionBase.AIIsValidPlusTargets`、默认/特化 `AIGetTargets`、`AIPlayer.GetAITargets` 和 target-set construction 的确定性状态机夹具，输出 `ai_target_legality_replay.json` / `.tsv`；当前 21/21 通过，覆盖 10 个 recovered target-legality/target-set case，但仍不是 Unity target wrapper identity、全量 `IsValid` / `IsLegalTarget` override、完整 target-set combination helper、list ownership 或 RNG stream replay。
- `tools/analyze_target_wrapper_identity_recovery.py`：消费 IL2CPP model、AI target legality recovery/replay、AI turn policy recovery、ActionBase/AIPlayer disassembly、TargetFinder disassembly 和 `Game.RandomRange` disassembly，输出 `target_wrapper_identity_recovery.json` / `.tsv`；当前 42/42 通过，恢复 `TargetFinderParameters` / `TargetTypes` / `TargetFinderBase` / `TargetFinderMulti` metadata、target result/cache offsets、target wrapper payload read、target-set list mutation、`Game.RandomRange(a,b)` inclusive upper bound、AI target sampling tail-swap removal 和 random-order target-set shuffle 静态形态，但 original `TargetWrapper` / target-set identity replay 仍为 false。
- `tools/analyze_ai_rng_list_order_recovery.py`：消费 target wrapper identity、highest-priority selection、AI target legality/replay、AI turn policy/replay、`Game.RandomRange` 和 AIPlayer/AIPlayerRandom 反汇编，输出 `ai_rng_list_order_recovery.json` / `.tsv`；当前 22/22 通过，恢复 inclusive RNG upper bound、highest-priority tie RNG、candidate selection/tail-swap removal、provider target-set RNG、target tail-swap sampling、target-set Fisher-Yates shuffle 和 `PerformRandomAction` RNG 静态形态，但 original AI RNG stream 与 candidate/list ownership replay 仍为 false。
- `tools/analyze_ai_turn_policy_recovery.py`：消费 `AIPlayerRandom.AmIDone` / `Behave` / `GetAITargetsRandom` / `AIPerformAction` / `AIPass`、`AIPlayer.AIPerformAction` / `PerformRandomAction` 和 pass 反汇编，恢复 Random AI turn selection、AmIDone action-counter gate、随机候选 list mutation、目标随机采样、action invocation packaging 和 pass/end-turn 形态，输出 `ai_turn_policy_recovery.json` / `.tsv`；当前 43/43 通过，但 AI turn replay 与 action execution replay 仍为 false。
- `tools/run_ai_turn_policy_replay.py`：专门执行 `AIPlayerRandom.AmIDone` / `Behave` / `GetAITargetsRandom` / `AIPerformAction` / `AIPass`、`AIPlayer.AIPerformAction` / `PerformRandomAction` 的确定性状态机夹具，输出 `ai_turn_policy_replay.json` / `.tsv`；当前 23/23 通过，覆盖 12 个 recovered turn/pass/invocation case，但仍不是原版 candidate list ownership、Unity RNG stream、完整 action side effects、UI/end-turn behavior 或 full monster-turn replay。
- `tools/analyze_action_execution_recovery.py`：消费 `ActionBase.Perform/Resolve`、`ActionPlay.PayCosts/PerformAction`、`ActionPass.Perform`、`ActionAddToDeck.Perform`、`ActionDungeonChoose.Perform`、`AIPhysical.GetStackInput` 和 `GamePhysical.AddToVisualStack*` 反汇编，恢复 action execution / card-play / visual-stack 派发形态，输出 `action_execution_recovery.json` / `.tsv`；当前 32/32 通过，但 original action execution、card play 和 visual-stack replay 仍为 false。
- `tools/run_action_execution_replay.py`：专门执行 `ActionBase.Perform/Resolve`、`ActionPlay` 桥、`ActionPass`、special action、`AIPhysical.GetStackInput` 和 `GamePhysical.AddToVisualStack*` 的确定性状态机夹具，输出 `action_execution_replay.json` / `.tsv`；当前 19/19 通过，覆盖 11 个 recovered action/visual-stack case，但仍不是 reaction/SBE manager、target wrapper type identity、Unity coroutine/yield 或 UI object graph replay。
- `tools/analyze_action_runtime_accuracy_gaps.py`：汇总 action execution replay、AI candidate/target/turn replay、全卡 ActionPlay bridge/hand-enumeration/action-target-pair matrix smoke、target wrapper identity recovery、AI RNG/list-order recovery、card payment/play/zone/lifecycle/equipment replay、reaction/SBE cleanup recovery、visual-stack coroutine recovery、Player payment recovery、pass/end-turn recovery、card play pipeline recovery 和 starter combat fixture，把确定性覆盖后的执行层剩余缺口拆成 11 类队列，输出 `action_runtime_accuracy_gaps.json` / `.tsv`；当前 76/76 通过，汇总 640 个上游检查，确认 deterministic action/runtime、AI-to-action、card pipeline 与 starter fixture coverage 均完整，且 reaction/SBE、visual-stack coroutine、Player.PayFor/payment UI bridge、pass/end-turn、target wrapper/target-set identity、target-set ordering、RNG inclusive boundary、AI RNG/list-order 和 all-equipment lifecycle/trigger fixture 已有静态形态证据，但 original action runtime 仍为 false。
- `tools/analyze_reaction_sbe_cleanup_recovery.py`：消费 `ActionBase.TriggerCleanUp/ShouldCheckSBEs`、`Game.CheckStateBasedEffects/Check*Reactions`、`ActionDelayedReaction/ActionAddCounter`、`ReactionCard` 和 `Counterspell` 反汇编，把 reaction/SBE cleanup 从 action runtime gap 推进为静态形态证据，输出 `reaction_sbe_cleanup_recovery.json` / `.tsv`；当前 29/29 通过，但 original reaction/SBE replay 与 prototype runtime readiness 仍为 false。
- `tools/analyze_visual_stack_coroutine_recovery.py`：消费 `GamePhysical.AddToVisualStack*`、`NoYield/EndNoYield`、`SuppressVisualStack/UnSuppressVisualStack`、`VisualStackCount/Pop`、`HandleVisualStack/VisualStackSpin`、`VirtualWait/Waiter`、`EndGame` 和 compiler-generated coroutine 反汇编，把 visual-stack/coroutine yield 缺口推进为静态形态证据，输出 `visual_stack_coroutine_recovery.json` / `.tsv`；当前 42/42 通过，但 original visual-stack replay 与 prototype runtime readiness 仍为 false。
- `tools/analyze_player_payment_recovery.py`：消费 `Card.PayFor`、`Player.CanAfford/ManaOff/PayFor/SetActions/GainMana/UpdateMana/UnOneFreeSpell` 反汇编，把 Player.PayFor/payment UI 缺口推进为静态形态证据，输出 `player_payment_recovery.json` / `.tsv`；当前 45/45 通过，但 original player payment replay、Unity payment UI replay 与 prototype runtime readiness 仍为 false。
- `tools/analyze_pass_end_turn_recovery.py`：消费 `ActionPass.Perform`、`AIPlayer.DoAIPass/AIPass/PhysicalAIPass`、`Player.Pass/NetPass/EndTurn/StartTurn/CanEndTurn`、`Game.EndTurn/StartTurn/BasicStartTurn/StartTurnAfterDraw` 和 start/end-turn trigger 反汇编，把 pass/end-turn 缺口推进为静态形态证据，输出 `pass_end_turn_recovery.json` / `.tsv`；当前 92/92 通过，但 original pass/end-turn replay 与 prototype runtime readiness 仍为 false。
- `tools/analyze_card_play_pipeline_recovery.py`：消费 `Card.CanAfford` / `ResourcesOff` / `GetActionCost` / `GetManaCost` / `PayFor` / `WillBeCountered` / `PlayMe` / `Exile` / `Die` / `DestroyDie` / `OnDeath` / `Bounce` / `Flick` / `ShouldBounceOnDeath` / `Destroy` / `WipeClean` / `Discard` / `MoveToGraveyard` / `GetAction` 和 `Player.PlaySideEffects` 反汇编，恢复 Card.PayFor/Card.WillBeCountered/Card.PlayMe 下面的成本、支付、反制边界、reaction、side-effect、牌区、DestroyDie/Bounce/Flick/Destroy/WipeClean lifecycle 和 action lookup 静态形态，输出 `card_play_pipeline_recovery.json` / `.tsv`；当前 50/50 通过，starter combat 已消费 normal-path ResourcesOff/CanAfford 费用修正 catalog、支付分支 catalog、WillBeCountered Enemy.WillCounter priority boundary、Card.PlayMe order、inactive reaction/alternate-branch catalog、Player.PlaySideEffects branch boundary/catalog、zone lifecycle/GetAction catalog、discard-to-Die boundary、Die graveyard boundary、DestroyDie/Bounce/Flick/Destroy/WipeClean static lifecycle catalog、deterministic lifecycle fixture、Sword equipment lifecycle boundary、Sword.CheckCounters equipment trigger boundary、Sword trigger gate boundary、出牌和牌区 fixture，但全量 card payment、card play、card zone/Die/Exile/DestroyDie/Bounce/Flick/Destroy/WipeClean 和 equipment lifecycle replay 仍为 false。
- `tools/analyze_current_user_loading.py`：结构化 `GameManager.GetCurrentUser` / `LoadCurrentUser` / `SetCurrentUser` 与 `DungeonUser.LoadUser` / `CreateUser` / `Load` / `Save` 的 current-user、profile 路径和属性数组读写证据，输出 `current_user_loading.json` / `current_user_loading.tsv`。
- `tools/run_current_user_loading_smoke.py`：验证 PlayerPrefs key、`profiles/<user>/player.hpl` / `player.hpl.bkp` 路径、`DungeonUser.attributes` offset `0x80`、`(\d+): (\d+)\D*` 属性解析、cache invalidation 和 missing-file -> `CreateUser` fallback；当前 19/19 通过。
- `tools/dungeon_user_save_codec.py` / `tools/run_dungeon_user_save_codec_smoke.py`：基于已恢复的 `DungeonUser.Load` / `Save` 文本证据提供保守 save codec；当前用合成 `player.hpl` 形态文本验证属性 pair、seen-list、history round-trip，并把解析出的 `DAMAGE2` / `WINCHAOS` UserAttribute 喂给 CardFinder unlock fixture，20/20 通过；真实 `player.hpl` 样本和 Unity PlayerPrefs 仍未验证。
- `tools/run_current_user_save_cardfinder_smoke.py`：把 synthetic PlayerPrefs `currentUser`、`profiles/<user>/player.hpl` 加载/解析和 CardFinder current-user unlock pool 串成一条 integration fixture；当前 33/33 通过，候选池从 31/75 扩到 33/86 并新增 `Execute` / `WildStrike1`，但真实 `player.hpl` 样本和原版 Unity PlayerPrefs 存储仍未验证。
- `tools/inventory_real_save_samples.py`：扫描工作区里的真实 `player.hpl` / `player.hpl.bkp` / `.hpl` / PlayerPrefs-like 文件并输出 `real_save_sample_inventory.json` / `.tsv`；当前 4/4 通过，候选文件数为 0，因此真实 save/PlayerPrefs 仍是阻塞缺口而不是已验证输入。
- `tools/extract_monsters_from_disassembly.py`：从 `MonsterList.GetMonsterList` 的字段写入模式恢复 `MonsterData` 表。
- `tools/extract_monster_initializers.py`：批量反汇编怪物 `PythonBuildAttributes`，导出基础战斗属性、base deck、起手偏好、起始装备和 level-up literal。
- `tools/extract_monster_behavior_index.py`：把 `monster_initializers` 和 IL2CPP 类型模型交叉，导出每个怪物的行为方法入口索引。
- `tools/extract_monster_levelup_calls.py`：从怪物 `LevelUpDeck` 方法中保守提取 `DeckAdd`、`DeckReplace` 和 powers 文本写入调用序列。
- `tools/extract_monster_behavior_calls.py`：从怪物 `ApplyToPlayer`、`StartTurn`、`ForbidPlay` 和特殊方法中导出保守虚调用 trace，并截断外部尾跳以避免混入相邻 IL2CPP 方法。
- `tools/build_monster_catalog_seed.py`：把 `MonsterData`、怪物初始化、level-up trace 和行为 trace 合并成 remaster-facing 的 `monster_catalog.json` / `monster_catalog.tsv` 目录种子。
- `tools/extract_profession_initializers.py`：批量反汇编职业 `Initialize` 和 `ClassBiases`，导出起始属性、起始牌组和职业 bias。
- `tools/extract_profession_behavior_index.py`：把 `professions_initializers` 和 IL2CPP 类型模型交叉，导出每个职业的行为方法入口索引。
- `tools/extract_profession_behavior_calls.py`：从职业的 player-application、action、combat-flow 和特殊方法中导出保守虚调用 trace，并用少量站点覆盖处理 LINQ/枚举器 cast 与路径敏感分支合流。
- `tools/extract_profession_action_loadouts.py`：解析 `Profession*.AddDungeonActions` / `AddCombatActions` 中的直接构造和字符串引用，导出职业到地城动作/战斗能力的装载关系。
- `tools/build_profession_skills_remaster_seed.py`：把职业静态文本、起始属性/牌组、职业动作装载、combat ability、dungeon action 和 dungeon talent 合并成 `profession_skills.json` / `profession_skills.tsv` 目录种子。
- `tools/run_profession_starting_profile_smoke.py`：读取 `profession_skills.json` 和 `cards.json`，验证 16 个职业起始属性、起始牌组、class bias、起始 actor 投影和起始牌引用；当前 70/70 通过，但仍不代表等级奖励、cooldown tick、targeting、UI 或 TalentBase 学习流程。
- `tools/run_profession_skill_runtime_smoke.py`：读取 `profession_skills.json` 和 `profession_skill_scheduler_recovery.json`，用 `tools/remaster_runtime.py` 验证职业 loadout、combat ability / dungeon action cooldown、CombatAbility.Use / DungeonAction.OnClick / ClearMind / WinFight cooldown order / ready-message-before-WinFight scheduler fixture、代表性职业/技能/地城动作效果和 43 条 DungeonTalent 的 60 个 handler effect；当前 65/65 通过，但仍不是原版准确 cooldown/targeting/UI/talent-learning 流程。
- `tools/run_profession_skill_runtime_matrix_smoke.py`：读取 `profession_skills.json`，执行全部职业、combat ability、dungeon action recovered source method 和全部 DungeonTalent handler；当前 19/19 通过，覆盖 154 个 source-method case、120 个 effect/recorded 可执行 case、266 个 effect event、38 个 recorded event 和 60 个 talent handler event，但仍不是原版准确 cooldown/targeting/UI/talent-learning 流程。
- `tools/run_profession_skill_scheduler_matrix.py`：读取 `profession_skills.json` 和 `profession_skill_scheduler_recovery.json`，把 CombatAbility / DungeonAction 基类 scheduler fixture 扩展到全部 60 个 cooldown initializer；当前 24/24 通过，覆盖 445 个 deterministic scheduler case，但 original scheduler / targeting / UI / TalentBase replay 仍为 false。
- `tools/run_profession_skill_recorded_replay.py`：专门执行职业/技能的 hand-recovered control-flow/lifecycle、recorded state_write 和 DungeonTalent handler fixture，输出 `profession_skill_recorded_replay.json` / `.tsv`；当前 39/39 通过，覆盖 4 个 manual entry、6 个 manual step、38 个 state-write recorded event 和 43 个 DungeonTalent handler case 的 60 个 handler event，但 original cooldown scheduler、targeting/UI 和 TalentBase learning replay 仍为 false。
- `tools/run_profession_skill_targeting_ui_bridge.py`：读取 `profession_skills.json` 和 `profession_skill_runtime_matrix_smoke.json`，复用 targeting/UI 术语分类，把 34 个 UI/selection entry、21 个 target/legality entry 和 85 个 focused source-method case 通过共享 runtime 桥接为 deterministic fixture；当前 19/19 通过，但 original targeting/UI action-object replay 仍为 false。
- `tools/analyze_profession_skill_context_dependency_index.py`：交叉 `profession_skills.json`、runtime accuracy gaps、source-method matrix smoke、targeting/UI bridge、scheduler matrix 和 recorded replay，把职业/技能/地城动作中的 read/context dependency step 分类到 runtime owner queue，输出 `profession_skill_context_dependency_index.json` / `.tsv`；当前 31/31 通过，覆盖 67 个 dependency entry、449 个 dependency step 和 13 个 owner queue，uncategorized=0，但 original value-flow、scheduler 和 targeting/UI replay 仍为 false。
- `tools/analyze_profession_skill_effect_value_flow_index.py`：交叉 `profession_skills.json`、source-method matrix smoke 和 runtime accuracy gaps，把职业/技能/地城动作中 no-immediate-arg effect step 分类为 parameter/receiver value-flow queue，输出 `profession_skill_effect_value_flow_index.json` / `.tsv`；当前 23/23 通过，覆盖 69 个 entry、196 个 effect step、108 个 zero-parameter receiver effect 和 88 个 parameter-value-required step，9 个 argument-flow queue 均无 unclassified，但 original parameter-value 与 receiver-identity replay 仍为 false。
- `tools/analyze_profession_skill_original_accuracy_flag_index.py`：交叉 14 个职业/技能 runtime artifact，把剩余 false original-accuracy / readiness flag 规范化为 `profession_skill_original_accuracy_flag_index.json` / `.tsv`；当前 13/13 通过，覆盖 105 个唯一 false flag entry、2,548 次 occurrence 和 8 个 work queue，uncategorized=0。
- `tools/analyze_profession_skill_runtime_accuracy_gaps.py`：交叉 `profession_skills.json`、职业/技能 runtime smoke、source-method matrix smoke、scheduler matrix、recorded replay、targeting/UI bridge、read/context dependency index、effect value-flow index、profession/skill original-accuracy flag index、profession/skill RNG recovery、profession/skill scheduler recovery、starting profile smoke、TalentBase reachability、TalentBase learning UI recovery 和 runtime engine coverage，输出 `profession_skill_runtime_accuracy_gaps.json` / `.tsv`；当前 132/132 通过，确认 129/129 条 profession/skill/action/talent 规则仍为 `executable_rule_complete=false`，并把 cooldown/legality/scheduler replay、targeting/UI selection、read/context dependency、parameter-value/receiver-identity、state writes/recorded events、manual control flow、RNG/shuffle、DungeonTalent handler、TalentBase reachability / learning UI 和 starting-profile integration 拆成原版准确性队列。
- `tools/analyze_profession_skill_rng_recovery.py`：交叉 `profession_skills.json`、DesperatePrayer / FindMonster / FindTreasure / DungeonTalent handler / CardFinder / Game RNG 反汇编和 runtime accuracy gap，输出 `profession_skill_rng_recovery.json` / `.tsv`；当前 20/20 通过，恢复 DesperatePrayer、FindMonster、FindTreasure、Charismatic 和 Hoard 的静态 RNG/random-reward 形态，并明确原版 RNG stream 与 shuffle/index stream replay 仍为 false。
- `tools/analyze_profession_skill_scheduler_recovery.py`：消费 `CombatAbility`、`ActionCombatAbilityBasic`、`DungeonAction`、`Dungeon.EndFight`、`Dungeon.WinFight`、`GamePhysical.EndGame` coroutine、`DungeonPlayer.WinFight`、`DungeonPlayer.CoolingDownAbilities` 基类反汇编和 WinFight/CoolingDownAbilities xref 扫描，恢复职业/技能共享 cooldown/use/OnClick/WinFight tick/ready-message 调度形态，输出 `profession_skill_scheduler_recovery.json` / `.tsv`；当前 68/68 通过，但 alternate coroutine branch replay、targeting/UI replay 和原版准确规则仍为 false。
- `tools/extract_combat_ability_initializers.py`：批量反汇编 `CombatAbility*.Initialize`，导出 cooldown 和初始 cooldown。
- `tools/extract_combat_ability_behavior_index.py`：把 combat ability 初始化表和 IL2CPP 类型模型交叉，导出每个战斗能力的行为方法入口索引。
- `tools/extract_combat_ability_behavior_calls.py`：从 combat ability 的 `DoMe`、`IsValid`、`FailText`、`AutoPerform` 和特殊方法中导出保守虚调用 trace。
- `tools/extract_dungeon_action_behavior_index.py`：把 `DungeonAction*` 类型模型交叉成地城动作入口索引，并从 `Initialize` 解出 cooldown 初始值。
- `tools/extract_dungeon_action_behavior_calls.py`：从地城动作的执行、验证、特殊、显示和选定配置方法中导出保守虚调用 trace 与按钮/确认文案 literal 引用。
- `tools/extract_dungeon_generation.py`：读取 Mach-O 浮点常量并汇总 `DungeonBoard` 金币、奖励 tier、奖励名概率、怪物等级分布和陷阱状态。
- `tools/analyze_cardfinder_rewards.py`：把结构化 `CardFinder.ChooseReward` 过滤链应用到具体 fixture；当前输出 Warrior floor 1 TreasureChest 的 base-unlocked 候选池、权重、概率表和 roll band，并把 requirement-gated 候选同 achievement 解锁证据分开列出。
- `tools/run_cardfinder_reward_smoke.py`：读取 `cardfinder_reward_analysis.json`，按原 `CardList.GetCardList()` 顺序扣减权重，验证 `RandomRange(1,totalWeight)` 的边界 roll 会落到预期候选；当前 96/96 通过，但仍不是完整 RNG/list replay。
- `tools/run_cardfinder_unlock_state_smoke.py`：给 CardFinder 分析传入显式 `UserAttribute` 解锁 fixture，验证 `CardData.HasAccess` 的 requirement gate 会把 `Execute` 经 `DAMAGE2` 加进 current-user 候选池，同时 `never` requirement 仍不可解锁。
- `tools/run_cardfinder_spawn_sequence_smoke.py`：把 `CardFinder` 候选池和 `Dungeon.SpawnCard` base-name 计数 mutation 串起来，当前用 `StormSlash1 -> StormSlash2` 的确定性目标 roll 序列验证共享 `StormSlash` 计数到达 `max_spawns` 后会移除后续同 base-name 候选。
- `tools/analyze_dungeon_spawn_tracking.py`：把 `Dungeon.SpawnCard` / `Dungeon.SpawnCount` 的两个计数字典、三个 flag byte、`Card.BaseName` key 归一化和 `BuildBoard` 阶段切换结构化到 `dungeon_spawn_tracking.json`。
- `tools/run_dungeon_spawn_tracking_smoke.py`：验证 `Card.BaseName` 共享等级变体计数，以及 `0x120/0x121/0x122` flag 对读写计数字典的影响；当前是 spawn tracking fixture，不是完整 CardFinder RNG replay。
- `tools/run_treasure_chest_loot_smoke.py`：读取 `dungeon_generation.json` 的 TreasureChest 规则并跑确定性宝箱 fixture，验证 1/2/3 件阈值、第一件必为卡、后续 0.45 金币分支、卡牌分支、SpawnCard 抑制和最终 loot shuffle helper 交换语义。
- `tools/run_mimic_chest_open_smoke.py`：读取 `dungeon_generation.json` 的 MimicChest 规则并跑确定性打开宝箱 fixture；当前 30/30 通过，验证 `SetLevel`、`CreateNamedMonsterAtLevel(tile, level, "MimicMonster")`、`Destroy`、`DisplayInMini(false)`、写 `Dungeon.displayedInMini` 和 `PlayerSprite.RefreshNoWipe` 的高层链路，但不 replay `DisplayInMini` / `RefreshNoWipe` 内部 Unity visual side effects。
- `tools/run_dungeon_shop_generation_smoke.py`：读取 `dungeon_generation.json` 和 floor seed，验证 `GenerateShop` 的负采样 early return、非负采样创建 `Shop` feature、以及 recovered Shop 虚槽 gold/budget setter；当前 28/28 通过，但不 replay 原版 `Game.RandomNormal` stream、Shop inventory/UI、prefab 或 Unity object graph。
- `tools/run_shop_inventory_ui_smoke.py`：读取 Shop metadata、字段访问表和 Shop 反汇编，验证 `text/items/boughtItems/maxItems/internalName` 字段、`GenerateItems` 物品列表、`BuildLootObjects` 计数、`MasterThief` 购买分支和 ShopDialogueObject 构造形态；当前 54/54 通过，但不 replay 原版 CardFinder/RNG stream、真实 ShopDialogueObject 渲染、Unity transforms 或 live UI event dispatch。
- `tools/analyze_shop_item_generation_recovery.py`：读取 Shop/CardFinder/Utility method pointers 和 Shop/CardFinder/Shuffle 反汇编，解析 `Shop.GenerateItems` 的 1/3/5 固定 tier、`CardFinder.ChooseReward(b, tiers, d, minAffinity)` 调用、重复检测后重选分支、`Utility.Shuffle` 终端洗牌和 `Game.RandomRange` helper；当前 40/40 通过，但不 replay 原版 RNG stream、完整 CardFinder 连续状态、泛型重复检测 helper 名称或 live Shop UI。
- `tools/analyze_utility_shuffle_recovery.py`：读取 8 个 `Utility.Shuffle` 泛型实例反汇编和 14 个直接调用 xref，输出 `utility_shuffle_recovery.json` / `.tsv`；当前 39/39 通过，确认 6 个直接 Fisher-Yates index-loop 变体、2 个 wrapper-to-recovered-shuffle 变体和 8 个地牢相关调用点，但不 replay 每次调用的原版 index draw 或共享 RNG stream。
- `tools/run_dungeon_path_between_smoke.py`：读取 `dungeon_generation.json` 的 PathBetween 规则并跑确定性路径 fixture，验证 wrapper tryNumber 1 -> 2 retry、`AvoidIfPossible` / `WillRevealDark` try-number predicate、destination `MoveNextTo` 例外、visited predecessor guard，并记录 11 条 PathBetween neighbor shuffle boundary；当前 21/21 通过，但不 replay `Utility.Shuffle` index stream 或原版 RNG。
- `tools/run_dungeon_reward_postprocessing_smoke.py`：读取 `dungeon_generation.json`、`current_user_loading.json` 和 `dungeon_user_save_codec_smoke.json`，验证 `GenerateRewards` 的 Kitten/Grizzly/Velociraptor 难度后处理、`DRAGON1`/`STEPS1` 当前用户属性奖励增量，以及 synthetic PlayerPrefs/current-user save loading 到后处理的路径；当前 21/21 通过，但仍不 replay 完整 reward RNG stream、feature placement、真实 `player.hpl` 或原版 PlayerPrefs。
- `tools/run_dungeon_seed_current_user_postprocessing_smoke.py`：把 synthetic PlayerPrefs `currentUser` + `profiles/<user>/player.hpl` 的 `DRAGON1`/`STEPS1` 属性加载进 floor seed harness，验证 seed 奖励序列和放置计数会消费 current-user reward 后处理；当前 23/23 通过，但仍是合成 save fixture，不 replay 原版 RNG、真实 `player.hpl` 或 Unity physical placement。
- `tools/analyze_dungeon_rng_replay_gaps.py`：汇总 `remaster_dungeon_seed.json`、TreasureChest/CardFinder/SpawnCard smoke 中的 RNG/list-order 消费点，输出 `dungeon_rng_replay_gaps.json` / `.tsv`；当前 40/40 通过，记录 20 类消费者、129 个已记录标量 fixture、0 个可推断但未记录标量消费者、17 次 shuffle 调用、17 条 shuffle order boundary 和 349 个 fixture-derived shuffle index draw，全部仍为 `original_replay_complete=false`。
- `tools/analyze_dungeon_shuffle_helper_recovery.py`：汇总 `dungeon_generation.json`、`remaster_dungeon_seed.json`、PathBetween smoke、TreasureChest loot smoke 和 `utility_shuffle_recovery.json` 中的 shuffle helper 证据，输出 `dungeon_shuffle_helper_recovery.json` / `.tsv`；当前 36/36 通过，目录化 6 条 shuffle helper 路径、5 条 `Utility.Shuffle`/delegated 路径的 index-loop 形态、1 条 TreasureChest `0x1001cb21c` 已恢复 helper，并确认 seed 17 条 order boundary 都有 fixture-derived index draw、PathBetween 11 条 smoke boundary 仍没有原版 index stream replay。
- `tools/analyze_dungeon_physical_construction_gaps.py`：汇总 `remaster_dungeon_seed.json` 和 `remaster_dungeon_mimic_seed.json` 的 feature registry、Tile child/ChildChange 记录、reward `CreateFeature` dispatch、TreasureChest/Shop/Mimic 特殊副作用和 physical construction boundary catalog，输出 `dungeon_physical_construction_gaps.json` / `.tsv`；当前 48/48 通过，记录 20 个 floor-1 feature、19 个原版 DungeonFeature-like child、0 个 collision、6 类物理构造缺口、6 条边界目录（floor-1 seed 5 条 covered、any-seed fixture 6/6 covered，其中 depth-2 seed 含 1 个 MimicChest tile）；全部仍为 Unity object graph fixture，Unity visual/UI internals 仍未 replay。
- `tools/extract_dungeon_talents.py`：从 `DungeonTalentList.GetDungeonTalentList` 的构造调用恢复 dungeon talent 列表、tier、repeatable 标记和 requirement 数组。
- `tools/extract_talentbase_behavior_calls.py`：从 `TalentBase` 的构造、card/prereq accessor、复制、排除判定、`CanLearn`、`PayFor` 和 `Learn` 中导出学习流程的保守虚调用 trace。
- `tools/extract_creature_talent_behavior_calls.py`：从 `Creature` / `CardBack` 的天赋列表、点数、tier 计数和卡背 UI 绑定方法中导出保守虚调用与字段访问 trace。
- `tools/extract_dungeon_player_talent_behavior_calls.py`：从 `DungeonPlayer` / `DungeonUser` 的 dungeon talent 查看、选择、移除、资格判定和显示序列化方法中导出保守虚调用、直接调用与字段访问 trace。
- `tools/extract_talentbase_asset_usage.py`：检查 Unity 资源中 `TalentBase` 是否存在序列化 MonoBehaviour 实例，并列出 `Talent` prefab/game object 上实际挂载的脚本组件。
- `tools/analyze_talent_learning_ui_recovery.py`：交叉 LevelUpReward / DungeonPlayer / SDB / ShopDialogueTalentViewer / CardBack / CardPhysical / TalentBase 反汇编、IL2CPP model、TalentBase reachability 和 DungeonTalent handler 覆盖，输出 `talent_learning_ui_recovery.json` / `.tsv`；当前 53/53 通过，恢复静态 talent-viewer、卡背绑定和 pay-and-learn 形态，但 live UI replay 与 prototype readiness 仍为 false。
- `tools/analyze_talentbase_reachability.py`：交叉 `TalentBase` / `Creature` / `DungeonPlayer` trace、`DungeonTalent` handler 覆盖、Talent learning UI recovery、Unity 资源使用和 ctor/gain xref 扫描，输出 `talentbase_reachability.json` / `.tsv`；当前 23/23 通过，把 DungeonTalent 可确认路径、TalentBase 静态 UI/学习链和 TalentBase 未验证动态/遗留/存档路径分开记录。
- `tools/build_remaster_starter_slice.py`：从 `cards.json`、`monster_catalog.json`、`profession_skills.json` 和 `dungeon_generation.json` 中挑选第一版 prototype fixture：10 张卡、3 个怪物、1 个职业和 floor 1 常量。
- `tools/run_remaster_starter_smoke.py`：读取 `remaster_starter_slice.json`，验证 9 张简单卡、`Sword` 双攻击触发假设、starter deck 依赖和 floor 1 常量。
- `tools/run_remaster_starter_combat.py`：读取 `remaster_starter_slice.json`，用确定性简化规则跑 3 个 starter 怪物的最小战斗回合夹具；starter 卡牌效果和 `Sword.CheckCounters` 触发通过 `tools/remaster_runtime.py` 执行，牌区现在区分 deck/hand/play_area/discard/exile/equipment，支持 discard reshuffle，并输出 normal-path `Card.ResourcesOff` / `CanAfford` cost modifier catalog / `Card.PayFor` branch catalog / `Card.WillBeCountered -> Enemy.WillCounter` priority boundary / `Card.PlayMe` order / `Game.PlayingCard(c)` boundary / delayed reaction/decay/doubleSpell boundary / inactive reaction+alternate-branch catalog / `Player.PlaySideEffects` branch boundary/catalog / zone lifecycle+GetAction catalog / discard-to-Die boundary / Die graveyard boundary / DestroyDie/Bounce/Flick/Destroy/WipeClean static lifecycle catalog / deterministic lifecycle fixture / Sword equipment lifecycle boundary / Sword.CheckCounters equipment trigger boundary / Sword trigger gate boundary、`ActionPlay` priority / highest-priority selection、`GetAllActions` / `GetAllActionsPlusTargets` candidate/action+target、`AIIsValidPlusTargets` / `AIGetTargets` target-legality、target-set construction、`AIPhysical.GetStackInput` / `ActionBase.Perform` order / action-counter snapshot boundary / `ActionBase.Resolve` current-action lifecycle boundary / `ActionPlay.PayCosts/PerformAction` bridge / `ActionPass.Perform` payload dispatch / `GamePhysical.AddToVisualStack*` shape、`AIPlayerRandom.AmIDone` / random candidate mutation / `AIPerformAction` / `AIPass` packaging fixture checks；RNG、active counter reactions、full target legality、target-set replay、完整 action execution、血量估算、状态结算、reaction/SBE、UI、visual stack、pass side effects、完整 zone/Die/Exile/DestroyDie/Bounce/Flick/Destroy/WipeClean/equipment lifecycle/object identity 和完整回合策略仍是 fixture 假设，不是原版 AI 或完整规则。
- `tools/run_monster_runtime_smoke.py`：读取 `monster_catalog.json`，用 `tools/remaster_runtime.py` 执行全部 73 个怪物的 recovered behavior effect 和 247 个升级等级组合；当前 38/38 通过，覆盖 137 个 behavior event、1001 个 level-up event 和 Pixie/Skeleton/Wyvern/Hydra/Sphinx/Genie 等代表性断言，但仍不是完整 monster AI、targeting、turn timing、RNG 或 Unity UI replay。
- `tools/run_monster_runtime_matrix_smoke.py`：读取 `monster_catalog.json`，按 source method 分开执行全部 87 个 recovered behavior case，覆盖 137 个 effect event、10 个 recorded state-write event 和 3 个 `Sphinx.ForbidPlay` predicate fixture；当前 18/18 通过，但仍不是完整 monster AI、turn timing、targeting、RNG 或 Unity UI replay。
- `tools/run_monster_deck_behavior_bridge.py`：读取 `monster_catalog.json`、`cards.json` 和 `card_actionplay_bridge_matrix_smoke.json`，把 25 个 deck-encoded 怪物 loadout 与 105 张引用行为卡连接到 recovered card runtime / ActionPlay bridge evidence；当前 19/19 通过，缺失卡、缺失 runtime rule、缺失 ActionPlay bridge 和 loadout mismatch 均为 0，但仍不是原版 monster AI turn/order/target/RNG replay。
- `tools/analyze_monster_context_dependency_index.py`：读取 `monster_catalog.json`、monster runtime accuracy gaps、monster source-method matrix、deck behavior bridge 和 control-flow replay，把 20 个怪物的 54 个 read/context dependency step 按 remaster 子系统 owner 分类，输出 `monster_context_dependency_index.json` / `.tsv`；当前 18/18 通过，8 个 owner 队列、0 个 uncategorized step，但 original value-flow 与 turn/target replay 仍为 false。
- `tools/analyze_monster_effect_value_flow_index.py`：读取 `monster_catalog.json`、monster behavior calls、monster runtime matrix smoke 和 monster runtime accuracy gaps，把 43 个 `effect_without_immediate_args` step 拆成 zero-parameter receiver effect、source-method player parameter、prior runtime read、card/deck payload、visual-stack payload、context-derived scalar 和 active-player identity 队列，输出 `monster_effect_value_flow_index.json` / `.tsv`；当前 19/19 通过，覆盖 26 个怪物、23 个 operation、16 个零参数 receiver effect、27 个仍需参数值恢复的 step，unclassified=0，但 original parameter-value 与 receiver-identity replay 仍为 false。
- `tools/run_monster_control_flow_replay.py`：读取 `monster_catalog.json`，用 `tools/remaster_runtime.py` 针对 Demon/Mage/Sphinx 三个手工恢复行为分支、Hydra/Sphinx/Treant 三个手工恢复升级生命值分支和 10 个 raw Monster state write 跑 focused replay；当前 34/34 通过，覆盖 17 个分支 fixture 和 10 个 state-write event，但原版 combat timing、typed state semantics 和 lifecycle replay 仍为 false。
- `tools/analyze_monster_rng_recovery.py`：交叉 `monster_catalog.json` 和 GoblinMechanist/Revenant/Deck/Game 反汇编，输出 `monster_rng_recovery.json` / `.tsv`；当前 14/14 通过，恢复 `Game.InGameRandomRange(a,b)` inclusive upper bound、`GoblinMechanist.StartTurn` 的随机装备/爆炸分支、9 个装备候选、`Revenant.RevenantEffect` 的 add-Scream-then-shuffle 和 `Deck.Shuffle` remaining-deck Fisher-Yates 形态，但原版 RNG stream 与 shuffle index stream replay 仍为 false。
- `tools/analyze_monster_original_accuracy_flag_index.py`：递归读取 29 个 monster/runtime/AI/action pipeline artifact，把剩余 false original-accuracy/readiness flag 规范化为 `monster_original_accuracy_flag_index.json` / `.tsv`；当前 13/13 通过，记录 283 个唯一 false flag entry、735 次 occurrence、9 个工作队列（monster rule completion、deck behavior/card-play bridge、value-flow/parameter/receiver identity、RNG/index stream、AI candidate/target/turn policy、behavior/level-up branch timing、typed state/restriction/visual UI、action/reaction/visual turn flow、global readiness）且 uncategorized=0。
- `tools/analyze_monster_runtime_accuracy_gaps.py`：交叉 `monster_catalog.json`、怪物 runtime smoke、monster runtime matrix smoke、monster deck behavior bridge、monster context dependency index、monster effect value-flow index、monster original-accuracy flag index、monster control-flow replay、monster RNG recovery、starter monster smoke、Sphinx restriction smoke、runtime engine coverage，以及 AI candidate / target legality / turn policy / action execution replay，输出 `monster_runtime_accuracy_gaps.json` / `.tsv`；当前 115/115 通过，确认 73/73 个怪物仍为 `executable_rule_complete=false`，并把 false flag index、deck-encoded behavior、level-up replay、manual behavior control flow、turn timing、target/player context、read/context owner queues、computed/receiver value flow、state writes、RNG/shuffle、UI/visual side effects、restriction predicates 和 AI policy 原版 replay 拆成准确性队列。
- `tools/run_starter_monster_runtime_smoke.py`：读取 `remaster_starter_slice.json`，用 `tools/remaster_runtime.py` 执行 Pixie/Skeleton/Wyvern 的 level-up deck mutation 和 ApplyToPlayer hook；当前 16/16 通过，但仍不是完整 monster AI、回合时机或限制谓词验证。
- `tools/run_sphinx_restriction_runtime_smoke.py`：读取 `monster_catalog.json`，用 `tools/remaster_runtime.py` 执行手工恢复的 `Sphinx.ForbidPlay` 禁牌类型谓词和 `StartTurn` 禁用类型选择 fixture；当前 13/13 通过，但仍不 replay Unity UI message、随机选择或完整 AI 回合。
- `tools/run_remaster_dungeon_seed.py`：读取 `dungeon_generation.json`，用确定性简化规则跑 floor 1 seed snapshot，并可用 `--depth 2 --seed 5` 生成含 1 个 MimicChest reward tile 的 `remaster_dungeon_mimic_seed.json` fixture；包括连通迷宫、AddMonsters 阶段式怪物放置、`Tile.CanBeACut` 几何预过滤、`Tile.IsUnused` 的 `DungeonFeature` 子对象过滤、`Tile.MonsterPercent`、`TileDistanceSort`、`RandomUnusedTile(list)` 风格的 reward 选点、sorted-ThreatComponent boss 选点、`GenerateRewardByName` 分派、GenerateRewards 难度/当前用户属性后处理、`TreasureChest`/`MimicChest` 特殊副作用注释、结构化 `CardFinder` 宝箱候选过滤/权重注释、`MimicChest.Open` 生成/显示链路注释、`ThreatComponent.Pick` 权重衰减、`GetToPlayerThreats` 的 lowest-current-threat 网格 fixture、`GetThreatComponents` 的 zero-threat flood-fill component fixture、`DungeonFeature.AtTile -> VirtualObject.SetParent` 证据、`AddRewards` 的 `goldTarget` 排程、`GenerateShop` 金币采样、怪物等级、奖励、金币和 boss 选择；输出也包含 monster-level bucket-walk indices、jitter、GenerateRewards health branch/count、random reward count、reward-name roll 标量记录、17 条 shuffle order boundary，以及 VirtualObject-like feature registry 和 6 条 physical construction boundary catalog，把原版 DungeonFeature-like 子对象、seed-covered physical boundary 和 harness-only start marker 区分记录。byte-accurate Unity physical 构造、RNG/list 顺序、CardFinder 精确列表/RNG replay、真实 `player.hpl`、下游 Unity visual side effects 和完整 reward RNG stream 仍未完整翻译。
- `tools/run_dungeon_seed_sweep.py`：对 `run_remaster_dungeon_seed.py` 跑 180 个 compact fixture case（10 个 seed、depth 1/2/3、Grizzly Bear/Kitten/Velociraptor、无属性/DRAGON1+STEPS1），输出 `dungeon_seed_sweep.json` / `.tsv`；当前 16/16 通过，确认 180/180 连通、奖励全放置、feature collision 为 0、depth 1 无 MimicChest、depth > 1 有 6 个 MimicChest case、physical boundary sweep union 6/6，同时记录 126 个 monster level-sum 非 exact case；仍不 replay 原版 RNG stream 或 Unity object graph。
- `tools/analyze_dungeon_generate_monster_levels_recovery.py`：交叉 `DungeonBoard.GenerateMonsterLevels` 反汇编、`dungeon_generation.json` 和 seed harness 输出，输出 `dungeon_generate_monster_levels_recovery.json` / `.tsv`；当前 20/20 通过，确认权重 `RandomFloat()+0.5`、截断计数、jitter、前三次偏置、上下限、目标表和 base-level bucket-walk 形态，并验证 harness 按 `raw_counts` 重放桶索引；原版 RNG stream 和 exact level-list replay 仍未完成。
- `tools/analyze_dungeon_monster_level_target_boundary.py`：交叉 `dungeon_generation.json`、`dungeon_seed_sweep.json` 和 seed harness 的 `GenerateMonsterLevels` floor loop，输出 `dungeon_monster_level_target_boundary.json` / `.tsv`；当前 18/18 通过，确认 180/180 monster level sum 至少达到 floor target，54 个 exact、126 个 over-target、0 个 under-target、最大 over-target 为 24，因此 seed sweep 的 126 个非 exact case 是 floor-boundary overshoot，不是低于目标的失败；exact 原版 level-list/RNG stream 仍未 replay。
- `tools/run_dungeon_add_rewards_bridge.py`：读取 `dungeon_generation.json` 并复跑 180 个 compact AddRewards fixture case，验证 ThreatComponent placement/picks、AddRewards `goldTarget` 排程、reward feature dispatch、Shop gold samples、VirtualObject-like feature registry 和 physical boundary coverage，输出 `dungeon_add_rewards_bridge.json` / `.tsv`；当前 28/28 通过，覆盖 2,658 个 placed reward、2,582 个 non-fallback ThreatComponent pick、648 个 Shop gold sample、0 个 gold schedule mismatch、0 个 feature registry count mismatch 和 6/6 physical boundary id，但仍不 replay 原版 RNG/shuffle stream 或 Unity physical object graph。
- `tools/analyze_dungeon_original_accuracy_flag_index.py`：递归读取 30 个地牢 seed/sweep/RNG/shuffle/shop/mimic/save/CardFinder/gap/recovery artifact，把剩余 false original-accuracy/readiness flag 规范化为 `dungeon_original_accuracy_flag_index.json` / `.tsv`；当前 12/12 通过，记录 262 个唯一 false flag entry、2,020 次 occurrence、6 个工作队列（RNG/index stream、Unity physical/visual UI、reward/ThreatComponent、CardFinder/unlock、real save/PlayerPrefs、global readiness）且 uncategorized=0。
- `tools/analyze_dungeon_generation_accuracy_gaps.py`：交叉 `dungeon_generation.json`、floor-1 seed harness、depth-2 MimicChest seed fixture、180-case seed sweep、GenerateMonsterLevels bucket-walk recovery、monster level target boundary、AddRewards bridge、CardFinder/SpawnCard/TreasureChest/Shop/MimicChest/current-user/save/reward-postprocessing smoke、Shop item generation recovery、seed-level current-user reward smoke、真实 save 样本 inventory、RNG replay gap、Utility.Shuffle recovery、PathBetween smoke、shuffle helper recovery、Unity physical construction gap 和 dungeon original-accuracy flag index，输出 `dungeon_generation_accuracy_gaps.json` / `.tsv`；当前 171/171 通过，汇总 396 个相关 smoke check、16 个 seed sweep check、20 个 GenerateMonsterLevels recovery check、18 个 monster level boundary check、28 个 AddRewards bridge check、40 个 RNG replay gap check、36 个 shuffle helper recovery check、39 个 Utility.Shuffle recovery check、21 个 PathBetween smoke check、40 个 Shop item generation recovery check 和 12 个 false-flag index check 且 failed=0，并透出 seed 349 个 fixture-derived shuffle index draw、PathBetween 11 条 neighbor shuffle boundary、physical boundary catalog 6 条、floor-1 seed 5 条 covered、any-seed fixture 6/6 covered；但仍确认 Unity physical construction、RNG/list order、原版 shuffle/index stream、exact 原版 monster level-list replay、ThreatComponent replay、gold/shuffle sourcing、full CardFinder integrated replay、SpawnCard tracking、真实 current-user save/PlayerPrefs、special reward visual side effects 和完整 reward RNG stream 不是原版准确。
- `tools/build_runtime_rule_quality.py`：交叉检查 `cards.json`、`monster_catalog.json` 和 `profession_skills.json` 的 normalized runtime rules，输出 `runtime_rule_quality.json` / `runtime_rule_quality.tsv`，确认未解析操作数、剩余 context/no-rule 条目和误标 executable 的条目。
- `tools/build_runtime_rule_backlog.py`：把 `runtime_rule_quality.json` 中的 context/no-rule 缺口（若存在）展开成带优先级的 `runtime_rule_backlog.json` / `runtime_rule_backlog.tsv`；当前队列为 0，说明 normalized recovery 缺口已清空，但不代表 runtime engine 完成。
- `tools/remaster_runtime.py`：一个保守的小型 normalized rule interpreter，当前对已抽取的 cards / monster behavior / monster level-up / profession / combat ability / dungeon action effect 标签和 DungeonTalent handler effects 都有显式 handler；很多 UI、选择器、对象挂接和视觉/RNG hook 仍是结构化记录，不是完整原版 combat engine。
- `tools/build_runtime_engine_coverage.py`：统计 `tools/remaster_runtime.py` 对 cards / monster behavior / monster level-up / profession / combat ability / dungeon action normalized effect steps 和 DungeonTalent handler operations 的覆盖，输出 `runtime_engine_coverage.json` / `runtime_engine_coverage.tsv`。当前 1442/1442 个 normalized effect/handler steps 有显式 interpreter handler，标签覆盖率 100%；cards 633/633、monster behavior 137/137、monster level-up 346/346、profession 57/57、combat ability 61/61、dungeon action 148/148、dungeon talent handler 60/60 均已覆盖。
- `tools/run_priority_runtime_smoke.py`：针对 priority 1、former backlog、deck-encoded monster、monster level-up、怪物属性/状态/记录型 hook、卡牌状态/控制/资源/选择 hook、combat ability、profession 和 dungeon action 跑窄范围推断/反汇编夹具，当前通过 `tools/remaster_runtime.py` 覆盖 420 个 check，包括全部 25 个 `behavior_encoded_in_deck` 怪物、全部 73 个 monster level-up rules、怪物属性/状态/记录型行为、卡牌高频与剩余记录型 effect、combat ability、profession hook、32 个 dungeon action recovered-effect fixture，以及 `BootsOfSpeed`、`Scimitars`、`PrayerOfViolence`、`GaussHourglass`、`Curse`、`PenaltyCurses`、`PenaltyCursesFinal`、`Demon`、`GiantSquid`、`Hydra`、`ProfessionSamurai`、`CombatAbilityStudy`、`CombatAbilityTutor` 和 `DungeonActionUberTeleport`。
- `tools/build_remaster_manifest.py`：把卡牌、怪物、地牢生成、职业/技能和 remaster 路线映射到 `remaster_manifest.json` / `remaster_manifest.tsv`，用于追踪分析覆盖与运行时缺口。
- `tools/build_completion_audit.py`：把用户目标拆成完成度检查项，逐项检查真实 artifact、计数、阻塞缺口和下一步，输出 `remaster_completion_audit.json` / `remaster_completion_audit.tsv`。
- `docs/remaster-roadmap.md`：把解析 artifact、runtime quality gate、runtime backlog、实现阶段和验收门槛整理成可执行 remaster 路线图。

## IL2CPP 类型模型

`global-metadata.dat` 的 metadata version 是 16。已确认本包关键表布局如下：

- type definition：header table `19`，每条 108 bytes，共 3518 个类型。
- method definition：header table `5`，每条 56 bytes，共 26064 个方法。
- field definition：header table `11`，每条 12 bytes，共 11842 个字段。
- parameter definition：header table `10`，每条 16 bytes，共 22975 个参数。
- property definition：header table `4`，每条 20 bytes，共 2930 个属性。
- event definition：header table `3`，每条 24 bytes，共 86 个事件。

`extract_il2cpp_metadata_model.py` 用这些布局恢复出了 1104 个游戏相关类型，其中 912 个和 Unity MonoScript 类名直接匹配。最重要的压缩索引是 `extracted/structured/key_systems.json`，包含：

- 16 个关键数据/逻辑类型摘要。
- 18 个 `Profession*` 类型摘要。
- 38 个 `CombatAbility*` 类型摘要，包含基类。
- 34 个 `DungeonAction*` 类型摘要，包含基类。
- 37 个 `DungeonBoard` 地牢生成相关方法入口。

## Native 方法入口

`find_il2cpp_method_pointers.py` 已经在 Mach-O 中定位到主 IL2CPP method pointer 表：

- 位置：`__DATA,__const`
- VM address：`0x1010f6158`
- file offset：`0x10f6158`
- 连续 `__TEXT,__text` 方法指针：24879 个

这使 metadata 中的 `method_index` 可以映射到 native 方法地址。例如：

- `CardList..cctor`：`method_index = 15998`，native address `0x1000ae1dc`
- `CardData..ctor`：`method_index = 15949`，native address `0x10008f564`
- `MonsterList.GetMonsterList`：`method_index = 22020`，native address `0x100113c40`
- `DungeonBoard.AddMonsters`：`method_index = 19507`，native address `0x100279248`

已导出的目标反汇编：

- `extracted/disassembly/CardData__ctor.txt`
- `extracted/disassembly/CardData_CanFit.txt`
- `extracted/disassembly/CardData_HasAccess.txt`
- `extracted/disassembly/CardData_HasNever.txt`
- `extracted/disassembly/CardData_GetTier.txt`
- `extracted/disassembly/CardData_FrequencyMod.txt`
- `extracted/disassembly/CardList__cctor.txt`
- `extracted/disassembly/CardList_GetCardList.txt`
- `extracted/disassembly/AchievementList_GetAchievementList.txt`
- `extracted/disassembly/MonsterList_GetMonsterList.txt`
- `extracted/disassembly/DungeonBoard_MonsterLevels.txt`
- `extracted/disassembly/DungeonBoard_GenerateMonsterLevels.txt`
- `extracted/disassembly/DungeonBoard_CreateMonster.txt`
- `extracted/disassembly/DungeonBoard_CreateNamedMonsterAtLevel.txt`
- `extracted/disassembly/DungeonBoard_CreateBoss.txt`
- `extracted/disassembly/DungeonBoard_AddMonsters.txt`
- `extracted/disassembly/DungeonBoard_AddRewards.txt`
- `extracted/disassembly/DungeonBoard_BuildMaze.txt`
- `extracted/disassembly/DungeonBoard_CreateFeature.txt`
- `extracted/disassembly/DungeonBoard_Initialize.txt`
- `extracted/disassembly/DungeonBoard_GetDimensions.txt`
- `extracted/disassembly/DungeonBoard_TileAt.txt`
- `extracted/disassembly/DungeonBoard_InDungeon.txt`
- `extracted/disassembly/DungeonBoard_AllTiles.txt`
- `extracted/disassembly/DungeonBoard_GetAllTilesList.txt`
- `extracted/disassembly/DungeonBoard_ScaleFactor.txt`
- `extracted/disassembly/DungeonBoard_BaseTileSize.txt`
- `extracted/disassembly/Dungeon_Initialize.txt`
- `extracted/disassembly/Dungeon_BuildBoard.txt`
- `extracted/disassembly/Dungeon_BuildFakeBoard.txt`
- `extracted/disassembly/Dungeon_RegenerateBossAttributes.txt`
- `extracted/disassembly/Dungeon_BuildBossAttributes.txt`
- `extracted/structured/dungeon_floorboss_field_accesses.json`
- `extracted/structured/dungeon_floorboss_field_accesses.txt`
- `extracted/disassembly/DungeonBoard_GoldTarget.txt`
- `extracted/disassembly/DungeonBoard_GoldByTier.txt`
- `extracted/disassembly/DungeonBoard_RewardTier.txt`
- `extracted/disassembly/DungeonBoard_GenerateReward.txt`
- `extracted/disassembly/DungeonBoard_GenerateRewardName.txt`
- `extracted/disassembly/DungeonBoard_GenerateRewardByName.txt`
- `extracted/disassembly/DungeonBoard_GenerateRewards.txt`
- `extracted/disassembly/DungeonBoard_GenerateShop.txt`
- `extracted/disassembly/DungeonBoard_GenerateChest.txt`
- `extracted/disassembly/DungeonBoard_GenerateMonsterChest.txt`
- `extracted/disassembly/DungeonBoard_GenerateRandomReward_tier.txt`
- `extracted/disassembly/DungeonBoard_GenerateChests.txt`
- `extracted/disassembly/DungeonBoard_GenerateRandomReward.txt`
- `extracted/disassembly/MonsterData_Frequency.txt`
- `extracted/disassembly/MonsterData_CanFit.txt`
- `extracted/disassembly/MonsterFinder_ChooseBoss_level.txt`
- `extracted/disassembly/MonsterFinder_ChooseBoss_exclude.txt`
- `extracted/disassembly/MonsterFinder_ChooseMonster_boss.txt`
- `extracted/disassembly/MonsterFinder_ChooseMonster_seen.txt`
- `extracted/disassembly/MonsterFinder_ChooseMonster_full.txt`
- `extracted/disassembly/DungeonTalentList_GetDungeonTalentList.txt`
- `extracted/disassembly/Profession*_ClassBiases.txt`

构造函数直接调用交叉引用：

- `extracted/structured/data_ctor_call_xrefs.json`
- `extracted/structured/data_ctor_call_xrefs.txt`

全方法扫描结果：`CardData..ctor` (`0x10008f564`) 和 `MonsterData..ctor` (`0x100112a60`) 都没有直接 `bl` 调用。也就是说，列表数据不是以普通 C# 构造调用形式保留在 native 代码里，而是由 IL2CPP 生成的对象分配 + 字段写入序列初始化。

## 内容解析结果

### 卡牌

证据文件：

- 资源路径：`extracted/resource_paths.json` 的 `groups.card_art`
- 资源/脚本匹配：`extracted/content_inventory.json` 的 `cards`
- 卡牌文本语料：`extracted/corpora/card_text_corpus.tsv`

已确认：

- 卡牌图路径共 375 个，来自 `textures/cards/...`。
- MonoScript 中存在大量同名卡牌类，例如 `Attack1`、`Fireball`、`FlameSlash1`、`SiphonLife3`、`DeckOfWonder`、`PhoenixFeather`。
- `CardData`、`ALL_CARDS`、`CardType`、`CardCost`、`ManaCost`、`HealthCost`、`DamageTypes` 等标识符在 IL2CPP metadata 中存在。
- `Achievement`、`AchievementList`、`UserAttribute`、`AchievementFlavor` 也已定位；卡牌解锁要求使用 `UserAttribute` 数值。
- string literal table 中保留了卡牌显示名和效果文本，主要集中在 literal index `4339-5315`，已经导出为 TSV。
- `CardData` 数据模型已恢复出 15 个字段：
  - `internalName`
  - `biases`
  - `gold`
  - `requirements`
  - `hasDynamicGold`
  - `dynamicGoldFunction`
  - `thief`
  - `priest`
  - `warrior`
  - `wizard`
  - `tier`
  - `maxSpawns`
  - `cardType`
  - `elementalAffinity`
  - `realName`
- `CardData` 构造函数参数与字段基本一一对应：`internalName`、`biases`、`gold`、`requirements`、`hasDynamicGold`、`dynamicGoldFunction`、四个职业权重、`tier`、`maxSpawns`、`cardType`、`elementalAffinity`、`longName`。
- `CardData` 行为方法已恢复出 `CanFit(minG,maxG,d)`、`HasAccess()`、`HasNever()`、`BiasMatch(b)`、`GetTier(d)`、`FrequencyMod(b)`。
- `CardList` 有 `cardList`、`allCards` 两个字段，并通过 `.cctor()`、`GetCardList()` 建立全卡表。
- `CardList..cctor` 已完成 native 反汇编并标注 string literal。当前从该方法导出 370 个卡牌/卡牌相关 internal id 到：
  - `extracted/structured/card_internal_ids_from_cctor.json`
  - `extracted/structured/card_internal_ids_from_cctor.tsv`
- `CardData..ctor` 反汇编确认了字段写入顺序：对象偏移 `0x10` 起依次写入 `internalName`、`biases`、`gold`、`requirements`、`hasDynamicGold`、`dynamicGoldFunction`、职业权重、`tier`、`maxSpawns`、`cardType`、`elementalAffinity`、`realName/longName`。
- 对 `CardData..ctor` 和 `MonsterData..ctor` 的全方法直接调用扫描结果为 0。`CardList..cctor` 可作为全卡 internal id 清单；完整的 `CardData` 数值表需要从 `CardList.GetCardList` 的 inline 对象分配 + 字段写入序列恢复。
- `CardList.GetCardList` 已完成 native 反汇编并由 `extract_card_data.py` 恢复 234 行可进奖励/抽卡池的 `CardData`：
  - `extracted/structured/card_data.json`
  - `extracted/structured/card_data.tsv`
- `AchievementList.GetAchievementList` 已完成 native 反汇编并由 `extract_achievements.py` 恢复 109 行 achievement/unlock 数据：
  - `extracted/structured/achievements.json`
  - `extracted/structured/achievements.tsv`
- `achievements` 覆盖 `attribute`/`attribute_name`、internal/display name、达成条件文本、奖励文本、点数成本和 flavor。`Achievement` 字段偏移为 `attribute=0x10`、`internalName/name=0x18`、`requirements/reward=0x28`、`cost/flavor=0x38`。
- `card_data` 当前 234 行均有完整 `internal_name`、`real_name`、`gold`、`tier`、`max_spawns`、`card_type`、`elemental_affinity`、`requirements`、`bias_array`/`bias_entries` 和四职业权重。类型分布为 `ActionCard=104`、`AttackCard=58`、`ManaCard=22`、`SpellCard=50`。
- `card_data` tier 分布覆盖 1..10；`max_spawns` 分布为 0/1/2/3/5。116 行有非空 `requirements`，23 行有非空 `bias_entries`。
- `CardData.requirements` 已通过 `UserAttribute` enum 和 `achievements` join 命名：当前卡表中出现的 38 个 requirement id 全部已有 `requirement_names`。例如 `ForceOfWill -> MANA2`、`KineticCharge -> MANA4`、`Skullcrack -> DAMAGE3`、`DivineInspiration -> BOSSLICH`、`ElementalSurge -> WINDRUID2`、`WildStrike* -> WINCHAOS`、`WildMana -> WINCHAOS2`。
- `CardData.HasNever()` 和 `CardData.HasAccess()` 直接确认 requirement id `103` (`0x67`) 是 never/disabled：`HasNever()` 只检查该值，`HasAccess()` 看到该值会立即返回 false。`card_data` 将其标为 `never`。
- `card_access_rules.json` 已把 access gate 结构化到全部 234 条 `CardData`：118 条 `base_unlocked`、39 条 `requirement_locked`、77 条 `never`。其中 38 个 requirement id 产生 116 个卡牌引用；除 `never=103` 外，只有 `WINCHAOS=167` 和 `WINCHAOS2=168` 没有 `AchievementList` 行。`card_access_rules_smoke.json` 当前 17/17 通过，覆盖 `Attack2` 的 never guard、`StormSlash1` base unlocked、`Execute` 经 `DAMAGE2` 解锁，以及 `WildStrike1` 经 enum-only `WINCHAOS` fixture 解锁。
- `bias_entries` 已从小对象数组解码为 `id`、`applies_to_tier`、`value`、`raw64`。`CardData.GetTier(d)` 会把调用方 bias list 中匹配且 `applies_to_tier=true` 的 `value` 加到 tier；`CardData.FrequencyMod(b)` 会把匹配且 `applies_to_tier=false` 的 `value` 加到抽取频率。当前卡表中出现的 bias id 为 `1,4,5,6,14,15,16,17`。
- `card_metadata_recovery.json` 已把这些 bias id 与职业 `ClassBiases()` 交叉：23 条 `bias_entries` 全部解码，8 个出现在 `CardData` 中的 bias id 均能链接到至少一个职业 bias；另有 9 个职业 bias id 当前没有 `CardData.bias_entries` 使用。该 artifact 当前 20/20 通过，同时确认 `colors/subtypes` 单卡值仍为 0 个安全恢复值，并把真实 Card 访问器证据、slot 98/123 虚槽扫描和静态/全局 offset collision 分开记录，避免误当作卡牌 metadata。
- `card_data` 中 21 行有非零 `elemental_affinity`，分布为 `fire(2)=5`、`earth(3)=5`、`water(4)=5`、`air(5)=6`；其余 213 行为 0。该结果与 `cards_partial` 从 `Card.PythonInitialize` 里恢复的 21 张非零元素卡一致。
- 单卡类的 `Initialize`/`PythonInitialize` 方法中可见名称、规则文本、cost、tier、decay 目标、AI 值和部分数组字段写入；例如 `Fireball.PythonInitialize` 写入显示名 `Fireball`、文本 `Deal 8 @fir damage.`、tier/费用/decay，并在 `[this + 0x170]` 的 fire 槽位加 1。
- `extract_card_initializers.py` 已从 370 个卡牌类的 `PythonInitialize` 中生成第一版保守卡牌表：
  - `extracted/structured/cards_partial.json`
  - `extracted/structured/cards_partial.tsv`
- `cards_partial` 当前包含 `class_name`、`card_type`、`display_name`、`rules_text`、`flavor_text`、`decay_to`、`method_index`、string literal 引用，以及从对象字段写入读出的核心数值。
- 已由 `Card` 基类方法反汇编确认的字段：
  - `action_cost = [this + 0xa8]`，证据是 `Card.GetActionCost` / `extracted/disassembly/Card_GetActionCost.txt`
  - `mana_cost = [this + 0x124]`，证据是 `Card.GetManaCost(ignoreOneFreeSpell)` / `extracted/disassembly/Card_GetManaCost_ignoreOneFreeSpell.txt`
  - `gold_cost = [this + 0x128]`，与 `Card` 字段表中 `manaCost -> goldCost -> level -> maxLevel` 的连续布局一致
  - `level = [this + 0x12c]`、`max_level = [this + 0x130]`，证据是 `Card.NextLevel`、`Card.MaxLevel`、`Card.LevelString`
- `tier = [this + 0x13c]` 已按 `Card` 字段顺序命名；`0x168` 是 `AIPlaySequence` 与 `AIKeepValue` 两个连续 int 的 packed 64-bit 写入，提取器已拆成 `ai_play_sequence`、`ai_keep_value`。
- `elemental_affinity = [this + 0x170]` 已通过 `Card.Initialize(forcePhysical)` 和多张元素法术的数组元素递增写入确认；目前提取器恢复 21 张卡的非零 affinity，索引命名为 `fire=2`、`earth=3`、`water=4`、`air=5`，例如 `Fireball {fire:1}`、`Blizzard {water:2}`、`Shock {air:1}`、`AcidLance {earth:1}`。
- `colors = [this + 0xb8]`、`subtypes = [this + 0x118]` 已由 `Card` 字段表和访问器反汇编确认。证据包括 `Card.HasColorAsManaType(mt)` 读取 `[this + 0xb8]`，`Card.Subtypes(s)` 写入/读取 `[this + 0x118]`，`Card.SubtypeString()`/`Card.HasSubtype(s)` 读取 `[this + 0x118]`。`card_metadata_recovery.json` 还把 `AdditionalExp.PythonInitialize` 的 `0x118` 命中归类为静态 init guard，把 `PrayerOfLife.PythonInitialize` 的 `0xb8` 命中归类为静态 literal cache，把 `Card.ChangeText` 的 `0xb8` 命中归类为静态 init guard；slot 98/123 虚槽扫描命中 58 条，但 Card 子类 `PythonInitialize` 初始化命中为 0，5 条 Card 子类命中均是运行时/effect slot-123 路径，不是 `this.colors/subtypes` 单卡赋值。
- `card_type` 来自 `Card.CardTypeString()` 中的 type-index 判定，已恢复普通七类：Action、Equipment、Spell、Attack、Mana、Reaction、Prayer。370 张中 343 张可归入这七类，另有 27 张为 Bonus/Penalty/Curse 等特殊卡。
- `decay_to`/升级回退目标已从字符串字段写入恢复 189 条，例如 `Fireball -> Sear`、`Attack4 -> Attack3`、`Blizzard -> FrostBolt`。
- `cards_partial` 覆盖 370 个卡牌类，其中 350 个有 display name，343 个有 rules text，21 个有二进制确认的非零 elemental affinity。缺文本的主要是额外属性、Penalty/Curse 等特殊卡或占位卡。
- `card_effect_index` 覆盖同一批 370 个卡牌类，已把效果入口方法结构化：304 张有 play 入口，300 张有 `PythonPlayEffect`，23 张有 `ReactionEffect*`/`Trigger*` 入口，108 张还有其他自定义方法（如 equipment enter/leave、modal choice、penalty、death prevention 等）。这个产物仍是入口索引，不等同于行为反编译，但已经给每张卡的后续反汇编提供稳定 method_index。
- `card_effect_slots` 导出 28 个卡牌效果相关类型的 1302 个虚方法 slot，合计 409 个不同 slot。关键基础效果已确认包括 `Card.GenerateMana(x)` slot `196`、`Card.Heal(x)` slot `209`、`Card.DealDamage(n,dtype)` slot `212`。
- `card_effect_tailcalls` 对 300 个 `PythonPlayEffect` 入口做保守模式匹配，当前 97 个是“单次 IL2CPP virtual resolver + 间接尾调用”的简单效果，97 个都能映射到已知 slot。已机械恢复的基础操作分布包括 `Card.DealDamage=50`、`Card.GenerateMana=11`、`Card.PoisonPlayer=12`、`Card.PlayerShield=5`、`Card.DRTemp=5`、`Card.Heal=2`、`Card.Burning=2` 等。
- `DamageTypes` 和 `ManaTypes` enum 已按 metadata 字段顺序加入 `card_effect_tailcalls.source.enum_maps`；例如 `dtype=1(PHYSICAL)`、`2(FIRE)`、`3(EARTH)`、`4(WATER)`、`5(AIR)`、`6(RAW)`。
- 代表性已解码样例：`Attack1 -> Card.DealDamage(n=1,dtype=1(PHYSICAL))`、`Fireball -> Card.DealDamage(n=8,dtype=2(FIRE))`、`Heal -> Card.Heal(x=5)`、`Mana1 -> Card.GenerateMana(x=2)`。对应反汇编已用 `--stop-at-branch` 截断，避免把尾调用后的下一个方法混入样例。
- `card_effect_calls` 覆盖 `card_effect_index` 中的 812 个 play/reaction/other custom 方法，其中 596 个有可提取操作、72 个有 literal 引用；1483 个虚调用中 1483 个已解析到方法名，未剩未命名 slot；另解析出 43 个直接 `bl` 方法调用。该 trace 现在会截断外部无条件尾跳，避免把相邻方法混入当前方法统计。
- `card_effect_calls` 的高频效果调用包括 `Card.get_player=289`、`Card.DealDamage=147`、`Player.Enemy=80`、`Card.Draw=51`、`Card.GenerateMana=23`、`Player.get_health=23`、`Card.PoisonPlayer=19`、`Card.Heal=18`、`Card.IsPhysical=18`。这些是从复杂方法级 trace 中汇总的调用证据，不等同于最终控制流语义。
- 新增解析的复杂卡牌行为包括：`BadWishes` / `BadWishes2` 的惩罚候选列表已标为合成 `StringList`，可恢复 `Add` / `Count`，随后构造 `ActionPenaltyChoice` 并调用 `set_parentCard` / `SetUp`；`Protean.HasDuplicates` 的临时集合已标为合成 `StringHashSet.Contains`；`CoinToss` / `Pickpocket` / `SoulSiphon` / `AdditionalCard` 可解析到 `DungeonPlayer.GainGold` / `GainMana` / `CardCost`；`Piety` 可解析到 `PrayerCard.RemoveCounter`；`Gag`、`Hamstring`、`Misdirection`、`WrathOfGod` 可解析到目标卡 `Discard`；`ElementalFortitude`、`DancePuppets`、`PenaltyPuppet` 中构造的 `EffectPlayerAttribute` 可解析到 `TickEndTurn()`；`ChaosPrayer`、`CurseOfDoom*`、`Haste`、`Storm`、`Obliterate` 的物理表现调度可解析到 `GamePhysical.AddToVisualStack` / `AddToVisualStackNoYield`，且 `Obliterate` 的全局牌枚举可解析到 `Player.get_globals`、`VirtualObject.Children` 和 `Card.Die`。
- `cards.json` 是第一版 remaster-facing 合并目录，覆盖 370 个卡牌类，并把来源拆成 `initializer`、`availability`、`effect_methods`、`simple_play_effect`、`behavior_trace`、`runtime_rule` 和 `rule_coverage`。其中 234 张有 `card_data` 的商店/抽取/requirement 数据，300 张有 `PythonPlayEffect` tailcall 记录，370 张都有 `card_effect_calls` trace 类归属。
- `cards.summary.rule_status_counts` 当前为 `simple_play_effect=91`、`trace_available=278`、`index_only=1`。`simple_play_effect` 表示可以从单一 tailcall 直接读出基础操作；`trace_available` 表示已有方法级调用证据但仍需翻译控制流；`index_only` 表示目前只有入口索引。所有条目的 `rule_coverage.executable_rule_complete` 都显式为 false，避免误用为完整运行时规则。
- `cards.summary.runtime_rule_status_counts` 当前为 `simple_tailcall=97`、`structured_trace=273`；`runtime_effect_step_count=633`、`runtime_manual_control_flow_step_count=10`、`runtime_state_write_step_count=23`、`runtime_construct_step_count=21`，`runtime_unresolved_operation_count=0`。所有 context/read-only 卡牌条目都已推进到保守 normalized rule；`structured_trace` 是 trace 调用到统一 runtime effect、状态写入、构造器、manual control-flow 或 named preserved call 的映射，不代表分支条件、计算值和 modal 选择已经完整可执行。
- `card_runtime_smoke.json` 现在提供卡牌专用 runtime fixture：45/45 通过，覆盖 `Attack1`、`Fireball`、`Mana1`、`Accelerate`、`Study`、`Armor`、`BootsOfSpeed`、`Scimitars`、`PrayerOfViolence`、`GaussHourglass`、`Curse`、`PenaltyCurses`、`StormBlade`、`AdditionalAction`、`BadWishes` 等代表性规则。它验证 633 个卡牌 effect label 都有共享 interpreter handler，并额外记录 state_write / construct 步骤；但它仍是保守回归夹具，不等同于完整原版 timing、RNG、UI 选择、equipment lifecycle 或 per-card AI。
- `card_runtime_matrix_smoke.json` 现在提供全卡 source-method runtime matrix：15/15 通过，覆盖 370 张卡的 599 个 recovered source-method case，其中 424 个包含 effect/recorded 步骤，执行得到 633 个 effect event 和 60 个 state_write / construct / lifecycle recorded event；异常数和 unsupported runtime event 数均为 0。它把 `card_runtime_smoke.json` 的代表性断言扩展为全卡 interpreter 覆盖，但仍不等同于完整原版 timing、RNG、UI 选择、equipment lifecycle 或 per-card AI。
- `card_manual_control_flow_replay.json` 现在把手工反汇编恢复的 5 张卡单独固定为 branch fixture：35/35 检查通过，覆盖 `BootsOfSpeed`、`GaussHourglass`、`PenaltyCurses`、`PenaltyCursesFinal` 和 `PrayerOfViolence` 的 10 个 manual control-flow step、4 个 source method、9 个正向分支 fixture 和 10 个跳过分支 fixture；它证明这些分支能通过共享 runtime 执行，但仍不等同于原版准确 combat timing、reaction ordering 或 UI scheduling replay。
- `card_recorded_step_replay.json` 现在把 recorded state/object 步骤单独固定为 fixture：16/16 检查通过，覆盖 52 张卡、60 个 recorded step、56 个 source-method case；其中 `state_write=23`、`construct=21`、`lifecycle_call=16`，构造类型 16 种，state write 目标/字段组合 20 种。它证明这些记录步骤都能通过共享 runtime 生成确定性事件，但仍不是 typed Unity object lifecycle、UI ownership 或 before/after invariant replay。
- `card_lifecycle_replay.json` 现在把 `Card.DestroyDie`、`Bounce`、`Flick`、`Destroy`、`WipeClean` 和 base `OnDeath/ShouldBounceOnDeath` 单独固定为确定性 lifecycle fixture：14/14 检查通过，覆盖 6 个 recovered lifecycle case，并验证顺序和状态变化；它仍不是 Unity physical object refresh、coroutine timing 或 receiver-specific subclass replay。
- `card_equipment_lifecycle_matrix.json` 现在把 42 张 Equipment 卡的 enter/leave、turn、trigger、CheckCounters 和 recorded lifecycle/state-write source-method 单独固定为确定性 fixture matrix：18/18 检查通过，覆盖 74 个 recovered source-method case、92 个 effect event、27 个 recorded event 和 6 个 CheckCounters case；它仍不是 original equipment timing、receiver object identity 或 Unity lifecycle replay。
- `card_zone_replay.json` 现在把 `Card.Exile`、`Die`、`Discard`、`MoveToGraveyard` 和 `GetAction` 单独固定为确定性 zone/action fixture：14/14 检查通过，覆盖 6 个 recovered zone/action case，并验证 Exile hand-size/Destroy、Die exile/normal graveyard 分支、Discard tail-call、MoveToGraveyard parent notify 和 GetAction existing/missing lookup；它仍不是 Unity physical object refresh、coroutine timing、live action construction 或 receiver-specific subclass replay。
- `card_payment_replay.json` 现在把 `Card.ResourcesOff`、`CanAfford`、`PayFor` 和 `WillBeCountered` 单独固定为确定性 payment/counter fixture：17/17 检查通过，覆盖 9 个 recovered case，并验证 action/mana deficit 合并、archthief/archmage/oneFreeSpell/freeSpellTurn/tempSpellReduce 成本修正、free-once NotFreeOnce 分支、theft action payment、GainMana 负数扣费、one-free-spell 消耗和 Enemy.WillCounter delegation；它仍不是 Player.PayFor internals、Unity UI refresh、live combat timing 或 original action/mana visual replay。
- `card_play_replay.json` 现在把 `Card.PlayMe` 和 `Player.PlaySideEffects` 单独固定为确定性 play/side-effect fixture：17/17 检查通过，覆盖 9 个 recovered case，并验证 priority/Game.PlayingCard 通知顺序、copyNext、CounterMe/DodgeMe/ElusiveMe 早死、reaction marker 早死、delayed reaction/DecayTrigger、doubleSpell replay、fluid/actionMana/barbed/actionPoison/actionDraw 副作用；它仍不是 reaction marker object semantics、subclass PlayEffect timing、Unity visual order 或 coroutine replay。
- `card_get_ai_value_adjustment_recovery.json` 固定 `Card.GetAIValue` 的 slot 105 边界：33/33 检查通过；`Card.get_player` 证明 card offset `0x38` 是 Game 引用，`Card.GetAIValue` 再读 `Game.scenario` offset `0x150`，并 tail-call `ScenarioBase.ChangeValue(slot 105)`；`ScenarioBase.ChangeValue(c, valIn)` 本体只有 `ret`，所以基类场景调整为 identity/no-op。该 artifact 仍不声明 live combat-state replay 完成。
- `ai_value_runtime_smoke.json` 固定共享 runtime 上的 AI value formula fixture：18/18 检查通过，用 `tools/remaster_runtime.py` 执行 `Card.AIWeight`、`Card.GetAIValue` scenario identity、`Creature.AIWeight` 和 real-hero bonus fixture；它证明 recovered formula 能被 runtime fixture 消费，但仍不是原版 live scenario/combat-state replay。
- `card_ai_value_matrix_smoke.json` 固定全卡共享 runtime AI value matrix：14/14 检查通过，执行 370 个 `Card.AIWeight` case 和 370 个基类 `Card.GetAIValue` scenario identity case，scenario identity mismatch 为 0；224 张 action cost <= 0 的卡按公式下限 1 计，base weight 范围 1..21。它证明全卡公式能被 runtime 消费，但仍不是原版 live scenario/combat-state replay。
- `card_ai_priority_matrix_smoke.json` 固定全卡共享 runtime ActionPlay priority matrix：20/20 检查通过，执行 370 个未反制 `ActionPlay.AIPriority` case、370 个反制 priority case 和 740 个 `ActionPlay.AIUseValue` case；未反制 priority 范围 5..100，反制 priority 范围 0..100，273 张卡在两条分支间 priority 会变化。它证明全卡 ActionPlay priority 公式能被 runtime 消费，但 original target legality、candidate/list identity、turn-level AI replay 仍为 false。
- `card_actionplay_bridge_matrix_smoke.json` 固定全卡共享 runtime ActionPlay candidate bridge、hand-enumeration 和 action+target pair matrix：42/42 检查通过，370 张 recovered card 均能打包为 normal `ActionPlay` candidate，合成全卡手牌经 `GetAllActionsInternal` / `GetAllActions` 形态过滤后保留 370 个 candidate，并经 `GetAllActionsPlusTargets` 形态包装为 370 个 synthetic action+target pair；null action、payload mismatch、cost mismatch、slot mismatch、candidate order mismatch、pair order mismatch 均为 0。它证明 `Card.GetAction`、recovered hand enumeration/filter 和 action+target pair 包装形态可由 runtime 消费，但 original `GetActions` list identity、target legality、candidate ownership、turn-level AI 和 RNG/list replay 仍为 false。
- `card_ai_value_recovery.json` 固定卡牌 AI value 基线：45/45 检查通过，`Card.AIWeight` 恢复为 `max(action_cost, 1) + mana_cost`，370 张卡都能从 initializer 成本算出 base weight（224 张 action cost <= 0 会按 1 计，weight 范围 1..21）；基类 `Card.AIPlayValue`、`Card.AIFlatValue`、`Card.AIMultValue` 均返回 0；`Card.GetAIValue` 形态为先虚派发 `AIWeight(slot 157)`，Creature/real-hero 分支加 10，再在存在 `game.scenario` 时 tail-call `ScenarioBase.ChangeValue(slot 105)`，基类 hook 为 identity/no-op。它还纳入 `ai_value_runtime_smoke.json` 的 18/18 runtime fixture、`card_ai_value_matrix_smoke.json` 的 14/14 全卡公式 fixture 和 `creature_ai_weight_recovery.json` 的 37/37 公式恢复：`intrinsicAIValue + 1 + AIFlatValue + MaxAttacks*(BASE_POWER+POISON) + (range-1)*0.75 + invis/movedSinceInvis/hero/PERMSHIELD`。Creature combat-state replay 和完整 card AI value replay 仍为 false。
- `card_effect_value_flow_index.json` 现在把 card computed/receiver value-flow 队列收窄为 deterministic argument-shape map：20/20 检查通过，覆盖 115 张卡、179 个 `effect_without_immediate_args` step 和 71 个 operation；其中 119 个是 zero-parameter receiver effect，60 个仍需要参数值恢复，argument-flow 队列包括 combat-state payload、string/list payload、choice/action payload、object-graph payload、deck/card payload、visual-stack payload 和 game-turn context；unclassified=0、matrix missing event=0，但 `original_parameter_value_replay_complete=false`、`original_receiver_identity_replay_complete=false`。
- `card_original_accuracy_flag_index.json` 现在把 39 个 card/runtime/AI/action pipeline artifact 中剩余的 false original-accuracy/readiness flag 规范化为独立索引：13/13 检查通过，记录 362 个唯一 false flag entry、8,052 次 occurrence 和 9 个工作队列（card rule completion、value-flow/parameter/receiver identity、RNG/index stream、AI candidate/target/turn policy、branch timing、typed lifecycle/zone/payment/play、action/reaction/visual turn flow、metadata colors/subtypes、global readiness），uncategorized=0。它仍是 promotion checklist，不代表这些 false flag 已完成 replay。
- `card_runtime_accuracy_gaps.json` 明确记录 interpreter 覆盖之后仍未完成的卡牌原版准确性工作：169/169 检查通过，并消费 `card_original_accuracy_flag_index.json` 的 13 个 false-flag index 检查；370/370 张卡仍为 `executable_rule_complete=false`；5 张卡有 10 个手工 control-flow 步骤，`card_manual_control_flow_replay.json` 已把它们细化为 19 个 branch fixture 且 original replay 仍为 false；52 张卡有 60 个 recorded state/construct/lifecycle 步骤，`card_recorded_step_replay.json` 已把它们细化为 56 个 source-method fixture 且 original lifecycle replay 仍为 false；`card_lifecycle_replay.json` 已把 6 个 recovered lifecycle case 细化为 14 个 deterministic fixture check 且 original Unity lifecycle replay 仍为 false；`card_equipment_lifecycle_matrix.json` 已把 42 张 Equipment 卡细化为 18 个 deterministic fixture check，覆盖 74 个 recovered source-method case、92 个 effect event、27 个 recorded event 和 6 个 CheckCounters case，且 original equipment timing/object identity replay 仍为 false；`card_zone_replay.json` 已把 6 个 recovered zone/action case 细化为 14 个 deterministic fixture check 且 original Unity zone replay 仍为 false；`card_payment_replay.json` 已把 9 个 recovered payment/counter case 细化为 17 个 deterministic fixture check 且 original Unity payment replay 仍为 false；`card_play_replay.json` 已把 9 个 recovered play/side-effect case 细化为 17 个 deterministic fixture check 且 original Unity play replay 仍为 false；`card_ai_value_matrix_smoke.json` 执行 370 个 AIWeight 和 370 个 base GetAIValue identity case，`card_ai_priority_matrix_smoke.json` 执行 370+370 个 ActionPlay priority case 和 740 个 AIUseValue case，`card_actionplay_bridge_matrix_smoke.json` 执行 370 个 ActionPlay candidate bridge case、370 个 full-hand filtered candidate case 和 370 个 synthetic action+target pair case，null action/payload/cost/slot/candidate-order/pair-order mismatch 均为 0，但 original combat-state/target/turn replay 仍为 false；214 张卡有 722 个 read/context 依赖，`card_context_dependency_index.json` 已把它们分到 11 个 remaster subsystem owner 队列（current-card owner 289、opponent context 95、player combat state 87、zone/object graph 75、card private state 74 等），uncategorized 为 0，但 original value-flow replay 仍为 false；115 张卡有 computed/receiver value 缺口，`card_effect_value_flow_index.json` 已把 179 个 step 分入 8 个 argument-flow 队列且 unclassified=0，但 original parameter-value/receiver-identity replay 仍为 false；23 张卡有 RNG/shuffle 候选，74 张卡有 UI/choice/visual/target 候选，42 张 equipment 类型卡已有 deterministic lifecycle matrix，46 张 lifecycle 候选卡仍需完整原版装备生命周期测试；370 张卡只有 AI scalar/base-value/ActionPlay priority/ActionPlay bridge/hand-enumeration/action-target-pair evidence，行为级 AI 规则为 0；colors/subtypes accessors 已确认且 3 个 offset collision 已排除，但单卡值仍为 0。Blur/Incorporeal/StormShape/VaporForm 已从 `EffectPlayerAttribute..ctor` tailcall 反汇编恢复为 binary effect：Blur/Incorporeal 用 att=24 赋予 physical resistance，StormShape 用 att=102/manaShield 赋予 mana-shield/storm-shape damage-to-mana effect，VaporForm 用 att=23 赋予 physical immunity；Anticipate/Enthrall 已确认构造 `ActionAnticipate` 并由 `ActionAnticipate.DiscardPerform` 经 `Card.ChangePlayer -> Player.get_hand -> Hand.MoveHere` 取得对手手牌，Bewitch 构造 `ActionBewitch` 并调用 `Card.Exile`，Charm 构造 `ActionCharm` 并调用 `Card.Die`，四者均由 `TriggerOnce` 包装；Scimitars 已确认 `ActionScimitars.OnTrigger` 保存 played-card payload 并调用 `Player.HandleEmergency`，`ActionScimitars.PerformAction` 再调用 `Player.DrawExNihil(slot 244)` 和 `Card.Temporary(slot 240)`；InfernalContract 已确认 `VirtualObject.ChildCount -> Deck.AddCardToDeck(Curse) -> Deck.Shuffle -> Player.DiscardAll`，SoulCrush 已确认状态 gate 后执行 `Card.DealDamage(10)` 与 `Card.ForceDiscard(2)`；剩余 rules-text assisted runtime card count 为 0。`card_rng_recovery.json` 进一步把这 23 张 RNG/shuffle 候选收窄为 27/27 通过的静态证据：25 个 random/shuffle step，覆盖 InGameRandomRange/Float、Player.RandomExile、Card.ForceRandomDiscard、Deck.RandomCardName、Deck.Shuffle、CardFinder.RandomAvailableNonEquipmentCard，并二进制确认 Stone 的 Curse 插入、Deck.Shuffle 和 ForceDiscard 形态，且补充 InfernalContract 的 Curse+Deck.Shuffle+DiscardAll 形态；original RNG stream 与 shuffle/index stream replay 仍为 false。
- `ai_action_candidate_replay.json` 现在把 `AIPlayer.GetAllActionsInternal`、`GetAllActions`、`AddHandCards` 和 `GetAllActionsPlusTargets` 固定为确定性 candidate/action+target fixture：13/13 检查通过，覆盖 5 个 recovered case，并验证 hand slot 183 枚举、`Card.GetAction(slot 143)`、非空 action 加入、card payload 去重、offset `0x64` invalid flag、`ActionBase.IsValid(slot 75)` 过滤、target-set offset `0x10/0x18` 成功标志和 target payload 打包；它仍不是 Unity collection identity、全量 IsValid override、target legality、target-set construction、AI turn ordering 或 RNG replay。
- `ai_target_legality_replay.json` 现在把 `AIIsValidPlusTargets`、默认/特化 `AIGetTargets`、discard target heuristic、`AIPlayer.GetAITargets` 随机不放回采样、target-set provider 分支和 random-order target-set 固定为确定性 fixture：21/21 检查通过，覆盖 10 个 recovered case，并验证 result/cache offset `0x10/0x18/0x90/0x98`、slot `75/77/79/81/85/112/159/431/432` 以及 `ai_keep_value` offset `0x16c`；它仍不是 Unity target wrapper identity、全量 IsValid/IsLegalTarget override、完整 target-set combination helper、list ownership 或 RNG stream replay。
- `target_wrapper_identity_recovery.json` 现在把 target wrapper 和 target-set identity 的静态边界单独固定：42/42 检查通过，恢复 `TargetFinderParameters` / `TargetTypes` / `TargetFinderBase` / `TargetFinderMulti` metadata、target result/cache layout、ActionBase/AIPlayer target wrapper payload read、target-set payload layout、list add/remove mutation shape、`Game.RandomRange(a,b)` inclusive upper bound、AI target sampling tail-swap removal 和 random-order target-set shuffle；它仍不是 original Unity `TargetWrapper` object identity、target UI ownership、完整 `IsLegalTarget` override、target-set list ownership/order 或 RNG stream replay。
- `ai_rng_list_order_recovery.json` 现在把 AI RNG 与 list-order mutation 静态边界单独固定：22/22 检查通过，恢复 `Game.RandomRange(a,b)` inclusive upper bound、`GetHighestPriorityAction` tie-break RNG、`AIPlayerRandom.Behave` candidate selection/tail-swap removal、provider target-set random index、single/random target tail-swap sampling、target-set random-order Fisher-Yates shuffle 和 `PerformRandomAction` 随机 action/target 派发形态；它仍不是 original System.Random/Unity RNG stream、cross-call interleaving、candidate list ownership、target list ownership 或 full AI turn replay。
- `ai_turn_policy_replay.json` 现在把 `AIPlayerRandom.AmIDone/Behave/GetAITargetsRandom/AIPerformAction/AIPass` 和 `AIPlayer.PerformRandomAction` 固定为确定性 turn-policy fixture：23/23 检查通过，覆盖 12 个 recovered case，并验证 done flag/threshold/baseline offset `0x148/0x160/0x164`、action performer offset `0x140`、target wrapper payload offset `0x58`、slot `4/20/22/31/32/75/78/92/112/159/416/417/418/419/427/428/442/443/444`、invalid candidate swap-out、target-set provider path、emergency action path、pass path 和 PerformRandomAction 三条路径；它仍不是原版 candidate list ownership、Unity RNG stream、完整 action side effects、UI/end-turn behavior 或 full monster-turn replay。
- `ai_policy_recovery_gaps.json` 把卡牌/怪物共用的 AI 缺口单独固定：54/54 检查通过，37 个 AI 相关类型、203 个 AI 相关方法、15 个 action AI 方法、13 个 card AI 方法（其中 7 个卡牌覆写）、48 个选中的 AI/value/policy target 扫描后直接 `bl` 命中为 0，并纳入 `card_ai_value_recovery.json` 的 45 个检查、`card_get_ai_value_adjustment_recovery.json` 的 33 个检查、`ai_value_runtime_smoke.json` 的 18 个 runtime fixture 检查、`card_ai_value_matrix_smoke.json` 的 14 个全卡 matrix 检查、`card_ai_priority_matrix_smoke.json` 的 20 个 ActionPlay priority matrix 检查、`card_actionplay_bridge_matrix_smoke.json` 的 42 个 ActionPlay bridge/hand-enumeration/action-target-pair matrix 检查和 `creature_ai_weight_recovery.json` 的 37 个公式检查；`ai_policy_virtual_slot_xrefs.json` 进一步固定 25 个选中 virtual slot 的 446 条 resolver-call 命中，其中 110 条来自 AI controller/action/card/object-value caller。`ai_virtual_dispatch_plan.json` 将这 110 条 policy 命中整理为 105 个 caller/slot 恢复组，其中 51 个 high-priority、21 个 medium-priority；policy domain 分布为 `ai_turn_policy=46`、`card_value=32`、`action_targeting=16`、`action_scoring=15`、`creature_combat_ai=1`。`ai_action_priority_recovery.json` 进一步恢复第一条 high-priority action scoring 边：`AIPlayer.GetHighestPriorityAction` 对候选 action 调 slot `146` 三次，`ActionBase.AIPriority` 返回 50，`ActionBase.AIGetNumberOfActions` 返回 0，`ActionPlay.AIGetNumberOfActions` 返回 1，`ActionPlay.AIPriority` 使用 `100 - (card.WillBeCountered() ? card.ai_keep_value : card.ai_play_sequence)`；`ActionBase.AIUseValue` 读取 action offset `0xb8` 并乘以 `-0.1`。`ai_highest_priority_selection.json` 继续恢复 `GetHighestPriorityAction` 的选择形态：先枚举 `GetAllActions()` 找最大 priority，再二次枚举收集所有同最大 priority 的 action；若列表为空返回 null，否则用 `RandomRange(0, count - 1)` 风格随机下标返回并列候选。`ai_action_candidate_recovery.json` 补上 `GetAllActions` 依赖：`GetAllActionsInternal` 通过 inherited hand slot `183` 和 collection slot `30` 枚举手牌，用 `Card.GetAction(slot 143)` 生成候选；`GetAllActions` 用 `ActionBase.get_card(slot 69)`、card payload offset `0xc0` 和 `ActionBase.IsValid(slot 75)` 过滤候选；`GetAllActionsPlusTargets` 调 `ActionBase.AIIsValidPlusTargets(slot 78)`，在 target-set offset `0x10` 成功时把 action 与 offset `0x18` 的 target payload 打包入列表。`ai_target_legality_recovery.json` 继续恢复后半段：`AIIsValidPlusTargets` 清 action target cache offset `0x90`，调用 `IsValid(slot 75)`，失败时写 result offset `0x10=false` / `0x18=null`，成功时使用 cached target offset `0x98` 或调用 `AIGetTargets(slot 79)`，再写 `0x10=true` / `0x18=target`；`ActionBase.AIGetTargets` 通过 `GetTargets(forAI=true)` 和 offset `0xa0/0xa1` 过滤候选，空过滤结果回退原列表；`ActionSingleTargetDiscard.AIGetTargets` 选择 card `ai_keep_value` offset `0x16c` 最高的 discard target；`AIPlayer.GetAITargets` 和 target-set 方法恢复了 target-finder count、随机不放回采样、slot `431/432` random-order target-set 以及 slot `159` provider 分支。`ai_turn_policy_recovery.json` 继续恢复回合执行层：`AIPlayerRandom.AmIDone` 用 self offset `0x148/0x160/0x164` 和状态 offset `0xfc` 判定结束；`AIPlayerRandom.Behave` 随机抽候选 action，调用 `AIIsValidPlusTargets(slot 78)`，对非法候选用 collection get/set slot `31/32` 交换移出，成功后调 `GetAITargetsRandom(slot 443)` 和 `AIPerformAction(slot 444)`，阻塞时调 `AIPass(slot 417)`；`GetAITargetsRandom` 调 `SetTargetFinderParameters(slot 112)` 并按 target count 随机不放回采样；`AIPerformAction` 把 target wrapper offset `0x58` payload 打包入 invocation，再调 slot `4` 和 `ActionBase.Perform(slot 92)`；`AIPass` / `DoAIPass` 和 `PerformRandomAction` 的 pass/随机派发形态也已固定。`action_execution_recovery.json` 继续向下恢复执行层：`ActionBase.Perform(targets)` 读 offset `0xbf` 并派发 slot `93`；`Perform(targets, shouldTransmit)` 调 `PayCostsTargets(slot 106)`、`HandleInvisibility(slot 94)`、`PerformAction(slot 103)`、`ShouldCheckSBEs(slot 105)` 和 `TriggerCleanUp(slot 98)`；`ActionBase.Resolve` 写/清 game current action offset `0xc0` 并调 `ResolveAction(slot 108)`、`CantResolve(slot 110)`、`FinishResolution(slot 102)`；`ActionPlay.PayCosts/PerformAction` 分别接到 `Card.PayFor(slot 115)` / `Card.PlayMe(slot 179)`；`AIPhysical.GetStackInput` 在无显式输入时走 `GetHighestPriorityAction(slot 429)`、`GetRandomTargetSet(slot 430)`、`ActionBase.Perform(slot 92)`；`GamePhysical.AddToVisualStack*` 的 shouldYield/dirty flag/visual stack enqueue 形态也已固定。关键 `AIPlayer` / `AIPlayerRandom` / `Card.GetAIValue` 反汇编包含 28 个 `blr` 间接调用和 6 个 `br` 间接分支。因此下一步不能只靠普通 direct-call xref，需要沿 virtual slot / vtable 派发解析 receiver family，再把 candidate replay、target legality、target-set replay、AI turn replay、action execution replay、card play replay、visual-stack replay、action scoring、pass/end-turn 和 card AI value 消费写成可执行回归。
- `action_execution_replay.json` 现在把 `ActionBase.Perform/Resolve`、`ActionPlay` 桥、`ActionPass`、`ActionAddToDeck`、`ActionDungeonChoose`、`AIPhysical.GetStackInput` 和 `GamePhysical.AddToVisualStack*` 单独固定为确定性 action/visual-stack fixture：19/19 检查通过，覆盖 11 个 recovered case，并验证 shouldTransmit flag 派发、PayCostsTargets/HandleInvisibility/PerformAction 顺序、action counter decrement、visual-stack no-yield refresh、currentAction 写清、Card.PayFor/Card.PlayMe 桥、pass payload、deck payload、dungeon choice delegate、AIPhysical priority/target/action dispatch、shouldYield/noYield visual stack 入队；它仍不是完整 reaction/SBE manager、target wrapper type identity、Unity coroutine/yield 或 UI object graph replay。
- `action_runtime_accuracy_gaps.json` 现在把 AI candidate、全卡 ActionPlay bridge/hand-enumeration/action-target-pair、target legality/target-set、target wrapper identity、AI RNG/list-order、turn/pass/invocation、action execution、reaction/SBE cleanup、visual-stack coroutine、Player payment、pass/end-turn、card payment/play/zone/lifecycle/equipment 和 starter combat fixture 串成执行层原版准确性队列：76/76 检查通过，汇总 640 个上游检查、11 类 blocking gap，并确认 deterministic action runtime、AI-to-action、card pipeline 与 starter fixture coverage 都为 true；target-set ordering、`Game.RandomRange` inclusive upper bound、AI RNG/list-order 静态形态和 all-equipment deterministic lifecycle matrix 也已恢复；同时 original action execution、visual stack/coroutine、reaction/SBE、player payment/UI、pass/end-turn、card payment/play/zone/lifecycle/equipment、AI turn、AI action execution、candidate、target legality、target-set replay、AI RNG stream/list ownership、TargetWrapper identity replay 和 target-set identity replay 全部仍为 false。下一步应按这个队列补 ActionBase.Perform/Resolve 原版 timing、original visual-stack replay、reaction/SBE cleanup replay、Player.PayFor/payment UI replay、original pass/end-turn replay、Card.PlayMe subclass timing、typed zone object identity、target wrapper identity、target-set identity、AI RNG/list order 和 equipment trigger timing/object identity。
- `reaction_sbe_cleanup_recovery.json` 现在把该队列中的 reaction/SBE cleanup 静态形态单独固定：29/29 检查通过，覆盖 17 个反汇编文件、7 类 gap、2 个 `ActionBase.TriggerCleanUp` overload、`ActionBase.ShouldCheckSBEs`、`Game.CheckStateBasedEffects`、6 个 `Game.Check*Reactions`、12 个 `ReactionCard` reaction 方法、`Counterspell.ShouldTrigger/ReactionEffect`、`ActionDelayedReaction.PerformAction` 和 `ActionAddCounter.PerformAction`；它仍不声明 original reaction/SBE replay 完成，因为真实 Unity reaction message identity、state-based-effect 循环时机、cleanup coroutine/yield 和 counter/delayed reaction end-to-end replay 尚未跑通。
- `card_play_pipeline_recovery.json` 在 `ActionPlay -> Card.PayFor/Card.PlayMe` 桥下面继续恢复 Card 层静态执行形态：`CanAfford`/`ResourcesOff`、action/mana cost、`PayFor` 的 free-once/theft/mana/oneFreeSpell 分支、`WillBeCountered`、`PlayMe` 的 copyNext/counter/dodge/elusive/reaction/trigger/side-effect/preplay/playeffect/delayed reaction/decay/doubleSpell 形态、`Player.PlaySideEffects` 的 fluid/actionMana/barbed/actionPoison/actionDraw 分支、`Exile`、`Die`、`DestroyDie`、base `OnDeath`、`Bounce`、`Flick`、base `ShouldBounceOnDeath`、`Destroy`、`WipeClean`、`Discard -> Die`、`MoveToGraveyard` 和 `GetAction` 枚举都已固定，当前 50/50 通过；`card_play_replay.json` 已把 `PlayMe` / `Player.PlaySideEffects` 细化为 17 个 deterministic fixture check；`card_payment_replay.json` 已把 `ResourcesOff` / `CanAfford` / `PayFor` / `WillBeCountered` 细化为 17 个 deterministic fixture check；`card_zone_replay.json` 已把 `Exile` / `Die` / `Discard` / `MoveToGraveyard` / `GetAction` 细化为 14 个 deterministic fixture check；`action_execution_replay.json` 已把上层 ActionBase/ActionPlay/pass/visual-stack 细化为 19 个 deterministic fixture check；`remaster_starter_combat.json` 已覆盖 normal starter path 的 ResourcesOff/CanAfford 费用修正 catalog、支付分支 catalog、WillBeCountered Enemy.WillCounter priority boundary、Card.PlayMe order、inactive reaction catalog、Player.PlaySideEffects branch boundary/catalog、zone lifecycle/GetAction catalog、discard-to-Die boundary、Die graveyard boundary、DestroyDie/Bounce/Flick/Destroy/WipeClean static lifecycle catalog、deterministic lifecycle fixture、Sword equipment lifecycle boundary、Sword.CheckCounters equipment trigger boundary、Sword trigger gate boundary、出牌和弃牌/装备区转移 fixture，但全量 card payment/Player.PayFor/payment UI、reaction marker semantics、subclass PlayEffect timing、active counter branch、card play、card zone/Die/Exile/DestroyDie/Bounce/Flick/Destroy/WipeClean 和 equipment lifecycle replay 仍为 false。
- 基础样例在新 trace 中和旧 tailcall 一致：`Attack1.PythonPlayEffect -> Card.DealDamage(n=1,dtype=1)`、`Fireball.PythonPlayEffect -> Card.DealDamage(n=8,dtype=2)`、`Heal.PythonPlayEffect -> Card.Heal(x=5)`、`Mana1.PythonPlayEffect -> Card.GenerateMana(x=2)`。
- 已恢复的复杂卡牌 literal 例子：`BadWishes.PlayEffect` 引用 `PenaltyCurses`、`PenaltyPlusCard`、`PenaltyDamage`、`PenaltyHeal`、`PenaltyMana`、`PenaltyMinusAction`、`PenaltyMinusCard`、`PenaltyPoison`；`ElementalFortitude` 引用 `Air/Earth/Fire/Water`；`StudyGuide.PlayEffect` 引用“从敌方弃牌堆选择一张牌，永久加入自己牌组”的完整文本；`SummonCompanions` 引用 `Turtle/Wolf/Eagle`。
- 已恢复的 reaction/equipment 样例：`Armor.EQEnterPlay` 调 `Card.get_player` 与 `Player.GolemWard`，离场调 `Player.UnGolemWard`；`Counterspell.ReactionEffect` 调 `Card.Draw`；`Cower.ReactionEffectDamage` 调 `Card.get_player` 与 `Player.Draw`；`ChainLightning` 的 reaction 入口可见 `Card.DealDamage` 和 `Game.get_activePlayer`。
- 对 `Card.Subtypes`、`Card.CardColor`、`Card.ACardColor` 的全游戏直接调用扫描只发现 2 条直接调用：
  - `BuiltInDecks.RandomDeck(s) -> Card.CardColor(s)`
  - `Card.CardColor(s) -> Card.ACardColor(s)`
- 对 slot `98` / `123` 的全游戏虚槽扫描发现 58 条 resolver call，但没有任何 Card 子类 `PythonInitialize` 命中；Card 子类相关命中只有 `CurseOfWeakness`、`InfernalContract`、`PenaltyCurses`、`PenaltyCursesFinal`、`Stone` 的运行时/effect slot-123 调用，不能解释为 colors/subtypes 初始化。
- 对 `0xb8`/`0x118` 的全游戏偏移访问扫描已导出：
  - `extracted/structured/card_color_subtype_call_xrefs.json`
  - `extracted/structured/card_color_subtype_call_xrefs.txt`
  - `extracted/structured/card_color_subtype_virtual_slot_xrefs.json`
  - `extracted/structured/card_color_subtype_virtual_slot_xrefs.txt`
  - `extracted/structured/card_color_subtype_field_accesses.json`
  - `extracted/structured/card_color_subtype_field_accesses.txt`
- 当前结论：`colors` 和 `subtypes` 的字段偏移与访问器已经确认；全游戏直接 helper call 只落在 `BuiltInDecks.RandomDeck(s) -> Card.CardColor(s) -> Card.ACardColor(s)` 的 deck filtering 路径，不是单卡初始化；slot 98/123 虚槽扫描也没有找到 Card 子类 `PythonInitialize` 初始化路径；已观察到的 `PythonInitialize` / `Card.ChangeText` 偏移命中是静态/全局 offset collision。每张卡的具体列表值仍没有以单卡 `PythonInitialize` 的直接 helper call、虚槽调用或直接字段写入模式保存，不能从现有提取器中安全自动填值。后续需要从 UI/卡牌行为侧反推，或找到另一条运行时初始化来源。

未完全确认：

- 每张卡的完整 `PlayEffect`/`ReactionEffect` 控制流和 `colors`/`subtypes` 的 per-card 值还没有全部自动恢复。现在已有合并后的 `cards.json` 作为 remaster 目录种子，`card_metadata_recovery.json` 已把 `CardData.bias_entries` 和元素亲和度边界结构化，`card_get_ai_value_adjustment_recovery.json` 已把 `Card.GetAIValue` 的 slot 105 场景 hook 解析为基类 identity，`card_ai_value_recovery.json` 已恢复基础 AI value/weight 链并纳入 scenario adjustment、`ai_value_runtime_smoke.json` runtime fixture、`card_ai_value_matrix_smoke.json` 全卡 formula fixture、`card_ai_priority_matrix_smoke.json` 全卡 ActionPlay priority fixture 与 `creature_ai_weight_recovery.json` 的 Creature weight 公式，`runtime_rule` 已把 trace 调用归一化成 effect / state_write / construct / lifecycle / manual_control_flow steps；`card_runtime_smoke.json` 覆盖一批代表性执行 fixture，`card_runtime_matrix_smoke.json` 覆盖全卡 source-method interpreter matrix，但它们仍不是可直接驱动完整战斗模拟器的规则文件。下一步应把 `structured_trace` 扩展成原版准确的全卡 timing、RNG、UI 选择、equipment lifecycle、per-card AI、AI value combat-state replay 和端到端规则测试，并继续寻找 colors/subtypes 的安全初始化来源。

当前直接未匹配到脚本的卡牌资源：

- `textures/cards/akamistormers`
- `textures/cards/clarity1`
- `textures/cards/clarity2`
- `textures/cards/copy`
- `textures/cards/corrosiveslash`
- `textures/cards/pierce`
- `textures/cards/professiondruidhuman`
- `textures/cards/professiondruidwolf`
- `textures/cards/selection_transparency`
- `textures/cards/shamanportrait`
- `textures/cards/sorcerousstrke1`

多数未匹配项是共用图、拼写差异、形态图或 UI 透明图，不一定是独立可打出的卡。

### 怪物与地牢 Actor

证据文件：

- 大图路径：`extracted/resource_paths.json` 的 `groups.dungeon_actor_big_art`
- 小图路径：`extracted/resource_paths.json` 的 `groups.dungeon_actor_tile_art`
- 脚本匹配：`extracted/content_inventory.json` 的 `dungeon_actors`
- 怪物文本语料：`extracted/corpora/monster_text_corpus.tsv`
- 怪物合并目录：`extracted/structured/monster_catalog.json`、`extracted/structured/monster_catalog.tsv`

已确认：

- big actor 资源 99 个，little/tile actor 资源 119 个。
- 93 个 big actor 可匹配脚本，例如 `GoblinKing`、`IceQueen`、`Lich`、`Vampire`、`UssuriWarQueen`、`AkamiStormcaller`。
- `MonsterData`、`MonsterList`、`AllMonsterNames`、`GetMonsterName`、`MonsterLevels`、`MonsterCounterString`、`AIMultValue`、`AIFlatValue` 等标识符存在。
- 怪物 display name、简短描述、bestiary/tips 文本集中在 literal index `6867-7105`，已导出为 TSV。
- `MonsterData` 数据模型已恢复出 13 个字段：
  - `internalName`
  - `locations`
  - `minLevel`
  - `maxLevel`
  - `boss`
  - `crypt`
  - `forest`
  - `dungeon`
  - `water`
  - `volcano`
  - `mountain`
  - `monsterName`
  - `bestiaryEntry`
- `MonsterData` 构造函数参数包括 `internalName`、`locations`、`minLevel`、`maxLevel`、`boss`、六类环境权重/开关、`monsterName`、`bestiaryEntry`。
- `MonsterData` 行为方法已恢复出 `Frequency(location)`、`CanFit(location,level)`、`FirstFloor()`、`GetTexture()`。
- `MonsterList` 有 `monsterList` 字段，并通过 `GetMonsterList()` 取得怪物表。
- `MonsterList.GetMonsterList` 已完成 native 反汇编并标注 string literal。旧的 literal 清单导出 179 个怪物列表相关 literal 到：
  - `extracted/structured/monster_literals_from_get_list.json`
  - `extracted/structured/monster_literals_from_get_list.tsv`
- `extract_monsters_from_disassembly.py` 已经从 `MonsterList.GetMonsterList` 的对象分配 + 字段写入序列恢复 73 行 `MonsterData`：
  - `extracted/structured/monsters.json`
  - `extracted/structured/monsters.tsv`
- 已恢复字段包括 `internal_name`、`display_name`、`bestiary_entry`、`locations`、`min_level`、`max_level`、`boss`、`crypt/forest/dungeon/water/volcano/mountain` 六类环境权重。
- `Environments` enum 已按 metadata 字段顺序恢复：`0=DUNGEON`、`1=WATER`、`2=VOLCANO`、`3=FOREST`、`4=CRYPT`、`5=MOUNTAIN`、`6=LAST`、`7=NONE`。
- `monsters.tsv` 中 boss 标记为 `true` 的条目有 18 个：`GoblinKing`、`IceQueen`、`Lich`、`Mime`、`ChromaticDemon`、`Titan`、`AkamiAscendent`、`UssuriWarQueen`、`GelatinousCube`、`Unicorn`、`Magmadon`、`Vampire`、`GiantShark`、`Genie`、`CumuloNimbus`、`Hydra`、`Phoenix`、`GiantSquid/Kraken`。
- `extract_monster_initializers.py` 已从 73 个怪物类的 `PythonBuildAttributes` 中恢复基础战斗初始化数据：
  - `extracted/structured/monster_initializers.json`
  - `extracted/structured/monster_initializers.tsv`
- `monster_initializers` 覆盖全部 73 个怪物类，`internal_name` 与 `monster_name` 均完整；71 个怪物有显式 `base_deck`，仅 `Clone` 与 `Hydra` 的基础 deck 为空。
- 已恢复的 `DungeonMonster` 初始化字段包括 `powers`、`monster_powers`、`base_deck`、`level`、`mana`、`boss`、`dilution`、`devour_type`、`health_mult`、`action_bonus`、`card_bonus`、`gold_mult`、`exp_mult`。
- 额外解析了简单返回数组形式的 `PreferredStartingCards` 和 `StartingEquipment`：51 个怪物有 preferred starting cards，14 个怪物有 starting equipment。`LevelUpDeck` 暂先导出该方法引用的 string literals，58 个怪物有可见 level-up deck literal。
- `monster_behavior_index` 已为 73 个怪物建立行为入口索引：72 个怪物直接声明 `LevelUpDeck`，43 个有 `ApplyToPlayer`，15 个有 `StartTurn`/`StartTurnAfterPoison`/`EndTurn`，1 个有 `ForbidPlay`（`Sphinx`），2 个有 revenant 特殊方法。该索引是入口表，不等同于行为反编译。
- `Monster` 基类虚方法 slot 已用于解析升级牌组调用：slot `112` 是 `Monster.DeckReplace(outCard,inCard)`，slot `115` 是 `Monster.DeckAdd(inCard)`。
- `monster_levelup_calls` 已从 72 个 `LevelUpDeck` 方法中提取调用序列：58 个怪物有可恢复操作，当前包含 `Monster.DeckAdd=200`、`Monster.DeckReplace=116`、`set_monster_powers=14` 和 `Monster.Invisible=1`。例如 `Goblin` 在有效等级门槛 `2` 与 `4` 各 `DeckAdd(GoblinAlly)`，`Demon` 设置 powers 文本并在有效等级门槛 `8` `DeckAdd(Corrupt)`，`AirElemental` 多次把 `StormSlash1` 替换为 `StormSlash2`，后续再替换/添加 `LightningStrike`、`StormSlash3`、`Zap`。额外手工核验的 boss 升级规则包括 `GiantSquid.LevelUpDeck` 单条 `ret`、以及 `Hydra.LevelUpDeck` 在 level 7/10 分别写 `Monster.health=90/150`。
- `monster_levelup_calls` 现在同时保留 `nearest_level_gate`、`active_level_gates` 和 `effective_level_min`。`active_level_gates` 追踪仍未到达 skip target 的 `level < N` 分支，`effective_level_min` 是当前操作地址上这些 active gate 的最大 `level_min`；这比最近比较更接近可执行规则，但仍不是完整控制流反编译，复杂分支需逐方法验证。
- `monster_behavior_calls` 覆盖 `ApplyToPlayer`、`StartTurn`、`EndTurn`、`StartTurnAfterPoison`、`ForbidPlay` 和 revenant 特殊方法共 63 个入口，其中 60 个有可恢复操作。当前追踪到 163 条虚调用，163 条已解析到具体方法，未剩未命名 slot。已命名的调用包括 `Player.AddToAttribute=43`、`Game.OtherPlayer=17`、`Game.get_them=16`、`Monster.Undead=10`、`Player.SetAttribute=6`、`Player.GainCounterFirst=5`、`Deck.AddCardToDeck=3`、`Player.DrawExNihil=3`、`Player.ElementalResist=3`、`Player.SuperFireShield=3`、`GamePhysical.AddToVisualStackNoYield=3`、`Card.IsPhysical=2`、`EffectPlayerAttribute.TickEndTurn=1`、`Phoenix.PhoenixAttributes=1`、`Phoenix.ToEgg=1`、`GoblinMechanist.BuildRandomEquipment=1`、`Game.DestroyTopMessage=1` 等。
- `monster_catalog.json` 是第一版 remaster-facing 怪物合并目录，覆盖 73 个怪物类，并把 `MonsterData`、`PythonBuildAttributes`、行为入口索引、`LevelUpDeck` 操作 trace、特殊行为 trace、`level_up_rule` 和 `behavior_runtime_rule` 合并到单条怪物记录中。73 个都有 `MonsterData` 和初始化数据，71 个有 base deck，58 个有 level-up 操作 trace，47 个有行为 trace；`AkamiAscendent`、`GelatinousCube`、`Genie` 和 `Mime` 的 boss 行为额外标记为由 `PythonBuildAttributes` 牌组/装备与卡牌 runtime 规则编码。
- `monster_catalog.summary.rule_status_counts` 当前为 `behavior_and_levelup_trace=39`、`behavior_trace_available=8`、`levelup_trace_available=19`、`index_only=7`。`level_up_rule_status_counts` 为 `structured_level_up=73`，共结构化 346 个升级步骤，未剩未解析升级操作；其中 10 个单指令 `ret` 的 `LevelUpDeck`、`Clone` 继承的基类空 `Monster.LevelUpDeck`、`Sphinx` / `Treant` 的生命值阈值写入已用反汇编或 trace 证据补齐。`behavior_runtime_status_counts` 为 `structured_trace=73`，共结构化 137 个行为效果步骤，其中 3 个来自手工核对的控制流反汇编恢复；无 hook 怪物的行为已由 base deck / starting equipment / preferred starting cards / level-up deck 进行牌组编码归一化，`Clone.CloneDeck`、`Mage.ManaGain` 和 `Sphinx.ForbidPlay` 也已有专门 normalized rule，未剩未解析行为操作。所有条目的 `rule_coverage.executable_rule_complete` 都显式为 false，避免把 level gate / call trace / normalized rule 误用为完整怪物 AI 或可执行升级规则。
- `monster_runtime_smoke.json` 现在提供怪物专用 runtime fixture：38/38 通过，执行全部 73 个 `behavior_runtime_rule`、247 个从 base level / `level_min` / exact health gate 派生的升级等级组合、137 个 behavior event 和 1001 个 level-up event；同时固定断言 Pixie/Skeleton/Wyvern deck mutation、Hydra/Sphinx health gate、GelatinousCube/Genie/GiantSquid 升级、Pixie/Skeleton/GiantSquid/Hydra/GelatinousCube/Vampire/Phoenix/Demon/Sphinx 行为。`monster_runtime_matrix_smoke.json` 进一步按 source method 分开执行全部 87 个 recovered behavior case，覆盖 137 个 effect event、10 个 recorded state-write event 和 3 个 Sphinx predicate fixture event。它们验证 recovered monster behavior / level-up effect label 都有共享 interpreter handler，但仍是保守回归夹具，不等同于完整原版 AI、targeting、turn timing、RNG 或 Unity UI side effects。
- `monster_control_flow_replay.json` 把怪物手工控制流和 raw state-write 缺口收窄成 focused replay：34/34 检查通过，Demon/Mage/Sphinx 三个行为分支、Hydra/Sphinx/Treant 三个升级生命值分支、7 个 state-write 怪物和 10 个 raw Monster field write 都能通过共享 runtime 产生确定性 fixture 事件；但它仍明确记录 `original_combat_or_lifecycle_replay_complete=false`，没有证明原版 AI、回合时机、typed state semantics 或 Unity lifecycle。
- `monster_effect_value_flow_index.json` 现在把 computed/receiver value-flow 队列收窄为 deterministic argument-shape map：19/19 检查通过，覆盖 26 个怪物、43 个 `effect_without_immediate_args` step 和 23 个 operation；其中 16 个是 zero-parameter receiver effect，27 个仍需要参数值恢复，argument-flow 队列包括 source-method player parameter、prior runtime read、card/deck payload、visual-stack payload、context-derived scalar 和 active-player identity；unclassified=0、matrix missing event=0，但 `original_parameter_value_replay_complete=false`、`original_receiver_identity_replay_complete=false`。
- `monster_original_accuracy_flag_index.json` 现在把 29 个 monster/runtime/AI/action pipeline artifact 中剩余的 false original-accuracy/readiness flag 规范化为独立索引：13/13 检查通过，记录 283 个唯一 false flag entry、735 次 occurrence 和 9 个工作队列，uncategorized=0。它仍是 promotion checklist，不代表这些 false flag 已完成 replay。
- `monster_runtime_accuracy_gaps.json` 明确记录 monster runtime 的剩余原版准确性队列：115/115 检查通过，并消费 `monster_original_accuracy_flag_index.json` 的 13 个 false-flag index 检查，归一化 interpreter 覆盖完成，并纳入 `monster_runtime_matrix_smoke.json` 的 18 个 source-method matrix 检查、`monster_deck_behavior_bridge.json` 的 19 个 loadout/card bridge 检查、`monster_context_dependency_index.json` 的 18 个 read/context owner queue 检查、`monster_effect_value_flow_index.json` 的 19 个 computed/receiver value-flow 检查、`monster_control_flow_replay.json` 的 34 个 focused replay 检查、`monster_rng_recovery.json` 的 14 个 RNG/list-order 静态形态检查，以及 AI candidate / target legality / turn policy / action execution 的 deterministic replay 检查；但 73/73 个怪物仍为非最终规则。当前队列包括 25 个 deck-encoded behavior 怪物、247 个 level-up threshold case / 1001 个 level-up event、3 个手工行为控制流步骤、15 个 turn timing 候选怪物、45 个 target/player context 候选、20 个 read/context 依赖（54 个 dependency step 已分入 8 个 owner 队列且 uncategorized=0）、26 个 computed-value/receiver 候选（43 个 step 已分入 7 个 argument-flow 队列且 unclassified=0）、7 个 raw state-write 怪物、2 个 RNG/shuffle 候选、2 个 UI/visual side-effect 候选、1 个 restriction predicate 怪物和 73 个 AI policy 原版 replay 缺口。AI candidate / target legality / turn policy / action execution deterministic fixture 均已通过，但 original Unity candidate、target legality、AI turn policy、action execution、read/context value-flow、parameter-value/receiver-identity 和 turn/target replay 均仍为 false；`monster_rng_recovery.json` 已恢复 GoblinMechanist 随机装备/爆炸、Revenant shuffle、Deck.Shuffle remaining-deck Fisher-Yates 和 inclusive `InGameRandomRange` 静态形态，但 original RNG stream / shuffle index stream replay 仍为 false。
- `ai_policy_recovery_gaps.json` 同时把怪物 AI blocker 和卡牌/action value 证据接起来：当前 73 个怪物全部仍在 AI policy 队列中，直接调用扫描仍为 0 命中，但虚拟槽位扫描已给出 446 条候选派发边；`card_get_ai_value_adjustment_recovery.json` 已确认 `Card.GetAIValue` 的基类场景 hook 为 identity，`card_ai_value_recovery.json` 已恢复基础 Card AI value/weight 形态，`ai_value_runtime_smoke.json` 已通过共享 runtime 执行 Card/Creature AI value 公式 fixture，`card_ai_value_matrix_smoke.json` 已把 Card.AIWeight/base GetAIValue formula fixture 扩展到全部 370 张卡，`card_ai_priority_matrix_smoke.json` 已把 ActionPlay.AIPriority 未反制/反制分支和 AIUseValue fixture 扩展到全部 370 张卡，`card_actionplay_bridge_matrix_smoke.json` 已把 normal ActionPlay candidate bridge 和 synthetic hand-enumeration fixture 扩展到全部 370 张卡，并通过 `creature_ai_weight_recovery.json` 恢复 `Creature.AIWeight` 公式，但 Creature combat-state replay、完整 AI value replay 和怪物 turn-level card choice 仍未完成。`ai_action_candidate_recovery.json` 已恢复 `GetAllActions` 候选 action 和 `GetAllActionsPlusTargets` action+target pair 的静态构造形态，`ai_action_candidate_replay.json` 已固定 5 个 deterministic candidate/action+target fixture；`ai_target_legality_recovery.json` 已恢复 `AIIsValidPlusTargets` result layout、`AIGetTargets` filter/override、discard target heuristic、target sampling 和 target-set construction 形态，`ai_target_legality_replay.json` 已固定 10 个 deterministic target-legality/target-set fixture；`target_wrapper_identity_recovery.json` 已恢复 TargetFinder metadata、target wrapper payload read 和 target-set list mutation 静态形态；`ai_rng_list_order_recovery.json` 已恢复 inclusive RandomRange、candidate tail-swap removal、target tail-swap sampling、target-set shuffle 和 PerformRandomAction RNG 静态形态；`ai_turn_policy_recovery.json` 已恢复 `AIPlayerRandom.Behave` 随机动作选择、目标随机化、action invocation packaging、pass 路径和 emergency action 派发形态，`ai_turn_policy_replay.json` 已固定 12 个 deterministic turn/pass/invocation fixture；`action_execution_recovery.json` 已恢复这些 AI action 进入 `ActionBase.Perform/Resolve`、`ActionPlay -> Card.PlayMe`、pass override 和 visual-stack dispatch 的静态形态，`action_execution_replay.json` 已把该层固定成 11 个 deterministic fixture。但 original candidate replay、original target legality replay、original target-set replay、original TargetWrapper/target-set identity replay、original AI RNG stream/list ownership replay、original AI turn replay、original action execution replay、card play replay 与 original visual-stack replay 仍为 false。已恢复的怪物行为 smoke 只能证明 effect label 可解释，不能证明原版选择手牌、目标、出牌顺序、pass/end-turn、reaction/SBE cleanup 或 emergency 行为。
- `card_play_pipeline_recovery.json` 已把怪物 AI action 最终会进入的 Card 层继续展开到 `Card.PayFor`、`Card.PlayMe`、reaction、`Player.PlaySideEffects`、card zone transition 和 `Card.GetAction`；但这仍是静态形态恢复，monster turn harness 尚未消费 card payment/card play/card zone replay。
- 代表性行为样例：`AirElemental.ApplyToPlayer` 调 `Player.AddToAttribute(att=26,x=1)` 与 `Player.AddToAttribute(att=32,x=1)`；`EarthElemental.ApplyToPlayer` 调 `Player.AddToAttribute(att=28,x=1)` 与 `Player.AddToAttribute(att=30,x=1)`，其 `StartTurn` 通过 `Game.get_them`、`Game.OtherPlayer` 后可见 `Player.BePoisoned`；`Ghost.ApplyToPlayer` 先调 `Monster.Undead(p)`，再调 `Player.AddToAttribute(att=24,x=1)`；`Brownie.ApplyToPlayer` 的 `Player.GainDodge` 已规范化为 `gain_dodge`；`GoblinMechanist.StartTurn` 可见 `Card.IsPhysical`、`GamePhysical.AddToVisualStackNoYield`、`Game.InGameRandomRange(0,4)`、`GoblinMechanist.BuildRandomEquipment` 和爆炸伤害 `Player.TakeDamage(n=5,dtype=2)`；`HandOfGlory.EndTurn` 构造 `EffectPlayerAttribute` 后调 `TickEndTurn`；`Phoenix` 的 revenant 路径已解析到 `Phoenix.PhoenixAttributes` 与 `Phoenix.ToEgg`；`Sphinx.StartTurn` 会统计手牌中 `Action`/`Spell`/`Attack` 的最高频类型，平手时会移除上一次禁用类型以实现“changing type”，再写回 Monster offset `0x118`；`Sphinx.ForbidPlay` 先排除 `Game.get_them()` 所属卡，再按 offset `0x118` 字符串和当前卡牌类型返回禁用结果。
- 例：`Kobold` 基础 deck 为 `Cower,Cower,Cower`，preferred starting card 为 `Cower`；`GelatinousCube` 基础 deck 为 `Slurp,Slurp,Slurp`，boss 为 true，`health_mult=3.0`；`AirElemental` 基础 deck 包含 `LightningStrike`、`StormSlash1`、`Shock`、`ManaSurge`，preferred starting card 为 `LightningStrike`。

未完全确认：

- 怪物表层数据、基础战斗初始化、行为入口索引、一批 `LevelUpDeck` 调用序列、行为虚调用 trace 和保守 normalized rule 已经恢复；`monster_behavior_calls` 已无未命名虚调用，也无未映射行为操作。`monster_runtime_smoke.json` 已用共享 interpreter 执行全部 73 个 recovered behavior rule 和 247 个 level-up threshold case，38/38 通过；`monster_runtime_matrix_smoke.json` 已按 source method 执行全部 87 个 recovered behavior case，18/18 通过，并把 10 个 raw state-write event 与 3 个 Sphinx predicate fixture 纳入矩阵；`monster_deck_behavior_bridge.json` 已把 25 个 deck-encoded 怪物 loadout 与 105 张引用行为卡连接到 recovered card runtime/ActionPlay bridge evidence，19/19 通过；`monster_context_dependency_index.json` 已把 20 个怪物的 54 个 read/context dependency step 分入 8 个 owner 队列且 uncategorized=0，18/18 通过；`monster_effect_value_flow_index.json` 已把 26 个怪物的 43 个 effect-without-immediate-args step 分入 7 个 argument-flow 队列且 unclassified=0，19/19 通过；`monster_control_flow_replay.json` 已把 Demon/Mage/Sphinx 行为分支、Hydra/Sphinx/Treant 升级生命值分支和 10 个 raw Monster state write 收窄为 34/34 通过的 focused replay；`monster_rng_recovery.json` 已把 GoblinMechanist 随机装备/爆炸、9 个装备候选、Revenant add-Scream-then-shuffle、Deck.Shuffle remaining-deck Fisher-Yates 和 inclusive `InGameRandomRange` 收窄为 14/14 通过的静态形态检查；`monster_original_accuracy_flag_index.json` 已把剩余 false flag 规范化为 283 个 entry / 735 次 occurrence / 9 个工作队列且 uncategorized=0；`monster_runtime_accuracy_gaps.json` 已提升到 115/115，并把 73/73 非最终怪物规则拆成 deck-encoded behavior、level-up replay、manual control flow、turn timing、target/player context、read/context owner queues、computed/receiver value flow、state writes、RNG/shuffle、UI/visual、restriction 和 AI policy 原版 replay 队列，同时接入 AI candidate、target legality、AI turn policy 和 action execution deterministic fixture 证据；`starter_monster_runtime_smoke.json` 已继续保留 Pixie/Skeleton/Wyvern starter fixture，16/16 通过；`sphinx_restriction_smoke.json` 已执行 `Sphinx.ForbidPlay` 谓词和禁用类型选择 fixture，13/13 通过。但每个升级分支的完整控制流、AI/turn hook 执行顺序、targeting、read/context value-flow、parameter-value/receiver-identity、原版 RNG stream / shuffle index stream、typed state semantics、Unity UI side effects 和特殊行为语义仍需继续恢复并写入测试。当前在 73 个怪物类的直接声明方法中未看到 `AIFlatValue`/`AIMultValue` 覆盖，AI 权重更可能分散在卡牌或基类战斗评估逻辑中。

当前 big actor 资源未匹配脚本：

- `krugs`
- `phoenixegg`
- `revenantreborn`
- `stairs`
- `treasurechest2`
- `treasurechest2dark`

这些更像特殊状态、地牢对象或事件资源，不一定对应独立 Monster 类。

### 职业

证据文件：

- `extracted/content_inventory.json` 的 `professions`
- `extracted/corpora/profession_text_corpus.tsv`
- `extracted/structured/professions.json`
- `extracted/structured/professions_initializers.json`
- `extracted/structured/professions_initializers.tsv`
- `extracted/structured/profession_behavior_index.json`
- `extracted/structured/profession_behavior_index.tsv`
- `extracted/structured/profession_behavior_calls.json`
- `extracted/structured/profession_behavior_calls.tsv`
- `extracted/structured/profession_action_loadouts.json`
- `extracted/structured/profession_action_loadouts.tsv`

已确认职业/形态：

- Assassin
- Bard
- Chaos Mage
- Dragon
- Druid human/wolf
- Monk
- Necromancer
- Paladin
- Priest
- Professor
- Ranger
- Samurai
- Thief
- Warrior
- Wizard
- Random

职业系统结构：

- `ProfessionBase` 字段：`health`、`mana`、`cards`、`actions`、`gold`、`equipSlots`、`deck`、`internalName`。
- `ProfessionBase` 关键方法覆盖职业贴图、能力文本、起始牌组、等级奖励、职业权重、战斗前牌组变换和胜利后重置逻辑，例如 `ApplyToPlayer(d)`、`AddCombatActions(d)`、`AddDungeonActions(d)`、`ClassBiases()`、`LevelUpRewards(targetLevel)`、`RewardWeight(c)`、`ResetAllAbilities(monsterLevel)`、`WinFight(monsterLevel)`、`DeckForCombat(deck)`。
- `ProfessionList` 提供 `AllProfessionList()`、`ProfessionName(x)`、`ProfessionIndex(s)`、`GetProfessionList(allowRandom)`、`ChooseRandomClass()`、`HasAccess(u,s)`。
- `ProfessionDruid` 有专属字段 `fightsSinceUpgrade`、`currentForm`，并提供 `WolfTexture()`、`HumanTexture()`、`DeckForCombat(deck)`。
- `ProfessionPaladin` 有专属字段 `fightsSinceUpgrade`，并提供 `LowestTierAttack()`、`WinFight(monsterLevel)`。
- 各职业类都覆盖 `Initialize()`、`ClassName()`、`Description()`、`AbilityDescription()`、`GetPossibleNames()` 和若干等级奖励方法；这些方法名已经导出到 `extracted/structured/key_systems.json`。
- `extract_profession_initializers.py` 已从 16 个 `Profession*.Initialize` 方法恢复出起始属性和起始牌组。`ProfessionBase` 字段偏移为：
  - `health = [this + 0x74]`
  - `mana = [this + 0x78]`
  - `cards = [this + 0x7c]`
  - `actions = [this + 0x80]`
  - `gold = [this + 0x84]`
  - `equip_slots = [this + 0x88]`
  - `deck = [this + 0x90]`
  - `internalName = [this + 0x98]`
- `professions_initializers.tsv` 覆盖 16 个职业，`internal_name`、6 个起始数值和牌组均已恢复。例：Warrior 起始为 `health=15, mana=0, cards=2, actions=1, gold=0, equip_slots=0`，牌组为 9 张 `Attack1` 加 1 张 `Sword`；Wizard 起始为 `health=15, mana=2, cards=2, actions=1`，牌组为 5 张 `Attack1`、3 张 `Mana1`、2 张 `JasrasJarringJolt`。
- `extract_profession_initializers.py` 现在也会解析各 `Profession*.ClassBiases()` 的常量 int 数组，并写入 `class_biases`。这些 bias id 会被 `CardData.GetTier` / `FrequencyMod` 用来调整卡牌 tier 或抽取频率。
- 已恢复的职业 bias 列表：Wizard `[0,14]`，Warrior `[1,17]`，Priest `[2,15]`，Thief `[3,16]`，Paladin `[4,15]`，Samurai `[5,14]`，Necromancer `[6,16]`，Ranger `[7,14]`，Assassin `[8,16]`，Monk `[8,16]`，Bard `[9,14]`，Professor `[10]`，Druid `[12,14]`，Chaos Mage `[13,14]`，Dragon `[14]`，Random `[2,15]`。
- `profession_behavior_index` 已为 16 个职业建立行为入口索引：2 个职业有 `ApplyToPlayer`，9 个有 `CombatApplyToPlayer`，16 个都有 `AddDungeonActions`，2 个有 `AddCombatActions`，15 个有等级奖励方法，4 个有 `WinFight`，1 个有 `DeckForCombat`，5 个有专属特殊方法。该索引是入口表，不等同于行为反编译。
- 职业入口分类目前覆盖 lifecycle、identity、player_application、actions、bias、level_rewards、combat_flow、special；`TieredCards` 已归入等级奖励类，当前没有未分类的职业方法。
- 示例：Assassin 同时有地牢与战斗动作入口，并覆盖 `ApplyToPlayer` / `CombatApplyToPlayer`；Druid 有 `CombatApplyToPlayer`、`WinFight`、`DeckForCombat`、`WolfTexture`、`HumanTexture`；Paladin 有 `WinFight` 和 `LowestTierAttack`。
- `profession_behavior_calls` 覆盖 43 个职业行为入口方法，其中 21 个有可提取操作、7 个有 literal 引用；89 个虚调用中 89 个已解析到方法名，未剩未命名 slot。新增解析包括 `DungeonAction.ReduceCooldown=1`、合成 `CardList.Add=1` 和第二条 `PlayerSprite.SetTexture` 分支。
- 已恢复的职业行为例子：`ProfessionAssassin.CombatApplyToPlayer` 调 `Player.ActionMana(x=2)`；各 `AddDungeonActions` 中的 `DungeonAction*` 构造对象已能解析到 `VirtualObject.set_dungeon`、具体 `DungeonAction*.Initialize` 和 `VirtualObject.ChildChange`；`ProfessionBard.WinFight` 经 `DungeonPlayer.GetActions` 遍历地城动作并调用 `DungeonAction.ReduceCooldown`；`ProfessionDruid.DeckForCombat` 用合成 `CardList.Add(card)` 记录过滤后的卡牌列表；`ProfessionDruid.WinFight` 读写 `fightsSinceUpgrade` / `currentForm`，调用 `WolfTexture()` / `HumanTexture()`，并经 `PlayerSprite.SetTexture` 与 `DungeonPlayer.ChangePortrait` 更新肖像；`ProfessionPaladin.WinFight` 读写 `fightsSinceUpgrade`，调用 `LowestTierAttack()`、`DungeonPlayer.LevelUp(c)`、`Card.NextLevel()` 和 `DungeonPlayer.PlayerSpriteText(...)`；`ProfessionProfessor.AddCombatActions` literal 引用 `CombatAbilityStudy`。
- 已恢复的职业 literal 例子：Druid `DeckForCombat` 引用 `AttackCard`、`ManaCard`、`SpellCard`；Druid 贴图引用 `ProfessionTextures/Little/ProfessionDruidWolf` / `ProfessionTextures/Little/ProfessionDruidHuman`；Dragon `SmashPage` 引用 `Dragon Smash!` 和对应文本/贴图。
- `profession_action_loadouts` 覆盖 18 个 `AddDungeonActions` / `AddCombatActions` 方法。10 个职业直接构造 dungeon action，合计 11 个构造动作；Professor 通过 literal `CombatAbilityStudy` 加入战斗能力。
- 已恢复的职业动作装载表：Chaos Mage -> `DungeonActionWildShape`；Dragon -> `DungeonActionDevour`、`DungeonActionHoard`；Monk -> `DungeonActionMeditate`；Necromancer -> `DungeonActionSwapLocation`；Priest/Random -> `DungeonActionOracle`；Ranger -> `DungeonActionInvisibility`；Thief -> `DungeonActionFindTreasure`；Warrior -> `DungeonActionSmash`；Wizard -> `DungeonActionTeleport`；Professor -> `CombatAbilityStudy`。

已恢复的职业核心能力文本包括：

- Assassin：打出 action card 时获得 mana。
- Bard：学习 songs，每首歌有若干场战斗冷却。
- Chaos Mage：周期性随机替换怪物。
- Dragon：吞噬非 boss 生物，并有 Hoard 体系。
- Druid：战斗后在人形/狼形之间切换，牌组规则不同。
- Monk：周期性忘记一张牌。
- Necromancer：在怪物位置开 portal 并换位。
- Paladin：周期性自动升级攻击牌。
- Priest：揭示一个 tile 及周围八格。
- Professor：从敌方 deck 搜牌，入手并永久加入自己 deck。
- Ranger：隐身穿过怪物直到交互。
- Samurai：击败同级或更高级敌人时完全治疗。
- Thief：发现隐藏宝藏。
- Warrior：摧毁地牢墙。
- Wizard：传送到任意位置。

### 技能、天赋、歌曲

证据文件：

- Combat abilities：`extracted/corpora/combat_abilities_corpus.tsv`
- Dungeon talents：`extracted/corpora/talent_text_corpus.tsv`
- Songs：`extracted/corpora/song_text_corpus.tsv`
- 结构化职业：`extracted/structured/professions.json`
- 结构化歌曲：`extracted/structured/songs.json`
- 结构化 combat abilities：`extracted/structured/combat_abilities.json`
- combat ability 初始化数值：`extracted/structured/combat_abilities_initializers.json`、`extracted/structured/combat_abilities_initializers.tsv`
- combat ability 行为入口：`extracted/structured/combat_ability_behavior_index.json`、`extracted/structured/combat_ability_behavior_index.tsv`
- combat ability 行为调用 trace：`extracted/structured/combat_ability_behavior_calls.json`、`extracted/structured/combat_ability_behavior_calls.tsv`
- dungeon action 行为入口：`extracted/structured/dungeon_action_behavior_index.json`、`extracted/structured/dungeon_action_behavior_index.tsv`
- dungeon action 行为调用 trace：`extracted/structured/dungeon_action_behavior_calls.json`、`extracted/structured/dungeon_action_behavior_calls.tsv`
- 职业/技能合并目录：`extracted/structured/profession_skills.json`、`extracted/structured/profession_skills.tsv`
- TalentBase 学习流程 trace：`extracted/structured/talentbase_behavior_calls.json`、`extracted/structured/talentbase_behavior_calls.tsv`
- Creature/CardBack 天赋管理 trace：`extracted/structured/creature_talent_behavior_calls.json`、`extracted/structured/creature_talent_behavior_calls.tsv`
- DungeonPlayer dungeon talent 选择/移除 trace：`extracted/structured/dungeon_player_talent_behavior_calls.json`、`extracted/structured/dungeon_player_talent_behavior_calls.tsv`
- TalentBase Unity 资源使用扫描：`extracted/structured/talentbase_asset_usage.json`、`extracted/structured/talentbase_asset_usage.tsv`
- TalentBase learning UI recovery：`extracted/structured/talent_learning_ui_recovery.json`、`extracted/structured/talent_learning_ui_recovery.tsv`
- TalentBase reachability 边界：`extracted/structured/talentbase_reachability.json`、`extracted/structured/talentbase_reachability.tsv`

已确认：

- Combat ability 资源 32 个，全部有匹配脚本。示例包括 `Adrenaline Rush`、`Avoidance`、`Burning Light`、`Distract`、`Double Strike`、`Kai Strike`、`Mana Font`、`Research`、`Vanish`、`Wild Power`。
- string literal 中可结构化出 35 个 combat ability 文本块；数量大于资源路径计数，是因为有些能力文本没有对应独立图标路径，或共用/旧版资源名。
- IL2CPP 类型模型中恢复出 37 个 `CombatAbility*` 子类，外加 `CombatAbility` 基类。子类中包含资源路径未覆盖的 `CombatAbilityChannel`、`CombatAbilityDevastate`、`CombatAbilityQuickness`、`CombatAbilityStudy` 等。
- `CombatAbility` 基类字段为 `cooldown`、`currentCooldown`、`action`；核心方法包括 `Create(s,d)`、`Initialize()`、`GetTexture()`、`Description()`、`Name()`、`IsValid()`、`MaxCooldown()`、`Use(a)`、`DoMe(p)`、`AutoPerform()`。
- `extract_combat_ability_initializers.py` 已从 37 个 `CombatAbility*.Initialize` 方法恢复出 cooldown 初始化值。`cooldown/currentCooldown` 以 packed int pair 写在 `[this + 0x74]`，`action` 写在 `[this + 0x80]`。
- combat ability cooldown 范围为 `0..5`；`Channel` 与 `LifeTap` 的 cooldown 为 0，其余常见例子包括 `Flee/Scrounge/Study=1`、`Avoidance/DoubleStrike/Surge=2`、`AdrenalineRush/Vanish=3`、`Devastate/WholeBody=4`、`DivineFavor/SpellFury=5`。
- `combat_ability_behavior_index` 已为 37 个 `CombatAbility*` 子类建立行为入口索引：37 个都有 `DoMe`，2 个有 `IsValid` / `FailText`（`Channel`、`LifeTap`），1 个有 `AutoPerform`（`Channel`），1 个有 `Strength`（`Channel`），2 个有特殊方法（`KaiUpgrade`、`ManaFontStrength`）。该索引是入口表，不等同于 `DoMe` 行为反编译。
- combat ability 入口分类目前包括 display、execution、validation、special；分类计数为 display 112、execution 38、validation 4、special 2。示例：`CombatAbilityChannel` 有 `DoMe`、`AutoPerform`、`IsValid`、`FailText` 和 `Strength`；`CombatAbilityLifeTap` 有 `DoMe`、`IsValid`、`FailText`；`CombatAbilityKaiStrike` 有 `DoMe` 与 `KaiUpgrade`；`CombatAbilityManaFont` 有 `DoMe` 与 `ManaFontStrength`。
- `profession_skills.json` 是第一版职业/技能 remaster-facing 合并目录，把 16 个职业、37 个 combat ability、33 个 dungeon action 和 43 个 dungeon talent 放在同一证据树下。它把职业静态文本、起始属性/牌组、职业 bias、职业动作装载、cooldown、显示 literal、handler effects 和调用 trace 合并为可浏览目录。
- `profession_skills.summary` 当前为 `profession_count=16`、`combat_ability_count=37`、`dungeon_action_count=33`、`dungeon_talent_count=43`；职业、combat ability 均为 `trace_available`，dungeon action 为 `trace_available=32` / `index_only=1`，dungeon talent 为 `handler_effects_available=43`。职业 `runtime_rule` 为 `structured_trace=16`，共 57 个效果步骤、6 个状态写入步骤；combat ability `runtime_rule` 为 `structured_trace=37`，共 61 个效果步骤、1 个状态写入步骤；dungeon action `runtime_rule` 为 `structured_trace=33`，共 148 个效果步骤、31 个状态写入步骤。三类 `runtime_rule` 的未解析操作数均为 0。所有条目的 `rule_coverage.executable_rule_complete` 都显式为 false，避免把 call trace 或 normalized rule 当作完整运行时规则。
- `profession_starting_profile_smoke.json` 现在提供全职业起始 profile 回归：70/70 通过，覆盖 16 个职业、157 个起始牌引用、22 个唯一起始牌、全部起始 stats、deck、class bias 和 starting actor 投影，并确认起始牌引用在 `cards.json` 中无缺失。它固定的是起始档案数据，不代表等级奖励、cooldown tick、target legality、UI 确认或 TalentBase 学习流程。
- `profession_skill_runtime_smoke.json` 现在提供职业/技能专用的 runtime fixture：65/65 通过，覆盖 Warrior/Wizard/Priest/Dragon/ChaosMage/Professor loadout、10 个 combat ability cooldown、7 个 dungeon action cooldown、16 个 scheduler fixture checks、12 个 WinFight/ready-message scheduler event、Assassin/Warrior/Professor 职业 hook、Adrenaline Rush/BoneShield/LifeTap、Alchemy/Teleport/Oracle，以及全部 43 个 dungeon talent 的 60 条 `handler_effects`。这些 fixture 使用共享 interpreter 和 `profession_skill_scheduler_recovery.json`，但仍只代表保守状态变更、基类 scheduler 形态和 action request，不代表原版完整 cooldown tick、target legality、UI 确认或 TalentBase 学习流程。
- `profession_skill_runtime_matrix_smoke.json` 现在提供职业/技能全量 source-method matrix：19/19 通过，覆盖 16 个职业、37 个 combat ability、33 个 dungeon action 的 154 个 recovered source-method case，其中 120 个包含 effect/recorded 步骤，执行得到 266 个 effect event 和 38 个 state_write / construct / lifecycle recorded event；43 个 DungeonTalent handler case 也执行出 60 个 handler event。异常数和 unsupported runtime event 数均为 0。它把代表性 smoke 扩展为全 source-method interpreter 覆盖，但仍不等同于原版 cooldown ticking、target legality、UI 确认或 TalentBase 学习流程。
- `profession_skill_scheduler_matrix.json` 现在把基类 scheduler fixture 从代表性样本扩展到全部 cooldown initializer：24/24 通过，覆盖 37 个 CombatAbility cooldown、23 个 DungeonAction cooldown、143 个 combat state case、90 个 dungeon action state case、46 个 DungeonAction.OnClick case、166 个 ready-message case，共 445 个 deterministic scheduler case；它验证的是 recovered base formula / ready-message shape，`original_scheduler_replay_complete=false`、`targeting_ui_replay_complete=false`、`prototype_runtime_ready=false`。
- `profession_skill_recorded_replay.json` 现在把职业/技能中最容易被 runtime matrix 掩盖的记录型路径单独 replay：39/39 检查通过，覆盖 `CombatAbilityStudy`、`CombatAbilityTutor`、`DungeonActionUberTeleport`、`ProfessionSamurai` 4 个 hand-recovered control-flow/lifecycle entry 的 6 个 manual step，13 个 state-write source entry 的 38 个 recorded event，以及 43 个 DungeonTalent handler case 的 60 个 handler event。它证明这些 deterministic fixture 能经共享 interpreter 执行，但 `original_cooldown_target_ui_talentbase_replay_complete=false`，所以不代表原版 cooldown scheduler、targeting/UI confirmation 或 TalentBase learning 流程。
- `profession_skill_targeting_ui_bridge.json` 现在把 targeting/UI 队列收窄为 focused bridge fixture：19/19 检查通过，覆盖 34 个 UI/selection entry、21 个 target/legality entry、37 个 targeting/UI union entry、85 个 focused source-method case、179 个 effect event 和 37 个 recorded event；`missing_matrix_case_count=0`、`exception_case_count=0`、`unsupported_case_count=0`，但 `original_targeting_ui_replay_complete=false`。
- `profession_skill_context_dependency_index.json` 现在把 read/context 队列收窄为 owner-indexed dependency map：31/31 检查通过，覆盖 67 个 dependency entry、449 个 dependency step、44 个 source method、97 个 operation 和 13 个 owner queue；uncategorized=0。它确认 dungeon_context、dungeon_runtime_state、dungeon_action_private_state、dungeon_board_tile_state、ui_physical_context、deck_card_collection_state、opponent_context、player_combat_state 等 owner 的队列边界，但 `original_value_flow_replay_complete=false`、`original_scheduler_replay_complete=false`、`original_targeting_ui_replay_complete=false`。
- `profession_skill_effect_value_flow_index.json` 现在把 no-immediate-arg effect 队列收窄为 parameter/receiver map：23/23 检查通过，覆盖 69 个 profession/skill/action entry、196 个 effect step、106 个 operation、108 个 zero-parameter receiver effect 和 88 个 parameter-value-required step；9 个 argument-flow queue 均无 unclassified，matrix missing event=0，但 `original_parameter_value_replay_complete=false` 且 `original_receiver_identity_replay_complete=false`。
- `profession_skill_original_accuracy_flag_index.json` 把职业/技能剩余 false flag 规范化为 105 个唯一 entry、2,548 次 occurrence 和 8 个 work queue，uncategorized=0；队列覆盖 cooldown/scheduler/legality、规则完成、RNG/shuffle、starting-profile/global readiness、state-write/recorded/control-flow、TalentBase、targeting/UI action-object 和 value-flow parameter/receiver identity。
- `profession_skill_runtime_accuracy_gaps.json` 明确记录职业/技能 runtime 的剩余原版准确性队列：132/132 检查通过，归一化 interpreter 覆盖完成并纳入 `profession_skill_original_accuracy_flag_index.json` 的 13 个 false-flag index 检查、`profession_skill_scheduler_matrix.json` 的 24 个检查 / 445 个 deterministic scheduler case、`profession_skill_recorded_replay.json` 的 39 个 replay 检查、`profession_skill_targeting_ui_bridge.json` 的 19 个 bridge 检查、`profession_skill_context_dependency_index.json` 的 31 个 read/context dependency 检查、`profession_skill_effect_value_flow_index.json` 的 23 个 value-flow 检查、`profession_skill_rng_recovery.json` 的 20 个静态 RNG/random-reward 形态检查、`profession_skill_scheduler_recovery.json` 的 68 个 cooldown/use/OnClick/WinFight/CoolingDownAbilities 调度恢复检查，以及 `talent_learning_ui_recovery.json` 的 53 个 TalentBase 学习 UI 静态链路检查，但 129/129 条 profession/skill/action/talent 规则仍为非最终规则；当前队列包括 60 个 cooldown initializer 条目、17 个 validation 条目、67 个 read/context 条目、69 个 parameter/receiver value-flow 条目、13 个 state-write 条目、4 个 manual control-flow 条目、3 个 RNG/shuffle runtime 条目、34 个 UI/selection 条目、21 个 target/legality 条目、2 个 random DungeonTalent handler、9 个 action-style DungeonTalent handler，以及 TalentBase 无序列化实例表/无直接构造路径的 reachability 边界和 live learning UI replay 缺口。scheduler 静态形态、targeting/UI bridge fixture、read/context owner index、effect value-flow owner index、全 initializer fixture 覆盖和 TalentBase 静态 UI/pay-and-learn 链路已恢复，但 `combat_ability_scheduler_replay_complete=false`、`dungeon_action_scheduler_replay_complete=false`、`original_targeting_ui_replay_complete=false`、`original_value_flow_replay_complete=false`、parameter-value/receiver-identity replay=false、`original_talent_learning_ui_replay_complete=false`；`profession_skill_rng_recovery.json` 已把 DesperatePrayer、FindMonster、FindTreasure、Charismatic 和 Hoard 收窄到静态证据，但原版 RNG stream 与 shuffle/index stream replay 仍为 false。
- `profession_skill_scheduler_recovery.json` 把职业/技能共享调度层从反汇编和 xref 扫描落成 68/68 个检查：`CombatAbility` 和 `DungeonAction` 的 `cooldown=0x74`、`currentCooldown=0x78` 已固定；两者 `ReduceCooldown` 都是 `currentCooldown - 1` 非负才写回，`ZeroCooldown` 写 0，`IsValid` 判断 `currentCooldown == 0`。`CombatAbility.MaxCooldown` 直接返回 cooldown，`Use` 写 `currentCooldown = MaxCooldown()`，若随后 `IsValid()` 为 false 且传入 `ActionBase`，写 action offset `0xe0`；`ActionCombatAbilityBasic.PerformAction` 读取 offset `0xd8` 的 combat ability 并通过 slot 79 调 `DoMe`。`DungeonAction.OnClick` 在 `ShouldPerform()` 为 true 时写 `currentCooldown = MaxCooldown()` 后派发 `Perform()`，false 分支走 `Not Here...` physical feedback；`DungeonAction.MaxCooldown` 在 positive cooldown 下读取 player 并用 `HasTalent("ClearMind")` 扣 1。`Dungeon.WinFight(level)` 外层顺序已恢复为 `EndFight()` -> `DungeonPlayer.WinFight(level)` -> `SaveDungeon()` -> `DungeonPlayer.GainExp(0)` -> `CheckEnqueuedWindows()` -> `CheckDevour()` tailcall，且 slot 151 xref 确认 `Dungeon -> DungeonPlayer.WinFight` 调用点。`DungeonPlayer.GetActions` 返回 `actions` offset `0xd0`，`GetCombatActions` 返回 `combatActions` offset `0xe8`；`DungeonPlayer.WinFight(monsterLevel)` 先遍历 dungeon actions 调 `DungeonAction.ReduceCooldown(slot 67)`，再遍历 combat abilities 调 `CombatAbility.ReduceCooldown(slot 71)`，随后调 `ProfessionBase.WinFight(monsterLevel)`，最后检查 `Crusader` 并可能 `FullHeal(slot 81)`。`DungeonPlayer.CoolingDownAbilities` 已确认枚举 action list、读取 offset `0x78`，在 `currentCooldown == 1` 或 `ProfessionBase.WillReset(action, monsterLevel)` 为真时用 `ButtonName() + " is ready!"` 生成文案；`GamePhysical.EndGame()` 创建 `method_index=21013` coroutine，该 coroutine 经 `GamePhysical.game -> Game.currentDungeon -> Dungeon.player` 通过 slot 152 调 `DungeonPlayer.CoolingDownAbilities(level)`，并将返回字符串存到 `$abilityString` offset `0x30`；jump table 解析确认 coroutine state 3 resume 到 `0x1002fb680` 的 `Dungeon.WinFight(level)` block，因此 ready-message 生成发生在 WinFight cooldown mutation pass 之前；同 coroutine 的另一处 slot 152 已判定为 `Game.get_them()` 返回玩家上的 `Player.get_health()`，`Dungeon.EndFight` 中 slot 152 也被证明不是 `Dungeon.player` receiver。alternate coroutine branch、UI/target replay 仍未完成。
- `combat_ability_behavior_calls` 进一步覆盖 44 个能力行为方法，其中 41 个有可提取操作；101 个虚调用已全部解析到方法名。最常见的调用包括 `Game.OtherPlayer` 11 次、`VirtualObject.get_dungeon` 9 次、`Player.get_deck` 7 次、`Player.Draw` 6 次、`Deck.DrawExNihil` 4 次、`Player.CreateEquipment` 4 次、`Player.GainActions` 4 次、`Player.DealDamage` 3 次、`Player.GainMana` 3 次。
- 已恢复的能力行为例子：`AdrenalineRush.DoMe` 调 `Player.Draw(i=3)`；`Avoidance.DoMe` 调 `Player.Invulnerable()`；`BoneShield.DoMe` 调 `Player.GainShield(x=10)`；`BurningLight.DoMe` 调 `Player.DealDamage(n=10,dtype=2)`；`LifeTap.DoMe` 调 `Player.LoseHealth(x=5)` 后 `Player.Draw()`；`KaiStrike.DoMe` 读取 mana，调用 `Player.DealDamage(dtype=6)`，再 `Player.SetMana(x=0)`；`KaiStrike.KaiUpgrade` 把能力 cooldown 字段写为 2。
- 新增解析的能力行为包括：`ClingingLight` / `Rend` / `TotalFocus` 构造 `EffectPlayerAttribute` 后调用 `EffectPlayerAttribute.TickEndTurn()`；`DoubleEquip` 中 `Player.CreateEquipment(...)` 的返回值被确认为 `Card` 并调用 `Card.Temporary()`；`DesperatePrayer` 的滚动战斗提示通过 `Game.physical -> GamePhysical.AddToVisualStackNoYield(...)` 调度。
- `Channel.DoMe` 会 `Player.GainActions(n=-1)`，调用 `CombatAbilityChannel.Strength()`，再把结果用于 `Player.GainMana(...)`；`Channel.IsValid` 和 `LifeTap.IsValid` 都通过 `VirtualObject.get_dungeon -> Dungeon.currentCombat -> Game.get_me` 读取当前玩家，并分别检查 `Player.get_gameActionsLeft` / `Player.get_health` 与 `currentCooldown`。`FailText` literal 分别是 `No actions left` 和 `Not enough health`。
- Bard songs 有 20 个文本入口，例如 Bellicose Ballad、Soothing Serenade、Knavish Nocturne、Chant of Clarity。
- Dungeon talent 文本中包含 passive 与 dungeon ability，例如 Archmage、Resourceful、Enlightenment、Alchemist、Impervious、Piercing、Polymorph、Teleport、Smash、Heal、Portent 等。
- `DungeonAction` 基类字段为 `cooldown`、`currentCooldown`、`button`；核心方法包括 `Initialize()`、`ReduceCooldown()`、`ZeroCooldown()`、`IsValid()`、`OnClick()`、`ShouldPerform()`、`Perform()`、`TileClick(t)`、`CardClick(c)`、`Savable()`。
- IL2CPP 类型模型中恢复出 33 个 `DungeonAction*` 子类，外加基类。子类覆盖 `Alchemy`、`Devour`、`FindMonster`、`FindTreasure`、`Heal`、`Hoard`、`Invisibility`、`Meditate`、`Murder`、`Oracle`、`Polymorph`、`Portent`、`Sacrifice`、`Sing`、`Smash`、`SwapLocation`、`Teleport`、`Upgrade`、`WildShape` 等地城动作。
- `dungeon_action_behavior_index` 已为 33 个 `DungeonAction*` 子类建立行为入口索引：23 个动作从 `Initialize` 中解出 cooldown，32 个有 `Perform`，3 个有 `IsValid`，9 个有 `IsLegalTarget`，2 个有 `CardClick`，1 个有 `TileClick`，3 个有存档序列化入口。示例 cooldown：`Alchemy=1`、`Smash=2`、`Heal=5`、`Teleport=1`、`Upgrade=3`。
- dungeon action 入口分类目前覆盖 lifecycle、display、cooldown、validation、execution、special；分类计数为 lifecycle 72、display 44、validation 31、execution 56、special 6，当前没有未分类方法。
- `dungeon_action_behavior_calls` 覆盖 138 个动作入口/显示/配置方法，其中 91 个有可提取操作、55 个有 literal 引用；327 个虚调用已全部解析到方法名。常见调用包括 `VirtualObject.get_dungeon=107`、`Dungeon.player` 字段读取 43 次、`Dungeon.board` 字段读取 13 次、`Tile.Refresh=9`、`DungeonPlayer.AddCard=5`、`DungeonPlayer.LevelUp=4`、`DungeonPlayer.RemoveCardFromDeck=4`、`Dungeon.currentAction` 字段写入 7 次。
- 已恢复的 dungeon action 行为例子：`DungeonActionAlchemy.Perform` 引用 `HealingPotion`，经 `Dungeon.player` 调 `DungeonPlayer.AddCard(s)`；`DungeonActionDeckTargetted.OnClick` 设置 `Dungeon.currentAction`，调用 `AllCards`/`ShouldCull`/`TitleString`/`DeckButtons`，最后打开 `Dungeon.DeckViewer`；`DungeonActionDevour.Perform` 调 `Tile.DestroyNormalMonster`、`Tile.Refresh`、`Monster.ExperienceValue()`、`Monster.Devour()`，并经 `Dungeon.player` 触发 `DungeonPlayer.ProcessKill`，literal 包含 `+` / ` EXP`；`DungeonActionMurder.Perform` 也恢复到 `Monster.ExperienceValue()`；`DungeonActionTeleport.Perform` 目标是 `Tile.TeleportHere`；`DungeonActionUpgrade.Perform` 经 `Dungeon.player` 调 `DungeonPlayer.LevelUp(c)`；`DungeonActionPortent` 的显示方法包含 dungeon/final-boss 文案。
- 新增解析的 dungeon action 行为包括：`DungeonActionHoard.HoardPerform` 构造 `LevelUpReward` 后调用 `LevelUpReward.GetShopDialogueObject()`，再经 `ShopDialogueObject.Center()` / `LowerLeft()` / `UpperCenterTo()` 组装选择界面；`DungeonActionDeckTargetted.CardClick` 通过 `Card.physical -> CardPhysical.CIN -> CardInteractionNormal.SelectWithMovement()` 进入卡牌选择；`DungeonActionTalentDeckTargetted` 的 `chosenCards` 已标为合成 `CardCollection`，可恢复 `Count`、`Contains`、`Add`、`Clear`、`Remove`、`get_Item`，并联动解析 `Card.UnHighlight()`、`CardPhysical.UnHighlightNow()` 和 `CardPhysical.DestroyButtonAbove()`；其 `BuildFromTalent` 现在恢复 `talent=0xc0`、`requiredCards=0xb4`、`maxCards=0xb8`、`multiCards=0xbc`：`Upgrade1` 设置 upgradable 过滤并执行 `DungeonPlayer.LevelUp(selected_card)`，`Purity` 最多选 3 张并执行 `DungeonStats.DeleteCard` + `DungeonPlayer.RemoveCardFromDeck`，`CopyTalent` 执行 `DungeonPlayer.AddCard(selected_card.internalName)`；`FindMonster.Perform` / `OracleOld.NearestMonsters` 现在能识别合成 `TileList` 的 `Count`、`Add`、`get_Item`；`DungeonActionOracle` / `OracleOld` 恢复 `Tile.MakeVisible()`、`Tile.WipeInvisibility()`；`DungeonActionPreparation.Perform` 恢复 `DungeonAction.ZeroCooldown()`；`DungeonActionSing.Perform` 恢复 `SongBase.PlaySong(player)`；`Polymorph` / `WildShape` 已恢复 `GamePhysical.VirtualWait()` 与 `DungeonBoard.CreatePolymorphAtLevel(t,m)`，同一 `blr` 的另一条分支通过 `branch_alternative_calls` 记录为 `DungeonBoard.CreateFeature(t,"FinalBoss")`。
- 已恢复的动作显示 literal 例子：`Alchemy`、`Devour`、`Dark Pact (`、`Choose a card to copy`、`Copy`、`Learn`、`Upgrade`、`Interrupted!`。这些来自 display 方法和执行方法的 string literal 引用，用于后续重建动作按钮、确认框和地牢说明文案。
- `DungeonTalent` 数据模型字段为 `internalName`、`name`、`requirements`、`effect`、`tier`、`repeatable`；构造函数参数为 `iname`、`n`、`effect`、`tier`、`repeatable`、`requirements`。
- `DungeonTalentList.GetDungeonTalentList` 已反汇编到 `extracted/disassembly/DungeonTalentList_GetDungeonTalentList.txt`，并由 `extract_dungeon_talents.py` 恢复 43 个 dungeon talent 条目：
  - `extracted/structured/dungeon_talents.json`
  - `extracted/structured/dungeon_talents.tsv`
- `dungeon_talents` 字段覆盖 `internal_name`、`display_name`、`effect`、`tier`、`repeatable`、`requirements`、构造调用地址和 `handler_effects`；43 条均有完整名称、效果文本和 tier。`summary` 现在也机器可读地导出 tier 分布、repeatable 清单、requirement 命名覆盖、handler 覆盖、handler effect 覆盖和 runtime scope 分布。
- tier 分布为 tier 1 共 11 条、tier 2 共 11 条、tier 3 共 11 条、tier 4 共 10 条。repeatable 为 true 的有 8 条：`Health1`、`Mana1`、`Upgrade1`、`Rich`、`Action1`、`EquipSlot`、`CopyTalent`、`LevelUp`。
- `dungeon_talents` 现在也通过 `UserAttribute` / `achievements` join 输出 `requirement_names`。31 个 prerequisite 引用全部已命名，未命名引用为 0。例如 `Teleport -> WINWIZARD`、`Smash -> WINWARRIOR`、`HealAbility -> WINPRIEST`、`Preparation -> WINTHIEF`、`Polymorph -> DEATH4`、`Tiltowait -> RICH`、`Archmage -> CLARITY`。
- `dungeon_talents` 已交叉标出 `DungeonTalentHandler` 运行时引用：43 条全部能在 handler 中匹配到 internal name。30 条只经 `ApplyTo` 处理，归为 dungeon/immediate 效果；13 条同时出现在 `ApplyToPlayer`，归为 player combat/passive 效果，例如 `Terror`、`Equipped`、`Vampiric`、`Poisonous`、`Archmage`、`Impervious`、`Cruel`、`Fluid`、`Desperate`、`Fiery`、`Invisible`、`Piercing`、`Hoard`。
- `handler_effects` 进一步把 43 条 talent 全部映射到 60 条运行效果：46 条来自 `ApplyTo`，14 条来自 `ApplyToPlayer`。已恢复的目标包括 `DungeonPlayer.GainMaxHealth/GainMana/GainActions/GainCards/GainGold/GainEquipSlot/LevelUp/UpgradeAllMax/AddCard/AddAction/AddPower/AddCombatAbility`，`DungeonBoard.GenerateRandomReward/DestroyAllWalls/RevealMonsters`，`GamePhysical.AddToVisualStackNoYield`，以及 `Player.SetAttribute/CreateEquipment/Fireballer/GainElusive/ElementalForm/ElementalFormBase/ElementalResist`。
- 代表性 talent 运行效果：`Ascendance` 依次调用 `GainMaxHealth(20)`、`GainMana(5)`、`GainActions(1)`；`Charismatic` 经 `DungeonBoard.GenerateRandomReward(5)` 生成 5 个额外奖励；`Hammer` 调 `DungeonBoard.DestroyAllWalls()`；`FindMonster` 调 `DungeonBoard.RevealMonsters()`；`Upgrade1` 经 `DungeonActionTalentDeckTargetted.BuildFromTalent/Perform` 选择可升级卡并调用 `DungeonPlayer.LevelUp(selected_card)`；`Purity` 最多选择 3 张卡，逐张调用 `DungeonStats.DeleteCard(card.internalName)` 和 `DungeonPlayer.RemoveCardFromDeck(card.internalName)`；`CopyTalent` 调 `DungeonPlayer.AddCard(selected_card.internalName)`；`Terror` 在战斗中把对手 hand size 字段减 1；`Equipped` 在战斗中 `CreateEquipment("Sword")`、`CreateEquipment("Shield")`、`CreateEquipment("Armor")`；`Hoard` 调 `CardFinder.RandomEquipment()` 后用结果创建装备；`Archmage` 增加 `archmage` player attribute，并通过 `Game.physical -> GamePhysical.AddToVisualStackNoYield(Dungeon.currentDungeon.physical, [], "RefreshManaCosts")` 触发 `DungeonPhysical.RefreshManaCosts`。
- 示例：`Card1/Smart` 为 tier 4，效果是每回合多抽一张牌；`Archmage` 为 tier 4，效果是 spells 不再消耗 mana；`Polymorph` 为 tier 2，解锁 cooldown 5 的 Polymorph dungeon ability；`Portent` 为 tier 1，解锁无 cooldown 的 Portent dungeon ability。
- 普通卡牌结构化数据没有 `Talent` / `Skill` 候选：`card_data.json` 的 234 张卡只分为 `ActionCard=104`、`AttackCard=58`、`ManaCard=22`、`SpellCard=50`，且 `card_data` / `cards_partial` / `card_effect_index` 针对 `Talent`、`Skill`、`Talented`、`Adventurous` 的检索无命中。因此 `TalentBase` 是独立于普通卡牌列表的卡背/生物天赋系统。
- `TalentBase` 字段为 `_card`、`rank`、`tier`、`name`、`text`、`immediate`、`cost`、`_prereq`、`physical`，并提供 `.ctor(card)`、`CreateMe()`、`CanLearn(bonusLevels)`、`PayFor()`、`Learn()` 等学习流程方法。
- `talentbase_behavior_calls` 覆盖 16 个 `TalentBase` 相关入口，34/34 个虚调用已全部命名。已恢复字段偏移为 `_card_id=0x74`、`rank=0x78`、`tier=0x7c`、`name=0x80`、`text=0x88`、`immediate=0x90`、`cost=0x94`、`_prereq_id=0x98`、`physical=0xa0`；`_card` 和 `_prereq` 实际以对象 id 存储，并由 `Game.GetObject(id)` 还原为 `Card` / `TalentBase`。
- `TalentBase` 构造和复制路径已恢复：`.ctor(card)` 复制 `card.game`、调用 `set_card(card)`、把 `rank=0` 且 `immediate=false` 后进入 `Initialize()`；`CopyInto` 会复制 `_card_id`、`rank/tier`、`name`、`text`、`immediate/cost` 和 `_prereq_id`；`IsKnown()` 是 `rank > 0`。
- `TalentBase.Excluded()` 在 `rank > 0` 时返回 false；否则若缺少 prereq/card 或 prereq 已 excluded 则排除；通过 prereq 后，会调用关联 `Card`/`Creature.NumTalentRanksOfTier(tier)`，当同 tier 已有已知 rank 时排除。这说明同一 tier 在该学习路径下最多允许一个已学习 rank。
- `TalentBase.CanLearn(bonusLevels)` 的主判定链为：`rank == 0`，`get_card()` 非空，`Card.get_player()` 非空，`Card.game.CurrentAction == null`，若有 `get_prereq()` 则 `prereq.IsKnown()` 为 true，`Excluded()` 为 false，最后调用 `Player.CanAfford(cost)`。无参 `CanLearn()` 只是以 `bonusLevels=0` 尾调该方法。当前反汇编中未看到 `bonusLevels` 参与最终 cost 计算。
- `TalentBase.PayFor()` 走 `get_card() -> Card.get_player() -> Player.PayFor(cost)`；`Learn()` 先令 `rank += 1`，若 `immediate` 为 false，则把 `tier` 加到关联 `Card.bonusAIValue`，最后调用 `VirtualObject.SetCopyDirty()`。`Initialize()` 在有 card 时调用 `VirtualObject.ListenTo(card)`。
- `talentbase_asset_usage` 确认 Unity 资源侧只有 1 个 `TalentBase` `MonoScript`，没有任何 `TalentBase` `MonoBehaviour` 实例；14 个名为 `Talent` 的 GameObject/prefab 实际挂载的是 `TalentPhysical`、`KeywordManager` 和 `VisualChildHandler`，不是 `TalentBase`。这与 `talentbase_ctor_call_xrefs` 中 `TalentBase..ctor` 直接调用数为 0 一致：当前包内没有可直接提取的序列化 `TalentBase` 表。
- `talent_learning_ui_recovery.json` 把 LevelUpReward -> DungeonPlayer -> SDB -> ShopDialogueTalentViewer 的 talent-viewer 静态链路、CardBack/CardPhysical 的 TalentPhysical 绑定，以及 `TalentBase.PayFor/Learn` 的付费/学习 side-effect 形态结构化为 53/53 通过；它仍明确保留 `original_talent_learning_ui_replay_complete=false` 和 `prototype_runtime_ready=false`，因为 live UI click/callback、对象 identity、payment UI 和 `TalentBase.Learn` side effect 还没有原版 replay。
- `talentbase_reachability.json` 把该证据边界结构化为 23/23 通过：DungeonTalent 共 43 条、handler effect 共 60 条，DungeonPlayer 选择/handler 路径已结构化；`TalentBase` 16 个行为方法、34/34 个虚调用，`Creature/CardBack` 12 个方法、17/17 个虚调用，DungeonPlayer 12 个方法、32/32 个虚调用均已命名；Talent learning UI 静态链路已恢复，但 `TalentBase` MonoBehaviour 实例为 0，直接 ctor xref 为 0，直接 talent gain xref 为 0，仅有 1 个 `Creature.GainTalent` 风格虚槽 xref。因此当前结论是 `runtime_learning_reachability_status=unverified_dynamic_or_legacy_path`，不是“已证明不可达”。
- `creature_talent_behavior_calls` 覆盖 12 个 `Creature` / `CardBack` 天赋管理入口，17/17 个虚调用已全部命名。`Creature._talents=0x190` 是天赋列表；`get_talents()` 懒初始化列表，并经 `Game.GetObject` / `TalentList.Add` 恢复对象引用。
- `Creature.UnspentTalents()` 调 `GetAttribute(att=21)`；`SpendTalent()` 调 `UnspentTalents()` 后用 `SetAttribute(att=21, val=UnspentTalents()-1)` 消耗一点；`UnTalent(talent)` 先把 `TalentBase.rank` 减 1，再用 `SetAttribute(att=21, val=UnspentTalents()+1)` 返还一点。
- `Creature.NumTalentOfTier(tier)` 遍历 `get_talents()` 后按 `TalentBase.tier` 计数；`Creature.NumTalentRanksOfTier(tier)` 对同 tier 的 `TalentBase.rank` 求和。`Creature.GainTalent(talent)` 会检查物理状态和 `Talented()`，懒初始化 `_talents`，再把传入 talent 经复制/绑定路径加入 `TalentList.Add`。
- `CardBack.GainTalent(talent)` 用 `talent.tier` 和 `Creature.NumTalentOfTier(tier)` 选择 `TalentPhysical` 槽位：tier 1 使用 `count`，tier 2 使用 `count+1`，其他 tier 使用 `count+3`；随后写回 `TalentBase.physical`、`TalentPhysical.talent` 并调用 `TalentPhysical.Initialize()`。
- `dungeon_player_talent_behavior_calls` 覆盖 12 个 `DungeonPlayer` / `DungeonUser` 相关入口，32/32 个虚调用已全部命名。直接调用包括 `SDB.TalentViewer`、`DungeonTalentHandler.ApplyTo`、`DungeonTalentHandler.GetByName` 和 `GameManager.LoadCurrentUser`。
- `DungeonPlayer.dungeonTalents=0xe0` 保存已选择 dungeon talent 的 `internalName` 字符串列表。`ChooseTalent(t)` 会读取 `DungeonTalent.internalName`，调用 `StringList.Add(internalName)`，随后直接调用 `DungeonTalentHandler.ApplyTo(t, player)`；若选择前后的 `Dungeon.activeShop` 没有改变，则调用 `Dungeon.WindowFinished()` 结束窗口。
- `DungeonPlayer.ShouldConsider(t, depth)` / `ShouldConsider_depth` 的选择门槛已恢复：非 repeatable talent 若 `dungeonTalents.Contains(internalName)` 则拒绝；`DungeonTalent.tier` 必须不大于 `depth`；用户解锁列表经 `GameManager.LoadCurrentUser().AllTalents()` 查询，但本 build 的 `DungeonUser.AllTalents()` 是 `mov w0,#0; ret`，因此该分支实际等价于没有额外用户 talent 列表；若 `DungeonTalent.requirements` 非空，则每个 requirement 都必须满足 `DungeonUser.GetAttribute(req) >= 1`。
- `DungeonPlayer.HasTalent(name)` 直接委托 `dungeonTalents.Contains(name)`；`RemoveTalent(name)` 调 `StringList.Remove(name)` 后在有 physical 时刷新 `DungeonPlayerPhysical.GeneratePlayerPortrait()`；`RemoveLastTalent()` 以 `StringList.Count()-1` 调 `RemoveAt`；`TalentsToString()` 遍历 internal name，经 `DungeonTalentHandler.GetByName(name)` 取显示对象并用 `", "` 拼接成展示文本。
- `DungeonUser.AllTalents()` 在当前二进制中已确认是返回 0/null 的 stub，所以可作为 remaster 数据源的具体解锁数据仍应以 43 条 `DungeonTalentList`、它们的 `requirements` 和 `DungeonTalentHandler` 运行效果为准。

未完全确认：

- `Profession` / `CombatAbility` / `DungeonAction` 的调用 trace 已进一步归一为保守 `runtime_rule`，`profession_starting_profile_smoke.json` 覆盖全部职业起始 stats/deck/bias/card reference，`profession_skill_runtime_smoke.json` 覆盖一批代表性执行 fixture，`profession_skill_runtime_matrix_smoke.json` 覆盖全部 recovered source-method matrix，`profession_skill_scheduler_matrix.json` 已把全部 60 个 cooldown initializer 收窄为 24/24 通过、445 个 deterministic scheduler case，`profession_skill_recorded_replay.json` 已把 4 个 hand-recovered control-flow/lifecycle entry、38 个 state-write recorded event 和 43 个 DungeonTalent handler case 收窄为 39/39 通过的 deterministic replay，`profession_skill_targeting_ui_bridge.json` 已把 37 个 targeting/UI entry、85 个 focused source-method case 收窄为 19/19 通过的 deterministic bridge，`profession_skill_context_dependency_index.json` 已把 67 个 read/context entry、449 个 dependency step 收窄为 31/31 通过的 owner-indexed dependency map，`profession_skill_effect_value_flow_index.json` 已把 69 个 no-immediate-arg effect entry、196 个 effect step 收窄为 23/23 通过的 parameter/receiver value-flow map，`profession_skill_original_accuracy_flag_index.json` 已把剩余 false flag 收敛为 105 个 entry / 2,548 次 occurrence / 8 个 work queue 且 uncategorized=0，`profession_skill_runtime_accuracy_gaps.json` 已把 129/129 条非最终 profession/skill/action/talent 规则拆成 132/132 通过的 cooldown/legality/scheduler replay、targeting/UI、read/context dependency、parameter-value/receiver-identity、state-write/recorded-event、manual-control、RNG/shuffle、DungeonTalent、TalentBase learning UI 和 starting-profile integration 队列；`profession_skill_rng_recovery.json` 20/20 通过，恢复 DesperatePrayer、FindMonster、FindTreasure、Charismatic 和 Hoard 的静态 RNG/random-reward 形态，但原版 RNG stream 与 shuffle/index stream replay 仍为 false；`profession_skill_scheduler_recovery.json` 进一步恢复共享 cooldown/use/OnClick/ClearMind、`Dungeon.WinFight` 外层顺序、`Dungeon -> DungeonPlayer.WinFight` 调用点、WinFight cooldown tick 顺序、ready-message 条件、`GamePhysical.EndGame` coroutine 中的 `DungeonPlayer.CoolingDownAbilities(level)` 调用点、ready-message-before-WinFight yield ordering 和 `Dungeon.EndFight` slot-152 非 player receiver 边界，并已被主 accuracy gaps artifact 纳入检查；`talent_learning_ui_recovery.json` 53/53 通过，恢复 LevelUpReward/DungeonPlayer/SDB/ShopDialogueTalentViewer/CardBack/TalentBase 的静态 viewer/pay-and-learn 链路。但 alternate coroutine branch replay、target/UI action-object replay、read/context value-flow、parameter-value/receiver-identity replay、original cooldown scheduler、live TalentBase UI/callback/payment replay 和 TalentBase learning replay 仍不是完整原版语义。`DungeonTalentList` 的 43 条显式 talent 已转成结构化数据，并已和 handler 引用、handler effect、DungeonPlayer 选择/移除流程交叉验证；`requirements` 已命名为 `UserAttribute`，handler effect 也能在共享 interpreter 中保守执行/记录。`TalentBase` 的通用对象管理、学习/付费链、tier 排除和卡背 physical 绑定已恢复，`talentbase_reachability.json` 现在 35/35 通过；`talent_learning_source_call_xrefs.json` 扫描 24,879 个 method pointer 后只发现 2 条 `DungeonPlayer -> SDB.TalentViewer` 直接调用，在扫描的 TalentBase/gain/TalentPhysical/UI endpoint 中没有非 UI 直接命中；IL2CPP type model 中 `TalentBase` 只有 1 个 type，且 direct child type count 为 0。无序列化实例表、无直接构造/学习/付费/gain source call 和仅 1 个虚槽 gain xref 仍是当前边界；动态构造、旧版本遗留路径、存档状态或 indirect path 仍未被具体定位。仍需确认的是这条系统在实际游戏流程中是否可达，以及卡背 UI 布局的非关键细节。

### 地牢生成逻辑

证据文件：

- `extracted/content_inventory.json` 的 `logic_identifiers.dungeon_generation`
- `extracted/corpora/dungeon_generation_identifiers.txt`
- `extracted/structured/dungeon_generation.json`
- `extracted/structured/dungeon_generation.md`
- IL2CPP metadata identifiers

已确认的关键类/方法/字段：

- `DungeonBoard`
- `DungeonBoardPhysical`
- `DungeonBoard` 字段：`WIDTH`、`HEIGHT`、`MAX_MONSTERS`、`MIN_MONSTERS`、`board`、`allTiles`、`physical`、`playerSprite`、`depth`、`environment`、`done`、`bossName`
- 重要偏移已在反汇编中确认：`board = [this + 0x78]`、`allTiles = [this + 0x80]`、`depth = [this + 0x98]`、`environment = [this + 0x9c]`、`bossName = [this + 0xa8]`、`Dungeon.difficulty = [dungeon + 0x11c]`、`Tile.x = [tile + 0x74]`、`Tile.y = [tile + 0x78]`、`Tile.board = [tile + 0x80]`、`Tile.impassable = [tile + 0x90]`。
- `MonsterLevels`
- `GenerateMonsterLevels`
- `AddRewards`
- `AddMonsters`
- `GetThreatComponents`
- `TilePlusInt`
- `ThreatComponent`
- `ShuffleTiles`
- `RandomElement`
- `floor1seed`、`floor2seed`、`floor3seed`
- `floorBoss`、`floorEnvironments`
- `bossesSeen`、`bossAttributes`
- `openSquares`

`DungeonBoard` 已恢复出的生成/布局相关方法包括：

- 地图状态与连通性：`Initialize()`、`FindDeletableSquares()`、`CanDelete(t)`、`IsConnected()`、`GetComponent(t)`、`AllTiles()`、`DarkSquares()`、`TileAt(i,j)`、`InDungeon(x,y)`、`GetNeighbors(...)`、`GetDiagonals(...)`、`DistanceBetween(a,b)`。
- 楼层构建：`BuildLastFloor()`、`PopulateDungeon()`、`DestroyAllWalls()`、`BuildMaze()`、`PathBetweenTryNumber(a,b,tryNumber)`、`PathBetween(a,b)`、`FindCutVertexInfos()`、`FindBestCutVertex()`。
- 怪物生成：`MonsterLevels()`、`GenerateMonsterLevels(number)`、`GetMonsterLevel(t)`、`CreateMonster(t)`、`CreateMonsterAtLevel(t,x)`、`CreateNamedMonsterAtLevel(t,x,s)`、`CreateBoss(t)`、`AddMonsters()`、`AllMonsters()`、`RevealMonsters()`。
- 奖励/功能点生成：`TotalGoldTarget()`、`GoldTarget()`、`GoldByTier(tier)`、`MinRewardTier()`、`MaxRewardTier()`、`RewardTiers()`、`RewardTier()`、`RandomGoldByTier(x)`、`RandomGoldTarget()`、`GenerateReward(t,goldTarget)`、`GenerateRewards()`、`GenerateShop(t,goldTarget)`、`GenerateChest(t,goldTarget)`、`GenerateMonsterChest(t,goldTarget)`、`GenerateRandomReward(...)`、`GenerateChests(x)`、`AddRewards()`、`AddWeirdFeature()`、`CreateFeature(t,featureType)`、`AddTaverns()`。
- 威胁与陷阱：`ThreatSort(a,b)`、`GetThreatComponents()`、`GetStartDistances()`、`GetToPlayerThreats()`、`AddTraps()`。

方法体级证据：

- `extract_dungeon_generation.py` 已将目前可机械复现的地牢生成规则导出为：
  - `extracted/structured/dungeon_generation.json`
  - `extracted/structured/dungeon_generation.md`
- `DungeonBoard.AddRewards` 已反汇编并标注 8 个特殊奖励/功能点 literal：`AltarToJeremiad`、`AltarToAlcoran`、`AltarToCairn`、`AltarToGauss`、`AltarToLiara`、`AltarToAston`、`MushroomPatch`、`Brainsucker`。导出文件：
  - `extracted/structured/dungeon_reward_literals_from_add_rewards.json`
  - `extracted/structured/dungeon_reward_literals_from_add_rewards.tsv`
- `Game.RandomRange(a,b)` 已确认是闭区间整数随机：方法体在调用 `System.Random.Next` 前会把 `b` 加 1。因此 `RandomRange(9,11)` 是 `9..11`。
- `DungeonBoard.Initialize`、`GetDimensions`、`TileAt`、`InDungeon`、`AllTiles`、`BaseTileSize` 已结构化到 `dungeon_generation.json` 的 `board_layout`：depth 1 创建 `8x8` 的 `Tile[,]`，其他普通楼层创建 `10x10`；`InDungeon(x,y)` 要求坐标非负且小于 `board.GetLength(0/1)`；`TileAt(i,j)` 在越界时返回 null，否则返回 `board[i,j]`；`BaseTileSize()` 返回约 `(0.48, 0.48, 0.01)`；`ScaleFactor()` 是 `(20 / width, 20 / height)`。
- `DungeonBoard.AddMonsters` 已反汇编到 `extracted/disassembly/DungeonBoard_AddMonsters.txt`。它会先计算怪物放置目标：`depth == 1` 时使用固定值 `6`，其他楼层使用 `Game.RandomRange(9,11)`。
- `DungeonBoard.MonsterLevels` 已反汇编到 `extracted/disassembly/DungeonBoard_MonsterLevels.txt`。该方法按 `depth` 返回 3 个基础怪物等级：
  - `depth == 1`：`[1, 2, 3]`
  - `depth == 2`：`[4, 5, 6]`
  - `depth == 3`：`[7, 8, 9]`
- `DungeonBoard.GenerateMonsterLevels(number)` 已反汇编到 `extracted/disassembly/DungeonBoard_GenerateMonsterLevels.txt`。可读逻辑是先取 `MonsterLevels()`，再为每个基础等级生成 `RandomFloat()+0.5` 权重，归一化并乘以目标数量，截断得到各等级数量；生成每个怪物时按当前基础等级桶取值，扣减该桶计数，计数降到 0 后前进到下一桶，耗尽后停在最后一桶。
- 怪物等级 jitter 已恢复：随机值 `<0.1` 时等级 `-1`，`0.8..0.95` 时等级 `+1`，`>0.95` 时等级 `+2`；前三个 jitter roll 额外加 `-0.15`、`-0.10`、`-0.05` 的偏置。结果会夹到楼层低位和高位 `+1`，最高 `10`。
- 怪物等级总和目标已恢复：depth 1 为 `13`，depth 2 为 `54`，depth 3 为 `70`。如果初始生成的等级总和低于目标，会随机挑选未到上限的条目递增，直到达到目标。
- `DungeonBoard.GoldTarget()`：depth 1..6 分别为 `10, 15, 23, 36, 54, 80`，默认 `10`；`TotalGoldTarget()` 返回 `GoldTarget * 9`。
- `DungeonBoard.GoldByTier(tier)`：tier 1..10 分别为 `3, 5, 8, 12, 15, 18, 21, 24, 27, 30`，默认 `10`。
- `RandomGoldByTier` 与 `RandomGoldTarget` 使用 `Game.RandomNormal(mean, dev)`，其中 `dev = mean * 0.1`。常量来自 `0x100f54920 = 0.100000001`。
- `GenerateReward(t,goldTarget)` 会先调用 `GenerateRewardName()`，再把得到的 reward name、目标 tile 和 `goldTarget` 传入 `GenerateRewardByName(s,t,goldTarget)`。`GenerateRewardByName` 对 `Shop` 调 `GenerateShop`，对 `TreasureChest` 调 `GenerateChest`，对 `MimicChest` 走 `CreateFeature(t,s)` 后调用 `GetMonsterLevel(t)` 和 `MimicChest.SetLevel(level)`；其他 reward name 直接走 `CreateFeature(t,s)`，若返回对象 type-check 为 `Monster`，再调用 `GetMonsterLevel(t)` 和 `Monster.LevelTo(level)`。
- `GenerateShop(t,goldTarget)` 使用 `int(Game.RandomNormal(goldTarget, goldTarget * 0.1))` 采样；负值会提前返回，非负值创建 `Shop` feature 并把采样金额传给 Shop 的虚方法槽（高置信为 shop 金币/预算赋值）。`GenerateChest` 创建 `TreasureChest` 后调用 `TreasureChest.GenerateLoot()`；`GenerateMonsterChest` 创建 `TreasureChest` 后调用 `TreasureChest.GenerateLoot(1)`。`GenerateLoot()` 先用 `Game.RandomFloat()` 和阈值 `0.85 / 0.98` 选择 1/2/3 件掉落，再委托 `GenerateLoot(itemCount)`；后者会写入 `loot` 偏移 `0x88`，第一件必走 `CardFinder.ChooseReward(classBiases, rewardTiers, dungeon, minAffinity)` 卡牌分支，后续 item 以 `0.45` 阈值进入金币分支：`RewardTier()` -> `RandomGoldByTier(tier)` -> 累加 `gold` 偏移 `0x90`，否则继续用 `CardFinder.ChooseReward` 加卡。`CardFinder.ChooseReward` 的结构已恢复为 `CardList.GetCardList()` 候选池、`CardData.HasAccess()`、`Dungeon.SpawnCount(internalName) < maxSpawns`、有效 `CardData.GetTier(d)` 落在 tier 范围内、`ProfessionBase.RewardWeight(card) + CardData.FrequencyMod(ClassBiases()) + DungeonPlayer.FrequencyMod(card)` 权重/亲和度门槛、`RewardWrapper(card, frequency)` 汇总和 `Game.RandomRange(1,totalWeight)` 加权选择；无候选时逐步降低 tier 下界，最终 fallback 为 `Attack3`。`cardfinder_reward_analysis.json` 已把 floor 1 Warrior TreasureChest 的卡牌分支 `[1,3]` tier 范围跑成 31 个 base-unlocked 候选、总权重 75，并按原 `CardList.GetCardList()` 顺序生成 `RandomRange(1,75)` 的连续 roll band；另单独列出 7 个 requirement-gated 但若解锁可通过的候选，其中 6 个已 join 到 `achievements.json` 的达成条件/奖励文案。`cardfinder_unlock_state_smoke.json` 现在用显式 `DAMAGE2` UserAttribute fixture 验证 `Execute` 会进入 current-user 候选池，使候选数从 31 增到 32、总权重从 75 增到 85，并验证 requirement `103/never` 即使作为解锁输入也仍拒绝。`current_user_loading.json` 已确认 `GameManager` 通过 PlayerPrefs key `currentUser` 选择用户、`DungeonUser.LoadUser` 构造 `profiles/<user>/player.hpl` 并在文件缺失时回退 `CreateUser`，`DungeonUser.Load` / `Save` 经 `attributes` offset `0x80` 和 `(\d+): (\d+)\D*` 读写属性 pair；`current_user_loading_smoke.json` 当前 19/19 通过，`current_user_save_cardfinder_smoke.json` 进一步把 synthetic PlayerPrefs currentUser、synthetic `profiles/<user>/player.hpl` 和 CardFinder 解锁池串成 33/33 通过的 integration fixture。全卡牌 requirement join 只剩 `WINCHAOS` / `WINCHAOS2` 两个 enum-only gate 没有 AchievementList 行，它们对应 `WildStrike1/2/3` 和 `WildMana`，并被标注为 Chaos Mage 胜利/Champion gate 的中置信推断。完整列表迭代副作用、真实 `player.hpl` round-trip、原版 Unity PlayerPrefs/runtime 接入和 RNG 序列还没有变成可执行规则。
- `dungeon_user_save_codec_smoke.json` 用合成 `player.hpl` 形态文本验证属性 pair、seen-list 和 history round-trip，当前 20/20 通过；`current_user_save_cardfinder_smoke.json` 再把 synthetic PlayerPrefs currentUser、`profiles/IntegrationUser/player.hpl` 加载/解析和 parsed UserAttribute -> CardFinder unlock 串联验证，当前 33/33 通过，解析出的 `DAMAGE2` / `WINCHAOS` 会把 Warrior floor 1 CardFinder current-user 候选池从 31/75 扩到 33/86，并确认 `never` 不会被当作解锁属性。`real_save_sample_inventory.json` 已把工作区真实样本搜索落成可复跑证据，当前 `candidate_file_count=0`、`exact_player_hpl_sample_count=0`、`playerprefs_candidate_count=0`；这些 smoke 仍不验证真实 `player.hpl` 样本、原版 Unity PlayerPrefs 或原版存储根目录行为。
- `cardfinder_reward_smoke.json` 对上述 roll band 跑 96 个 selector 边界检查，覆盖每个候选 band 的起止 roll、连续性和越界 roll，当前全部通过。这个 smoke 验证的是已知候选池上的扣权重选择器，不验证原版 RNG 状态、CardList 构造副作用、用户成就状态或多次奖励之间的 `Dungeon.SpawnCard` mutation 序列。
- `cardfinder_spawn_sequence_smoke.json` 把选择和 mutation 串成一个确定性 fixture：默认先用当前 roll band 选 `StormSlash1`，调用 `SpawnCard` 语义递增 `StormSlash` 计数；随后重新计算候选池，再选 `StormSlash2` 并把 `StormSlash` 计数递增到 2。最终 `StormSlash1/2` 会从 Warrior floor 1 base-unlocked 候选池移除，候选数从 31 降到 29，总权重从 75 降到 61；当前 16/16 检查通过。这个 smoke 覆盖的是 base-name max-spawn mutation sequencing 的一个目标 roll fixture，仍不等同于 Unity RNG 状态、完整 chest loot branching 或用户成就状态。
- `dungeon_spawn_tracking.json` 进一步把 `Dungeon.SpawnCard(s)` / `Dungeon.SpawnCount(s)` 结构化：两者都会先调用 `Card.BaseName(s)`，该 helper 用 `(\d+)` 捕获第一个数字段并用空串替换，因此 `StormSlash1/2/3` 共享 `StormSlash` 计数。`SpawnCard` 在 flag `0x121` 为真时确保并递增 `this+0x110` 字典，在 flag `0x122` 为真时确保并递增 `this+0x108` 字典；`SpawnCount` 用 flag `0x120` 选择读取 `0x110` 或 `0x108`，缺 key 返回 0。`Dungeon.Initialize` / `PopulateFromString` 会创建这两个字典，`OutputToString` 会序列化两者；`BuildBoard` 生成阶段先设置读取/更新 `0x108`，结束后切到读取/更新 `0x110`，`BuildFakeBoard` 直接使用后者。`dungeon_spawn_tracking_smoke.json` 已验证 base-name 共享计数和 flag 控制读写，但还没有把连续 chest reward 的 mutation/RNG 序列并入完整 seed replay。
- `treasure_chest_loot_smoke.json` 已把 TreasureChest loot branch 和最终 shuffle helper 跑成 24/24 个确定性检查：`Game.RandomFloat()` item-count roll 分别覆盖 1/2/3 件，`GenerateMonsterChest` 风格的一件宝箱确认第一件必为卡，三件宝箱的卡-金-卡 fixture 验证第二件 `roll < 0.45` 时走 `RewardTier -> RandomGoldByTier` 金币累加，三件全卡 fixture 验证 `StormSlash1/2` 选完后第三张因共享 base-name 计数转向 `FrostSlash1`。反汇编中的 `0x1001cb21c` helper 已恢复为 `for i in 0..Count-1: RandomRange(i, Count-1); swap list[i] and list[randomIndex]`，fixture 现在同时输出 `loot_before_shuffle`、`loot_after_shuffle` 和每轮交换事件。该 smoke 不 replay Unity RNG stream、shuffle index 来源、完整 reward placement 或用户成就状态。
- `dungeon_reward_postprocessing_smoke.json` 已把 `GenerateRewards` 后处理跑成 21/21 个确定性检查：Grizzly Bear 追加 `HealthPack`，Kitten 追加 `HealingPool` 和 `TreasureChest`，Velociraptor 不追加普通奖励；当前用户属性 `DRAGON1` 追加 `TreasureChest`，`STEPS1` 追加 `HealthPack`，两者叠加时和 Kitten 难度一起得到 `TreasureChest=2`、`HealingPool=1`、`HealthPack=1`。该 smoke 也确认合成 `DAMAGE2`/`WINCHAOS` player.hpl fixture 不会触发这两个地牢奖励后处理，并新增 synthetic PlayerPrefs `currentUser` + `profiles/RewardUser/player.hpl` 的 `DRAGON1`/`STEPS1` 加载后处理 fixture；`dungeon_seed_current_user_postprocessing_smoke.json` 进一步把同一类 synthetic current-user save 属性接入 floor seed harness，当前 23/23 通过，确认 loaded seed reward count 从 12 增到 14、user-attribute post-processing rewards=2 且 placed reward count=14；但仍不 replay 完整 `GenerateRewards` RNG stream、真实用户文件、原版 PlayerPrefs 或 Unity feature placement 顺序。
- `dungeon_rng_replay_gaps.json` 明确把地牢 seed 和奖励相关 RNG/list-order 缺口拆成 20 类消费者：maze wall、monster level、GenerateRewards、gold、boss、AddMonsters、ThreatComponent、RandomUnusedTile、TreasureChest、CardFinder 和 SpawnCard 都已入列；当前 seed 中有 129 个已记录标量 fixture、0 个可推断但未记录标量消费者、17 次 shuffle 调用、17 条 shuffle order boundary 和 349 个 fixture-derived shuffle index draw，全部仍不是原版共享 RNG stream replay。
- `dungeon_path_between_smoke.json` 已把 PathBetween wrapper 和 try-number predicate 跑成 21/21 个确定性检查，覆盖 tryNumber 1 避开 `AvoidIfPossible`、失败后 retry tryNumber 2、destination `MoveNextTo` 例外、tryNumber 0 的 `WillRevealDark` rejection 和 predecessor visited guard；它记录 11 条 neighbor shuffle boundary，但这些 boundary 仍不是原版 `Utility.Shuffle` index stream。
- `utility_shuffle_recovery.json` 把 8 个 `Utility.Shuffle` 泛型实例单独拆出：当前 39/39 检查通过，6 个实例是直接 `for i in 0..Count-1: RandomRange(i, Count-1); swap` 形态，2 个实例是 wrapper 并委托到已恢复的 list shuffle；14 个直接 xref 中 8 个和地牢生成相关。该 artifact 恢复的是 index-loop 形态，不 replay 每个调用点的原版 index draw 或共享 RNG stream。
- `dungeon_shuffle_helper_recovery.json` 把 shuffle helper 证据单独拆出：当前 36/36 检查通过，6 条路径包括 BuildMaze tile order、AddRewards reward-name final shuffle、AddMonsters all-tile shuffle、RandomUnusedTile(list)、PathBetween neighbor shuffle 和 TreasureChest loot final shuffle；其中 5 条 `Utility.Shuffle`/delegated 路径已接入 `utility_shuffle_recovery.json` 的 index-loop 形态，TreasureChest 的 `0x1001cb21c` helper 已由 smoke 验证，floor seed 的 17 条 shuffle boundary 现在都有可重放当前 fixture order 的 349 个 derived index draw，PathBetween 由独立 smoke 记录 11 条 boundary，但这些路径仍没有接入 floor seed 原版 RNG stream。
- `dungeon_shop_generation_smoke.json` 已把 `GenerateShop(t,goldTarget)` 跑成 28/28 个确定性检查：验证 `int(Game.RandomNormal(goldTarget, goldTarget * 0.1))` 的负值 early return、非负值创建 `Shop` feature、`Shop` 虚槽 `0x5c` 接收采样金额，以及 floor seed 中 4 个 Shop 样本的 gold/budget 值 `[9, 7, 5, 5]`。该 smoke 不 replay 原版 RNG stream、Shop inventory/UI、prefab allocation、transforms 或真实 Unity object graph。
- `shop_inventory_ui_smoke.json` 已把 Shop 类自身跑成 54/54 个确定性检查：固定 `text/items/boughtItems/maxItems/internalName` 字段、`Initialize/GenerateText/GenerateItems/BuildLootObjects/AcquireCard/Finished` 的 method index/slot、`GenerateItems` 的 1/3/5 tier 物品 fixture、`BuildLootObjects` 的 bought/max 计数、`MasterThief` 免费购买分支和 ShopDialogueObject 构造形态。该 smoke 不 replay 原版 CardFinder/RNG stream、ShopDialogueObject 渲染、Unity transforms 或 live UI event dispatch。
- `shop_item_generation_recovery.json` 已把 `Shop.GenerateItems(goldTarget)` 的静态调用形态跑成 40/40 个检查：`Shop.items` 写入偏移 `0x88`，固定 tier 数组为 `[1, 3, 5]`，物品选择调 `CardFinder.ChooseReward(b, tiers, d, minAffinity)` method_index `15974`，该 tail thunk 进入 onlyCards overload `15977`，重复检测 helper 为 method_index `9411`（当前无游戏 metadata 名称），命中重复后重选，最后以 `Utility.Shuffle` method_index `24534` 洗牌并写回 `items`。该 recovery 不 replay 原版共享 RNG stream、连续 CardFinder 列表/SpawnCard mutation、泛型 helper 名称或 Shop UI live dispatch。
- `dungeon_physical_construction_gaps.json` 明确把 Unity physical construction 缺口拆成 feature registry、Tile.IsUnused child-filter、reward dispatch、TreasureChest post-create、MimicChest visual side effects 和 Shop gold/physical state 6 类；当前 floor-1 seed 有 20 个 registry feature、19 个原版 DungeonFeature-like child、1 个 harness start marker、19 个 ChildChange 记录、0 个 tile collision，并记录 6 条 physical boundary catalog（floor-1 seed 5 条 covered、any-seed fixture 6/6 covered）。`remaster_dungeon_mimic_seed.json` 提供 depth-2 seed 5 MimicChest tile，`mimic_chest_open_smoke.json` 已把 MimicChest open path 补成 deterministic fixture，`dungeon_shop_generation_smoke.json` 已把 Shop gold/budget setter 补成 deterministic fixture，`shop_inventory_ui_smoke.json` 已把 Shop inventory/dialogue state 补成 deterministic fixture；这仍不是 byte-accurate Unity object graph。
- `dungeon_original_accuracy_flag_index.json` 把剩余地牢 false flag 独立成可审查索引：262 个唯一 entry / 2,020 次 occurrence，被归入 6 个队列且 uncategorized=0；其中包括 seed 规则 false 项、180-case sweep 的重复 false 项、真实 save/PlayerPrefs 样本缺失派生 flag、RNG/index stream、Unity physical/visual UI、reward/ThreatComponent 和 CardFinder/unlock 队列。
- `dungeon_generation_accuracy_gaps.json` 明确记录地牢生成 runtime 的剩余原版准确性队列：171/171 检查通过，`dungeon_generation.json` 的 13 个预期 section 均存在，floor-1 seed 连通且 396 个相关 smoke check 全部通过，另纳入 `dungeon_seed_sweep.json` 的 16 个 check / 180 个 compact case、`dungeon_generate_monster_levels_recovery.json` 的 20 个 check（bucket walk recovered and harnessed）、`dungeon_monster_level_target_boundary.json` 的 18 个 check（180/180 至少达到 target、54 exact、126 over-target、0 under-target）、`dungeon_add_rewards_bridge.json` 的 28 个 check / 180 个 AddRewards case、40 个 RNG replay gap check、36 个 shuffle helper recovery check、39 个 Utility.Shuffle recovery check、21 个 PathBetween smoke check、40 个 Shop item generation recovery check 和 12 个 false-flag index check；AddRewards bridge 覆盖 2,658 个 placed reward、2,582 个 non-fallback ThreatComponent pick、648 个 Shop gold sample、0 个 gold schedule mismatch、0 个 feature registry count mismatch 和 6/6 physical boundary id，floor seed 的 17 条 shuffle boundary 已有 349 个 fixture-derived index draw，PathBetween smoke 另记录 11 条 neighbor shuffle boundary，physical boundary any-seed fixture coverage 为 6/6 且 depth-2 seed 记录 1 个 MimicChest tile；sweep 的 126 个非 exact monster level-sum case 已确认为 floor-boundary overshoot，而不是 under-target 失败。seed 规则中仍有 4 个原版准确性 false 项（`add_rewards_gold_distribution_original_accurate`、`add_rewards_threat_component_replay_original_accurate`、`tile_feature_placement_original_accurate`、`unity_physical_replay_complete`），synthetic PlayerPrefs/currentUser + synthetic `player.hpl` + CardFinder integration、synthetic current-user save reward post-processing、seed-level synthetic current-user reward placement、Shop gold/budget setter fixture、Shop inventory/dialogue fixture 与 Shop item generation 调用形态已覆盖，真实 `player.hpl` 样本和原版 Unity PlayerPrefs 仍未验证，且 inventory 确认当前工作区候选样本数为 0；18 个相关 artifact 仍为 `prototype_runtime_ready=false`。
- `MimicChest.SetLevel(x)` 已确认只是把传入整数写入 `internalLevel` 偏移 `0xa8`。`MimicChest.Open()` 使用 `Parent()` 取得 chest 所在 tile，以 `DungeonBoard.CreateNamedMonsterAtLevel(tile, internalLevel, "MimicMonster")` 生成拟态怪，随后 `VirtualObject.Destroy()` 销毁 chest，调用 `Monster.DisplayInMini(false)`，把返回 monster 写入 `Dungeon.displayedInMini` 偏移 `0xe8`，最后经 `Dungeon.board.playerSprite` 调 `PlayerSprite.RefreshNoWipe()`。`mimic_chest_open_smoke.json` 已对这条高层链路跑 30/30 fixture 检查；`DisplayInMini`/`RefreshNoWipe` 内部 Unity visual side effects 仍未 replay。
- `AddRewards()` 的奖励预算排程已恢复：先构造 `ThreatComponent(component, board, GetToPlayerThreats())`，按 threat 降序排序；每个 shuffled reward 通过 `Game.RandomFloat() * currentWeightTotal` 选择组件，并用 `ThreatComponent.Pick()` 增加该组件 `picks`、降低 `openSquares` 和 `weight`。随后按排序后的组件和 `picks` 数放置奖励，`goldTarget = int((TotalGoldTarget() / rewardCount) * multiplier)`，`multiplier` 初始为 `1 + rewardCount * 0.025`，每放一个奖励后加 `-0.05`。
- `dungeon_add_rewards_bridge.json` 对 AddRewards 的 fixture consistency 跑 28/28 检查：180 个 compact case 中 2,658 个奖励全部成功放置，2,582 个 non-fallback 奖励匹配 ThreatComponent pick，648 个 Shop gold sample 全部有采样记录，`goldTarget` 排程、feature registry count、tile child change 和 duplicate reward tile mismatch 均为 0；这仍是确定性 bridge，不是原版 RNG/shuffle stream 或 Unity physical replay。
- `ThreatComponent.Pick()` 的消耗规则已恢复：当旧 `openSquares < 2` 时消耗全部当前 weight；否则 weight 为 `1.0` 消耗 `0.5`，weight 为 `0.5` 消耗 `0.35`，weight 为 `0.15` 消耗 `0.1`，其他值消耗全部当前 weight。
- 奖励 tier 范围：depth 1 为 `1..4`，depth 2 为 `4..7`，depth 3 为 `7..10`，depth 4 为 `0..0`，其他默认 `7..10`。
- `RewardTier()` 概率：depth 1 以 `0.333/0.666` 阈值产出 tier `1/2/3`；depth 2 产出 `4/5/6`；depth 3 以 `0.4/0.8` 阈值产出 `7/8/9`；depth 4 或其他分支返回 tier `10`。
- `GenerateRewardName()` 概率已恢复：`TreasureChest 40%`、`HealthPack 15%`、`Shop 15%`、`Monastery/Blacksmith/SmoothieShack/HealingPool` 各 `5%`。最后 `10%` 在 depth 1 全部是 `GoblinHoarder`；depth > 1 时为 `MimicChest 5%` 和 `GoblinHoarder 5%`。
- `GenerateRewards()` 初始奖励列表会加入 3 个 `Shop`。随后 50% 分支加入 1 个 `HealingPool` 和 `depth + RandomRange(1,2)` 个 `HealthPack`；另一分支加入 `depth * 2 + RandomRange(2,4)` 个 `HealthPack`。再按约 `33.3% / 33.36% / 33.34%` 加入 `Monastery`、`Blacksmith` 或 `SmoothieShack`，并额外调用 `GenerateRewardName()` `RandomRange(2,4)` 次。
- `GenerateRewards()` 后处理已恢复：若当前用户 `DRAGON1` 成就属性非零，额外加入 1 个 `TreasureChest`；若 `STEPS1` 非零，额外加入 1 个 `HealthPack`。`Dungeon.Initialize` 会把 `GameManager.Difficulty()` 存进 `Dungeon.difficulty`，难度选项来自 UI 工厂：`Kitten`、`Grizzly Bear`、`Velociraptor`。`Grizzly Bear` 难度额外加入 1 个 `HealthPack`；`Kitten` 额外加入 1 个 `HealingPool` 和 1 个 `TreasureChest`；当前未看到 `Velociraptor` 的额外奖励分支。
- `DungeonBoard.CreateMonster` 与 `DungeonBoard.CreateBoss` 已反汇编到 `extracted/disassembly/DungeonBoard_CreateMonster.txt`、`extracted/disassembly/DungeonBoard_CreateBoss.txt`。`CreateBoss` 的主流程已经结构化到 `dungeon_generation.json` 的 `boss_generation`：
  - boss 基础等级为 `min(last(MonsterLevels()) + 1, 10)`，所以 depth 1/2/3 分别为 `4/7/10`。
  - 若 `Dungeon.floorBoss[depth - 1]` 不是空字符串，直接使用该固定 internal name。
  - 否则构造此前楼层的 `floorBoss` 排除列表，调用 `MonsterFinder.ChooseBoss(environment, bossLevel, exclude)`。
  - `ChooseBoss` 循环调用 `ChooseMonster(environment, level, boss=true)`，并拒绝空字符串和排除列表里的 boss。
  - `MonsterData.CanFit(environment, level)` 要求 `Frequency(environment) >= 1` 且 `minLevel <= level <= maxLevel`；`Frequency` 使用 `MonsterData` 中对应环境权重。
  - 选择时按权重做 `Game.RandomFloat() * totalWeight` 抽样；`dungeon_generation.md` 已列出每个 depth/environment 的 boss 候选权重和基础概率。
  - 生成后通过名称创建 monster，并调用 `Monster.LevelTo(clamp(baseBossLevel + Dungeon.BonusMonsterLevel(), 1, 10))`。
- `Dungeon.floorBoss` 生命周期已经确认：`Dungeon.Initialize` 创建长度为 3 的 `floorBoss` 和 `floorEnvironments`，初始 `floorBoss` 全部是空字符串、`floorEnvironments` 全部是 `NONE = 7`；首次生成楼层时 `CreateBoss` 因此走随机分支；`Dungeon.BuildBoard`/`BuildFakeBoard` 会在生成后把 `board.bossName` 和 `board.environment` 写回对应下标；`PopulateFromString` 可从存档/seed 还原这些值。
- `Dungeon.PopulateFromString`/`OutputToString` 的尾段序列化顺序已确认：`difficulty`、`initialseed`、`floor1seed`、`floor2seed`、`floor3seed`、`seedName`、`savedUserAttributes` 的 count 与逐项 int、`levelUpSeed`、`builtFromSeed`、`floorBoss[0..2]`、`floorEnvironments[0..2]`，分隔符为 `|`。其中 `floorBoss` 三项按 string 存取，`floorEnvironments` 三项按 int 存取。
- `CreateBoss` 还有一个含 `LichHunter` 字面量的特殊分支：当生成的 boss 通过 runtime type token `0x79b` 检查时（高置信推断为 `Lich`），会临时把 boss tile 标记为 `impassable`，取 `PlayerTile()`，对玩家 tile 调 `GetComponent(playerTile)`，从该连通分量里调用 `RandomUnusedTile(component)`，在选中的 tile 上生成 `LichHunter`，然后恢复 boss tile。
- `DungeonBoard.AddTraps` 在这个 build 中是空方法，反汇编只有 `ret`。
- `DungeonBoard.BuildMaze`、`CanDelete`、`FindDeletableSquares`、`IsConnected` 已反汇编并结构化到 `dungeon_generation.json`。关键墙状态字段是 `Tile.impassable = [tile + 0x90]`，证据是 `Tile.BlocksMovement` 看到该字段为 true 时立即返回阻挡。
- `BuildMaze` 算法：取 `Utility.Shuffle(DungeonBoard.AllTiles())`，逐个 tile 调 `CanDelete(tile)`；`CanDelete` 会临时把 tile 设为 impassable，调用 `IsConnected()` 检查剩余非墙 tile 是否仍在同一连通分量，然后恢复原状。若可删除且 `Game.RandomFloat() < 0.5`，该 tile 被永久设为墙。因此生成的墙数量是随机的，但所有可通行 tile 保持连通。
- `DungeonBoard.PathBetween`/`PathBetweenTryNumber` 已结构化到 `pathing_and_cut_vertices`：先尝试 `tryNumber = 1`，失败时用 `tryNumber = 2` 重试；内部使用和棋盘同尺寸的前驱表，从起点出发按 shuffled `Tile.Neighbors()` 扩展，命中目标后沿前驱表回溯生成路径。候选 tile 必须 `Tile.OpenAndVisible()`，或是目标 tile 且 `Tile.MoveNextTo()`；`tryNumber = 1` 会避开 `Tile.AvoidIfPossible()`，除非该 tile 是目标；`tryNumber = 2` 放宽这个 avoidance 检查。
- cut-vertex 检测逻辑已恢复：`FindCutVertexInfos()` 从 `FindDeletableSquares()` 取候选，只保留 `Tile.IsUnused()` 且 `Tile.CanBeACut()` 的 tile；临时把该 tile 的 `impassable` 设为 true，重算 `GetThreatComponents()`，如果 component 数量增加则记录候选。候选记录 offset `0x10` 是切断后的 component list，offset `0x18` 是候选 tile；`FindBestCutVertex()` 返回 offset `0x18` 的 tile。
- `Tile.IsUnused()` 会先检查 `Tile.impassable` offset `0x90`，为 true 时立即返回 false；否则调用 `VirtualObject.Children()` 并扫描子对象，发现解析后的子对象是 `DungeonFeature`（RTTI token `0x7a2`）时返回 false，遍历完没有命中则返回 true。`0x7a2` 的类型名来自交叉验证：`Tile.PrimeFeature()` 对同一 token 调用 virtual slot `0x43`（`DungeonFeature.Priority`），`Tile.BlocksMovement()` 对同一 token 调用 slot `0x4a`（`DungeonFeature.BlocksMovement`）；`Tile.GetMonster()`/`HasMonster()` 另用 token `0x77e` 判定 `Monster`，且 `Monster` 继承自 `DungeonFeature`，所以怪物也被 `IsUnused()` 的 feature 检查覆盖。seed harness 现在输出 `placements.feature_registry`，显式记录 placed feature 到 tile child 的 fixture 级注册，当前 floor 1 seed 为 20 个 registered features、0 个 tile collision；这仍不是 byte-accurate Unity 对象构造。
- `Tile.CanBeACut()` 已恢复为几何预过滤：读取 `Left/Up/Right/Down`，四个 accessor 都委托 `DungeonBoard.TileAt()` 做坐标边界检查；分别统计存在的正交邻居数，以及 left-up、up-right、right-down、down-left 四个角桥是否缺失得到 gap 数；返回条件是 `neighbor_count >= 2 && gap_count > 1`。这个方法本身不读取 `impassable`，后续仍由 `FindCutVertexInfos()` 的 `IsUnused()` 和 component-count test 过滤。
- `AddMonsters()` 会先循环调用 `FindBestCutVertex()`，最多选 5 个非空 cut tile，把它们加入 `selected_tiles` 并临时设为 `impassable=true`。随后取 `Utility.Shuffle(AllTiles())`，先对已选 tile 的邻居做临时阻挡，再用 `Tile.IsUnused()` 与 `Game.RandomFloat() < Tile.MonsterPercent()` 做概率选点；每个新选点的未阻挡邻居也会临时设为 impassable。概率 pass 后释放这些邻居阻挡；如果还没达到目标数量，再从同一 shuffled allTiles 中补入 unused 且未在 `selected_tiles` 的 tile。最终释放 `selected_tiles` 的 impassable 标记，构造 `TilePlusInt(tile, GetStartDistances()[tile.x, tile.y])`、用 `TileDistanceSort` 排序，并按排序后的 tile 顺序调用 `CreateMonsterAtLevel(tile, generatedLevel, seen)`。这里的 `seen` 是 dictionary 风格的 internalName 计数 map。
- `AddTaverns()` 已恢复主要规则：入口先要求 `Dungeon.player.profession`（`DungeonPlayer + 0xd8`）非空并通过 runtime type token `0x7a7` 检查（高置信推断为 `ProfessionBard`），随后通过 `CreateFeature(tile, "Tavern")` 创建；depth 1 放在 `RandomUnusedNeighbor(PlayerTile())`，其他层放在 `RandomUnusedTile()`；若 depth 1 且 `WINBARD2`/`Musical Champion` 属性 149 非零，会在另一个 `RandomUnusedTile()` 额外创建一个 `Tavern`。`DungeonPlayer.Initialize` 会把 `ProfessionBase.Build(...)` 的返回值写入 offset `0xd8`。

当前可推断的生成管线：

1. DungeonBoard 使用 floor seed 生成每层地图。
2. `BuildMaze` 先在 tile 网格上随机尝试造墙；每次造墙都通过 `IsConnected()` 维护通行区域连通性。
3. `GenerateMonsterLevels` 先建立怪物等级分布，并通过楼层等级总和目标补足威胁值。
4. `AddRewards` 将宝箱、血包、shop、altar、mimic/goblin hoarder 等奖励/功能对象放入地图。
5. `GetThreatComponents` 计算威胁组件，用于后续怪物放置。
6. `AddMonsters` 先临时保留若干 cut vertex tile，再通过概率选点与 backfill 放置普通怪；boss 生成走 `CreateBoss`/`AddBoss` 路径。

未完全确认：

- 地牢生成主流程目前已结构化到 `dungeon_generation.json`：地图尺寸、奖励概率、奖励后处理、金币目标、怪物数量、怪物基础等级、等级 jitter、boss 主选择权重、`floorBoss` 生命周期、floor boss 序列化、`LichHunter` 放置路径、`BuildMaze` 墙生成、PathBetween predicate、cut-vertex 检测、AddMonsters 选点/释放/最终生成顺序和 tavern 主放置规则都已有结构化数据。`dungeon_generation_accuracy_gaps.json` 已把剩余地牢风险拆成 Unity physical construction、RNG/list ordering、ThreatComponent replay、gold/shuffle、CardFinder integrated replay、SpawnCard tracking、real save/PlayerPrefs、special reward visuals 和 post-processing integration 队列。剩余大项主要在卡牌复杂效果、怪物复杂行为、地牢原版准确 seed/reward replay、职业/技能动作的可执行级语义。

## Remaster 建议

已补第一版数据契约：

- `schemas/card-data.schema.json` 对应 `extracted/structured/card_data.json`
- `schemas/card-effect-index.schema.json` 对应 `extracted/structured/card_effect_index.json`
- `schemas/card-effect-slots.schema.json` 对应 `extracted/structured/card_effect_slots.json`
- `schemas/card-effect-tailcalls.schema.json` 对应 `extracted/structured/card_effect_tailcalls.json`
- `schemas/card-effect-calls.schema.json` 对应 `extracted/structured/card_effect_calls.json`
- `schemas/cards.schema.json` 对应 `extracted/structured/cards.json`
- `schemas/monsters.schema.json` 对应 `extracted/structured/monsters.json`
- `schemas/monster-catalog.schema.json` 对应 `extracted/structured/monster_catalog.json`
- `schemas/monster-behavior-index.schema.json` 对应 `extracted/structured/monster_behavior_index.json`
- `schemas/monster-levelup-calls.schema.json` 对应 `extracted/structured/monster_levelup_calls.json`
- `schemas/monster-behavior-calls.schema.json` 对应 `extracted/structured/monster_behavior_calls.json`
- `schemas/professions.schema.json` 对应 `extracted/structured/professions_initializers.json`
- `schemas/profession-behavior-index.schema.json` 对应 `extracted/structured/profession_behavior_index.json`
- `schemas/profession-behavior-calls.schema.json` 对应 `extracted/structured/profession_behavior_calls.json`
- `schemas/profession-action-loadouts.schema.json` 对应 `extracted/structured/profession_action_loadouts.json`
- `schemas/profession-skills.schema.json` 对应 `extracted/structured/profession_skills.json`
- `schemas/combat-abilities.schema.json` 对应 `extracted/structured/combat_abilities_initializers.json`
- `schemas/combat-ability-behavior-index.schema.json` 对应 `extracted/structured/combat_ability_behavior_index.json`
- `schemas/combat-ability-behavior-calls.schema.json` 对应 `extracted/structured/combat_ability_behavior_calls.json`
- `schemas/dungeon-action-behavior-index.schema.json` 对应 `extracted/structured/dungeon_action_behavior_index.json`
- `schemas/dungeon-action-behavior-calls.schema.json` 对应 `extracted/structured/dungeon_action_behavior_calls.json`
- `schemas/dungeon-talents.schema.json` 对应 `extracted/structured/dungeon_talents.json`
- `schemas/talentbase-behavior-calls.schema.json` 对应 `extracted/structured/talentbase_behavior_calls.json`
- `schemas/creature-talent-behavior-calls.schema.json` 对应 `extracted/structured/creature_talent_behavior_calls.json`
- `schemas/dungeon-player-talent-behavior-calls.schema.json` 对应 `extracted/structured/dungeon_player_talent_behavior_calls.json`
- `schemas/dungeon-generation.schema.json` 对应 `extracted/structured/dungeon_generation.json`
- `schemas/remaster-starter-slice.schema.json` 对应 `extracted/structured/remaster_starter_slice.json`
- `schemas/remaster-starter-smoke.schema.json` 对应 `extracted/structured/remaster_starter_smoke.json`
- `schemas/remaster-starter-combat.schema.json` 对应 `extracted/structured/remaster_starter_combat.json`
- `schemas/remaster-dungeon-seed.schema.json` 对应 `extracted/structured/remaster_dungeon_seed.json` 和 `extracted/structured/remaster_dungeon_mimic_seed.json`
- `schemas/remaster-completion-audit.schema.json` 对应 `extracted/structured/remaster_completion_audit.json`
- `schemas/remaster-manifest.schema.json` 对应 `extracted/structured/remaster_manifest.json`

这些 schema 约束的是当前已稳定的提取数据，不把尚未恢复的卡牌执行效果、怪物 AI/回合逻辑、职业/技能行为或地牢后处理伪装成完整运行时 API。

建议采用“数据优先、规则引擎优先”的 remaster 路线，而不是先复刻 UI：

1. 先把原版内容导成结构化数据：
   - `cards.json`
   - `monsters.json`
   - `monster_catalog.json`
   - `professions.json`
   - `profession_skills.json`
   - `dungeon_talents.json` / `talents.json`
   - `songs.json`
   - `dungeon_generation.json`
   - `remaster_starter_slice.json`
   - `remaster_starter_asset_pack.json`
   - `remaster_starter_smoke.json`
   - `remaster_starter_combat.json`
   - `remaster_dungeon_seed.json`
   - `remaster_dungeon_mimic_seed.json`
   - `remaster_manifest.json`
   - `remaster_completion_audit.json`
   - 结构建议从 `extracted/structured/key_systems.json` 的字段模型开始，而不是从 UI 文本倒推。

2. 建一个纯逻辑战斗模拟器：
   - deterministic RNG
   - deck/hand/graveyard/exile/stack
   - actions/mana/health/counters/status
   - card play resolution
   - combat ability cooldown

3. 建一个地牢生成测试器：
   - 输入 seed、职业、floor
   - 输出地图、monster placements、reward placements、boss
   - 对比原版规律和 bestiary/achievement 文本

4. UI 层最后接入：
   - 原版是 2D 卡牌 UI 和 tile dungeon，不需要 3D。
   - 如果目标是快速可玩和跨平台，Web/TypeScript 或 Godot 都比旧 Unity 工程更适合重写。
   - 如果目标是移动端商店发布且团队熟悉 Unity，可用现代 Unity 重新实现，但不要尝试升级原 Unity 4.7 项目，因为没有工程源文件且原代码是 UnityScript/IL2CPP。

5. 验证方式：
   - 每张卡写独立规则测试。
   - 每个怪物有 deck/AI smoke test。
   - 每个职业有起始牌组、能力冷却、特殊规则测试。
   - 每个 dungeon seed 有 snapshot test。

## 法务和素材边界

这份解析能帮助理解原版结构，但 remaster 是否能直接使用原素材、原文本、原名称和原规则，取决于授权。若没有明确授权，建议把提取结果作为研究参考，最终发布版本使用新素材、新文本，并只保留受许可的玩法灵感。

## 下一步

优先级最高的后续工作：

1. 继续把 `cards.json` 中 `runtime_rule.status=structured_trace` 的归一化步骤推进为原版准确的全卡规则；card effect trace 已收敛为已命名/已归类操作且未剩 unresolved 或 context-only 条目，`card_runtime_smoke.json` 已覆盖 45 个代表性执行检查，`card_runtime_matrix_smoke.json` 已覆盖 599 个全卡 source-method case，`card_manual_control_flow_replay.json` 已覆盖 5 张手工控制流卡的 19 个 branch fixture，`card_recorded_step_replay.json` 已覆盖 52 张卡的 60 个 recorded state/object step fixture，`card_lifecycle_replay.json` 已覆盖 6 个 recovered lifecycle case 的 14 个 deterministic fixture check，`card_equipment_lifecycle_matrix.json` 已覆盖 42 张 Equipment 卡的 74 个 source-method case 和 18 个 deterministic fixture check，`card_zone_replay.json` 已覆盖 6 个 recovered zone/action case 的 14 个 deterministic fixture check，`card_payment_replay.json` 已覆盖 9 个 recovered payment/counter case 的 17 个 deterministic fixture check，`card_play_replay.json` 已覆盖 9 个 recovered play/side-effect case 的 17 个 deterministic fixture check，`ai_action_candidate_replay.json` 已覆盖 5 个 recovered candidate/action+target case 的 13 个 deterministic fixture check，`ai_target_legality_replay.json` 已覆盖 10 个 recovered target-legality/target-set case 的 21 个 deterministic fixture check，`target_wrapper_identity_recovery.json` 已恢复 TargetFinder metadata、target wrapper payload、target-set list mutation、inclusive RandomRange、tail-swap target sampling 和 random-order target-set shuffle 静态形态，`ai_turn_policy_replay.json` 已覆盖 12 个 recovered turn/pass/invocation case 的 23 个 deterministic fixture check，`action_execution_replay.json` 已覆盖 11 个 recovered action/visual-stack case 的 19 个 deterministic fixture check，`action_runtime_accuracy_gaps.json` 已把 deterministic AI/action/card/starter 覆盖后的原版执行层缺口拆成 11 类队列，`card_get_ai_value_adjustment_recovery.json` 已恢复 `Card.GetAIValue` scenario adjustment 的基类 identity hook，`ai_value_runtime_smoke.json` 已通过共享 runtime 执行 Card/Creature AI value formula fixture，`card_ai_value_matrix_smoke.json` 已通过共享 runtime 执行全部 370 张卡的 AIWeight/base GetAIValue formula fixture，`card_ai_priority_matrix_smoke.json` 已通过共享 runtime 执行全部 370 张卡的 ActionPlay priority/AIUseValue fixture，`card_actionplay_bridge_matrix_smoke.json` 已通过共享 runtime 执行全部 370 张卡的 normal ActionPlay candidate bridge/full-hand enumeration/action-target-pair fixture，`card_ai_value_recovery.json` 已恢复基础 Card AI value/weight 链，`creature_ai_weight_recovery.json` 已恢复 Creature.AIWeight 公式，`card_effect_value_flow_index.json` 已把 179 个 computed/receiver value-flow step 分入 8 个 argument-flow 队列且 unclassified=0，`card_original_accuracy_flag_index.json` 已把剩余 false flag 规范化为 362 个 entry / 8,052 次 occurrence / 9 个工作队列且 uncategorized=0，`card_runtime_accuracy_gaps.json` 已把剩余工作拆成 169/169 通过的队列，`card_rng_recovery.json` 已把 23 张 RNG/shuffle 候选收窄为 27/27 静态 helper/list-order 证据并二进制确认 Stone shuffle 形态，`action_execution_recovery.json` 已恢复 `ActionBase.Perform/Resolve` 与 `ActionPlay -> Card.PlayMe` 的静态执行桥，`card_play_pipeline_recovery.json` 已恢复 Card.PayFor/Card.PlayMe 下层成本、支付、reaction、side-effect、牌区和 action lookup 静态形态。剩余重点不再是基础 slot 命名，而是 Card.GetAIValue live scenario/combat replay、Creature.AIWeight combat-state replay、original candidate/target legality/target-set/TargetWrapper identity/AI-turn/action/card-payment/card-play/card-zone/lifecycle replay、visual-stack coroutine replay、pass/end-turn side effects、原版 timing/reaction/UI 级控制流、typed object lifecycle、分支条件、参数值/receiver identity、card RNG stream/shuffle index replay、UI 选择、equipment lifecycle 和 per-card AI。
2. 继续把 `monster_catalog.json` 中已归一化的 `behavior_runtime_rule`、`level_up_rule` 和特殊行为翻译成原版准确的怪物 AI、回合和限制规则；`monster_runtime_smoke.json` 已覆盖全部 73 个 recovered behavior rule 和 247 个升级等级组合，`monster_runtime_matrix_smoke.json` 已覆盖全部 87 个 behavior source-method case，`monster_context_dependency_index.json` 已把 54 个 read/context dependency step 分入 8 个 owner 队列，`monster_effect_value_flow_index.json` 已把 43 个 computed/receiver value-flow step 分入 7 个 argument-flow 队列，`monster_control_flow_replay.json` 已覆盖三组手工行为/升级分支和 10 个 raw state-write fixture，`monster_original_accuracy_flag_index.json` 已把剩余 false flag 规范化为 283 个 entry / 735 次 occurrence / 9 个工作队列且 uncategorized=0，`monster_runtime_accuracy_gaps.json` 已把剩余 AI、action execution、turn timing、target/player context、value-flow、receiver identity、RNG、UI、restriction、typed state-write 和 level-up replay 风险拆成队列，`prototype/starter/` 的 Monsters 视图现在可在 starter 子集和 73 个全量 recovered monsters 间切换并展示素材、等级、boss flag、地点、牌组、倍率、behavior/level-up status；starter / Sphinx fixture 继续覆盖重点回归，但整体还不是完整可执行升级/AI 引擎。
3. 基于现有 `dungeon_generation.json` 写一版可执行楼层生成器/seed 回放验证，把结构化规则和原版统计行为对齐；`prototype/starter/` 的 Dungeon 视图现在会把 floor-1 seed snapshot 和 recovered board/RNG、maze/pathing、monster placement、rewards/gold、boss/feature、accuracy gap 摘要放在同一页面，便于检查生成逻辑；`dungeon_generation_accuracy_gaps.json` 已把剩余 Unity physical construction、RNG/list order、ThreatComponent/gold/shuffle、CardFinder/SpawnCard、real save/PlayerPrefs 和 special reward visual side-effect 风险拆成队列。
4. 继续把 `profession_skills.json` 中的职业 `AddDungeonActions`、`AddCombatActions`、`CombatApplyToPlayer`、`DeckForCombat`、`WinFight`、combat ability `DoMe` 和 dungeon action `Perform` / `IsLegalTarget` trace 翻译成原版准确规则；profession trace 已收敛为 89/89 已命名，combat ability trace 已收敛为 101/101 已命名，dungeon action trace 已收敛为 327/327 已命名，`profession_starting_profile_smoke.json` 已覆盖 70 个起始档案检查，`profession_skill_runtime_smoke.json` 已覆盖 65 个保守执行检查（含 16 个 scheduler fixture checks 和 12 个 WinFight/ready-message scheduler event），`profession_skill_runtime_matrix_smoke.json` 已覆盖 154 个全职业/技能/地城动作 source-method case 和全部 43 个 DungeonTalent handler，`profession_skill_scheduler_matrix.json` 已覆盖全部 60 个 cooldown initializer 和 445 个 deterministic scheduler case，`profession_skill_recorded_replay.json` 已覆盖 39 个 hand-recovered/manual、state-write recorded 和 DungeonTalent handler replay 检查，`profession_skill_targeting_ui_bridge.json` 已覆盖 19 个 targeting/UI bridge 检查、37 个 targeting/UI entry 和 85 个 focused source-method case，`profession_skill_context_dependency_index.json` 已覆盖 31 个 read/context dependency 检查、67 个 dependency entry 和 449 个 dependency step，`profession_skill_effect_value_flow_index.json` 已覆盖 23 个 no-immediate-arg effect value-flow 检查、69 个 entry 和 196 个 effect step，`profession_skill_original_accuracy_flag_index.json` 已把 105 个 false flag entry / 2,548 次 occurrence 拆入 8 个工作队列且 uncategorized=0，`profession_skill_runtime_accuracy_gaps.json` 已把剩余 cooldown/legality/scheduler、targeting/UI、read/context、parameter-value/receiver-identity、state-write/RNG、TalentBase learning UI 和起始档案集成风险拆成 132/132 通过的队列，`profession_skill_rng_recovery.json` 已把 DesperatePrayer、FindMonster、FindTreasure、Charismatic 和 Hoard 收窄为 20/20 通过的静态 RNG/random-reward 证据，`profession_skill_scheduler_recovery.json` 已恢复 CombatAbility/DungeonAction 基类 cooldown/use/OnClick/ClearMind、`Dungeon.WinFight` 外层顺序、`Dungeon -> DungeonPlayer.WinFight` 调用点、DungeonPlayer.WinFight cooldown tick 顺序、CoolingDownAbilities ready-message 条件、`GamePhysical.EndGame` coroutine 中的 `DungeonPlayer.CoolingDownAbilities(level)` 调用点、ready-message-before-WinFight yield ordering 和 `Dungeon.EndFight` slot-152 非 player receiver 边界，并已接入主缺口检查，`talent_learning_ui_recovery.json` 53/53 通过并固定 LevelUpReward/DungeonPlayer/SDB/ShopDialogueTalentViewer/CardBack/TalentBase 静态链路，`talentbase_reachability.json` 35/35 通过并固定 TalentBase type-model、实例/构造/learning source-call 证据边界，`talent_learning_source_call_xrefs.json` 扫描 24,879 个 method pointer 后只发现 2 条 static viewer 直接调用。剩余重点不再是基础 slot 命名、起始档案、CoolingDownAbilities 调用点、这条 ready-message/WinFight 相对顺序或 deterministic state-write/talent-handler/targeting-UI/effect value-flow fixture，而是 original RNG stream、shuffle/index stream、read/context value-flow、parameter-value/receiver-identity replay、alternate coroutine branch replay、控制流、分支条件、target legality、UI action-object 确认、live TalentBase UI/callback/payment replay 和 TalentBase 动态/遗留/存档构造路径。
5. 在现有 `schemas/` 基础上，等卡牌执行效果、怪物 AI/回合逻辑、职业/技能行为和地牢后处理继续恢复后，扩展成完整运行时规则 schema。
6. Texture2D PNG 已导出到 `extracted/textures/`，并在 `texture_export_manifest.json` / `.tsv` 保留 resource path 到唯一 PNG 的映射；`remaster_starter_asset_pack.json` 已把 starter fixture 收敛为 21 个 prototype 素材条目和 25 个 PNG 引用，5/5 检查通过；后续仍需按授权边界决定发布版是否使用原始素材。
7. 基于 `remaster_starter_slice.json` 实现第一版 prototype：10 张卡、3 个怪物、Warrior 职业和 floor 1 常量已经固定，且 `remaster_starter_asset_pack.json` 已提供对应 starter card、monster big/tile、Warrior big/little 与 floor 1 tile/background/badge PNG。`prototype/starter/` 已补上 Cards starter/all 370、Monsters starter/all 73、地牢生成规则摘要、Remaster audit/roadmap 状态和全职业/技能目录浏览面。`remaster_starter_smoke.json` 目前已通过 12 个解释器级 smoke check，`remaster_starter_combat.json` 已能用确定性简化规则跑 3 场 starter 战斗夹具且 `uses_shared_runtime_engine=true`，starter combat checks 88/88 通过；但完整 action execution、完整 card play replay、完整 zone/Die/Exile/DestroyDie/Bounce/Flick/Destroy/WipeClean/equipment lifecycle/object identity、visual-stack replay、AI/target/RNG/UI/timing 和 Unity physical 构造仍为阻塞未完成。
   `profession_skill_runtime_accuracy_gaps.json` 现为 132/132，并消费 `profession_skill_original_accuracy_flag_index.json` 的 13/13 false-flag index 检查；`profession_skill_original_accuracy_flag_index.json` 记录 105 个唯一 false flag entry、2,548 次 occurrence 和 8 个工作队列且 uncategorized=0；`profession_skill_scheduler_matrix.json` 24/24 通过，补充全部 60 个 cooldown initializer / 445 个 scheduler fixture case；`profession_skill_targeting_ui_bridge.json` 19/19 通过，补充 37 个 targeting/UI entry / 85 个 focused source-method case；`profession_skill_context_dependency_index.json` 31/31 通过，补充 67 个 read/context dependency entry / 449 个 dependency step / 13 个 owner queue；`profession_skill_effect_value_flow_index.json` 23/23 通过，补充 69 个 no-immediate-arg effect entry / 196 个 effect step / 9 个 parameter/receiver queue；`talent_learning_ui_recovery.json` 53/53 通过，补充 TalentBase viewer/card-back/pay-and-learn 静态链路；`talent_learning_source_call_xrefs.json` 补充 24,879 个 method pointer 的直接调用扫描，当前只命中 2 条 static viewer call；`profession_skill_rng_recovery.json` 20/20 通过，补充 DesperatePrayer、FindMonster、FindTreasure、Charismatic 和 Hoard 的静态 RNG/random-reward 证据，但 original profession/skill RNG stream、shuffle/index stream、read/context value-flow、parameter-value/receiver-identity replay、original targeting/UI action-object replay 与 live TalentBase learning UI replay 仍未完成。
   `card_runtime_accuracy_gaps.json` 现为 169/169，并消费 `card_original_accuracy_flag_index.json` 的 13/13 false-flag index 检查；`card_original_accuracy_flag_index.json` 记录 362 个唯一 false flag entry、8,052 次 occurrence 和 9 个工作队列且 uncategorized=0；`card_context_dependency_index.json` 14/14 通过，已把 214 张卡的 722 个 read/context dependency step 分入 11 个 owner 队列且 uncategorized=0；`card_effect_value_flow_index.json` 20/20 通过，已把 115 张卡的 179 个 computed/receiver value-flow step 分入 8 个 argument-flow 队列且 unclassified=0；其中 `card_equipment_lifecycle_matrix.json` 18/18 通过，覆盖 42 张 Equipment 卡的 74 个 source-method case、92 个 effect event 和 27 个 recorded event，original equipment timing/object identity replay 仍未完成；`card_ai_value_matrix_smoke.json` 14/14 通过，覆盖 370 个 `Card.AIWeight` case、370 个 base `Card.GetAIValue` identity case 且 scenario identity mismatch 为 0，`card_ai_priority_matrix_smoke.json` 20/20 通过，覆盖 370 个未反制 ActionPlay priority case、370 个反制 priority case、740 个 AIUseValue case 和 273 张分支 priority 会变化的卡，`card_actionplay_bridge_matrix_smoke.json` 42/42 通过，覆盖 370 个 normal ActionPlay candidate bridge case、370 个 full-hand filtered candidate case 和 370 个 synthetic action+target pair case，null action/payload/cost/slot/candidate-order/pair-order mismatch 均为 0。Blur/Incorporeal/StormShape/VaporForm 已从 `EffectPlayerAttribute..ctor` tailcall 反汇编恢复为 att=24 physical resistance / att=102 manaShield storm-shape / att=23 physical immunity 的 binary effect，Anticipate/Bewitch/Charm/Enthrall 已从 opponent-hand Action/TriggerOnce 反汇编恢复为 binary target-action effect，Scimitars 已从 `ActionScimitars` 的 `OnTrigger` / `PerformAction` 反汇编恢复为 `Player.DrawExNihil(slot 244)` + `Card.Temporary(slot 240)` 的 binary action effect，InfernalContract 已从 `PlayEffect` 反汇编恢复为 `VirtualObject.ChildCount -> Deck.AddCardToDeck(Curse) -> Deck.Shuffle -> Player.DiscardAll`，SoulCrush 已从 `PlayEffect` 反汇编恢复为状态 gate 后的 `Card.DealDamage(10)` + `Card.ForceDiscard(2)`，剩余 rules-text assisted runtime card count 为 0；`card_rng_recovery.json` 27/27 通过，补充 23 张卡牌 RNG/shuffle 候选的 25 个静态 helper/list-order 证据并二进制确认 Stone 的 Curse 插入、Deck.Shuffle 和 ForceDiscard 形态，且补充 InfernalContract 的 Curse+Deck.Shuffle+DiscardAll 形态，但 original card RNG stream、shuffle/index stream、parameter-value/receiver-identity replay 与 live AI value combat-state replay 仍未完成。
   `card_play_pipeline_recovery.json` 现在补足了 starter/prototype 仍缺的 Card.PayFor/Card.WillBeCountered/Card.PlayMe 下层静态证据和 Card.Exile/Card.Die/DestroyDie/Bounce/Flick/Destroy/WipeClean lifecycle 静态证据：50/50 检查通过；`remaster_starter_combat.json` 已把 normal-path ResourcesOff/CanAfford cost modifier catalog、card payment branch catalog、WillBeCountered Enemy.WillCounter priority boundary、Card.PlayMe order、inactive reaction catalog、Player.PlaySideEffects branch boundary/catalog、zone lifecycle/GetAction catalog、discard-to-Die boundary、Die graveyard boundary、DestroyDie/Bounce/Flick/Destroy/WipeClean static lifecycle catalog、deterministic lifecycle fixture、Sword equipment lifecycle boundary、Sword.CheckCounters equipment trigger boundary、Sword trigger gate boundary、card play 和 card zone fixture 接入 starter harness，但 active alternate cost/PayFor/WillBeCountered/Card.PlayMe 分支、完整 zone/Die/Exile/DestroyDie/Bounce/Flick/Destroy/WipeClean/equipment lifecycle/object identity、reaction/SBE cleanup、UI 和完整 timing 仍未 replay。
   `reaction_sbe_cleanup_recovery.json` 现在补足了 starter/prototype 仍缺的 ActionBase/Game reaction-SBE cleanup 静态证据：29/29 检查通过，并覆盖 TriggerCleanUp、ShouldCheckSBEs、CheckStateBasedEffects、Check*Reactions、ReactionCard、Counterspell、ActionDelayedReaction 和 ActionAddCounter；但 active reaction message identity、SBE cleanup timing、counter/delayed reaction end-to-end replay 和 prototype runtime readiness 仍未完成。
   `visual_stack_coroutine_recovery.json` 现在补足了 starter/prototype 仍缺的 GamePhysical visual-stack/coroutine 静态证据：42/42 检查通过，并覆盖 AddToVisualStack enqueue/dirty flag、NoYield scope、suppressed queue transfer、count/pop、HandleVisualStack/VisualStackSpin coroutine entry、VirtualWait/Waiter bridge 和 EndGame yield state；但 original visual-stack replay、Unity coroutine timing 和 prototype runtime readiness 仍未完成。
   `player_payment_recovery.json` 现在补足了 starter/prototype 仍缺的 Card.PayFor -> Player.PayFor/GainMana/UpdateMana/UnOneFreeSpell 静态证据：45/45 检查通过，并覆盖 action cost、ManaOff theft discount、mana clamp、UpdateMana text/UI bridge 和 one-free-spell consumption；但 original player payment replay、Unity payment UI replay 和 prototype runtime readiness 仍未完成。
8. 使用 `runtime_rule_quality.json` 作为 normalized rule 门禁：当前 3 个质量检查通过，跨卡牌/怪物/职业技能的 unresolved operation 总数为 0，剩余 context/no-rule 条目为 0，误标 `executable_rule_complete=true` 的条目为 0；但 `runtime_rules_complete=false`，因为 normalized rule 还没有全部变成可执行并带测试的 runtime engine。
9. 使用 `runtime_rule_backlog.json` 记录 normalized runtime 缺口：当前 `entry_count=0`，没有剩余 priority backlog；下一阶段队列应从已结构化的卡牌、怪物、职业/技能规则中挑选可执行 handler 和回归测试，而不是继续补 context/no-rule recovery。
10. 使用 `priority_runtime_smoke.json` 跟踪 priority 1、former backlog、deck-encoded monster、monster level-up、怪物/卡牌/职业/技能/地城动作首批可执行夹具：当前 420/420 通过，`shared_runtime_engine_check_count=420`，覆盖 `BootsOfSpeed` 回合开始抽牌/加 action、`Scimitars` 非 temporary Attack 复制、`PrayerOfViolence` 最后 counter 伤害表、`GaussHourglass` 空 play area 时按 `maxHandSize` 安排下一手额外抽牌、`Curse` 不可主动打出且回合末死亡、`PenaltyCurses*`、`Demon`、`GiantSquid`、`Hydra`、全部 25 个 `behavior_encoded_in_deck` 怪物 loadout、全部 73 个 monster level-up rules、怪物属性/状态/记录型行为、卡牌高频和剩余记录型 effect、combat ability、profession hook，以及 32 个 dungeon action recovered-effect fixture；这些夹具已通过保守共享 interpreter 执行，但仍明确标记为 rules text + trace/disassembly/loadout inference，不是完整原版 runtime engine 证明。
11. 使用 `runtime_engine_coverage.json` 排下一批工程化工作：当前共享 interpreter 支持 1442/1442 个 normalized effect/handler steps（100% 标签覆盖），其中 cards 633/633、monster behavior 137/137、monster level-up 346/346、profession 57/57、combat ability 61/61、dungeon action 148/148、dungeon talent handler 60/60。下一步不再是补 unsupported effect 标签，而是把保守记录型 handler 升级为原版准确的控制流、时机、AI、RNG、UI 选择和对象生命周期语义。
